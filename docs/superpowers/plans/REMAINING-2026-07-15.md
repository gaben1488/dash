# REMAINING — единый свод остатка до целевого продукта (2026-07-15)

> **Статус: ЕДИНСТВЕННЫЙ живой реестр остатка.** Поглощает открытые пункты
> мастер-плана «Отчёт 2.0», поднятого плана 14.07, EXECUTION_PLAN_2026-07-10,
> BUG_BLOCKER_REGISTER, ретроспективы месяца, код-аудита 15.07, свеп-реестра и
> триажа сигналов. Всё, чего здесь нет, — либо закрыто (см. §2 с коммитами),
> либо отброшено (см. §2, с причиной). Проверено по коду на HEAD `5b50b85`
> (ветка `claude/refactor-consolidation`).
>
> Решения пользователя 15.07, вплетённые в эпики: D1 — канон строк это
> **отдельные книги**; Реестр = **Google Sheets и лучше**; ЦДТ — учреждение УО
> (закрыто `8476c34`); сайт остаётся **открытым**; ЕИС — **потом**;
> рекомендации — **сначала вывести готовое ядро**.

---

## §1. Карта эпиков до цели

Оценка — в сессиях (1 сессия ≈ 1-2 закоммиченных таска с зелёным гейтом,
калибровка по факту фазы 0: 8 задач за день). Статусы: не начат / частично /
**in-progress** (исполняется прямо сейчас параллельным агентом).

| # | Эпик | Зачем (ценность) | Что входит | Сессий | Зависит от | Статус |
|---|---|---|---|---:|---|---|
| E1 | **Сигнальный контур честности** | 1632 срабатывания → ~1000 честных; сверка перестаёт светить 861 ложным красным за 2025; сигналы становятся пригодными для еженедельного отчёта | Техзадачи триажа §3.1-3.8 (`signal_audit_2026-07-14.md`): economyFlag ≠ канон AD «да/нет» (`signals.ts:343`), lowCompetition/factDateBeforePlan/earlyClosure/factWithoutDate/overdue/financeDelay, словарь ЕП-кластеров; плашка 5.4-A «расчёт за 2025 не построен» (грепом подтверждено: в коде нет); удалить budgetMismatch (deprecated); доверификация P0-4 (`reconcile.ts` crossVerifyQuarterly `'all'`) и P0-9 (toggleMethod) | 1-2 | — | **in-progress** (§3-фиксы — параллельный агент этой волны; 5.4-A и P0-4/9 — не начаты) |
| E2 | **D1-переезд: отдельные книги — канон строк** | Решение пользователя 15.07. Убивает крупнейший дефект QA (УЭР 35 vs 76 строк, МКУ «ЦЭР» невидим, УАГЗО Δ=+60 млн): одно число — один источник | Пульт/Свод/dashboard-DTO пересаживаются на отдельные книги (канон `readDeptSheet` уже готов, `3c073c5`+`f8e775a`); сводная книга уходит в роль сверки/выгрузки; характеризация чисел до/после; бейдж происхождения на переход | 2-3 | E1 (числа не должны врать до пересадки) | не начат |
| E3 | **Отчёт 2.0: проекция + страница + сутевая из отчётов Drive** | Ядро продукта: dash содержит всё, что писали в отчёте руками. Закрывает «Показано 0/21» контрольного списка | Фаза 1.4-1.6 (buildReport-проекция, реверс-инжиниринг эталонной недели из `Отчеты по закупкам/`, `/api/report`); фаза 2 (каркас с оглавлением, claim→оверлей-доказательство, блоки КП/ЕП/8 ГРБС, «что изменилось за неделю» — оживить мёртвый `changeWindow`, `store.ts:184`); систематический захват сути ВСЕХ отчётов Drive (не одной недели): нарратив причин Δ8, phrase-bank, еженедельный процедурный формат | 5-7 | E1, E2 | частично (фаза 1.1-1.3 закрыта: `df55907`, `443faa1`, `afbb771`; 1.4+ не начаты) |
| E4 | **Проводка готового ядра (Трек F, остаток)** | ~40% ядра построено и не видно. Решение 15.07: «сначала вывести ядро». Минимум работы — прямой ответ на запрос | scorecard-стек (роут `/api/analytics/scorecard` без клиента), compliance-44fz, `snapshot.datasetAnalyses` → DTO + панель (греп: в server ноль упоминаний), wire `recommendations.ts` (0 импортёров — проверено на HEAD), судьба каждого из ~12 orphan-роутов и 19 мёртвых функций `api.ts` (провести или снести) | 2-3 | — (параллельно всему) | частично (централизация выведена `96d95f0`) |
| E5 | **Модуль совмещения** | Эпик пользователя: похожие названия закупок/обоснования/сроки → предложения совмещения с прогнозом экономии. 70% кода уже есть | Ур.1: honest-похожесть в `/api/rows/subjects` (сейчас containment>0.7, коммент врёт «Levenshtein») + cross-ГРБС кандидаты (centralization уже на экране); Ур.2(б): свой справочник названий (развить `subject-classify.ts`: лемматизация + кластеризация; НЕ ОКПД2/КТРУ — решение 14.07); Ур.2(а): контролируемый словарь причин/оправданий из свободного текста AE; совмещение по срокам (окно план-дат) | 3-5 | E4 (проводка основы) | частично |
| E6 | **Мониторинг незаключённых** | Эпик пользователя: 385 незаключённых планов — крупнейшая организационная корзина триажа (пользователь 15.07: «важны») | Вью «осталось отыграть» (C-4, сигнал `planWithoutExecution` есть); объяснение по строке (AE/AF, заполнено 91%, C-1) + оценка качества объяснения (словарь причин из E5); переносы дат: snapshot-diff план-дат = антифрод Δ4 «переброс квартала» + Δ5 «внесено под факт»; жизненный цикл позиции (причина → решение → снятие/дата) | 2-3 | E5 (словарь причин), частично сам по себе | не начат |
| E7 | **Рекомендации + реакции** | Замыкает цикл аналитика: рекомендация → реакция ГРБС (принята/нет/частично + обоснование) → история. ~40% ручного отчёта — этот контур | Фаза 3 мастер-плана: сущность Recommendation со стабильным ключом, таблица `recommendation_reactions`, UI-ввод, архив исчезнувших, временна́я честность к снапшоту; Δ2 жизненный цикл, Δ3 экономия→рекомендации, Δ6 кандидаты ЕП→ЭА (движок из E4) | 2-3 | E4 (wire recommendations), E5 (кандидаты совмещения) | не начат |
| E8 | **Реестр = Google Sheets и лучше** | Решение 15.07: Реестр должен уметь всё, что Sheets, плюс провенанс и сигналы — иначе миграция (E13) невозможна | Фаза 4.1: правки с провенансом (old_value), пресеты сигналов, семантика сортировки (вопрос §4), C-1 комментарий ГРБС по клику; **экспорт полных данных при активных фильтрах** — просьба, потерянная дважды (06-14 и 07-09; правка export-путей `api.ts:186-190`); массовые операции, undo | 3-4 | E2 (канон источника) | частично (фаза 0 честности фильтров закрыта: `8795e25`…`14a91fd`) |
| E9 | **Переплавка IA: 7 страниц → 4 раздела + чистка мёртвого** | Старая IA при готовой новой спеке; тупики навигации; мёртвый код вводит в заблуждение | Фаза 4.2-4.4: Лаборатория (весь аудит-слой наружу: Benford/Z/сезонность/дробление, 12 графиков), Данные (сверка с категориями + доверие + журнал + замечания), удаление 6 мёртвых компонентов, мёртвых таблиц `procurement_rows`/`input_errors`, bundle split (Known Residual) | 4-6 | E3 (Отчёт — новая главная) | не начат |
| E10 | **Dash-конструктор** | Эпик пользователя: гибкость/настраиваемость — пользователь собирает свой экран, а не живёт в зашитой раскладке | Shape-бриф (что настраивается: блоки/метрики/оси/пороги?) → конфигурация видимости и порядка блоков, сохраняемые пресеты (view-конфиги в БД), пер-виджет выбор осей 6-мерной модели; фундамент — словарь продукта и реестр метрик уже SSOT | 3-5 | E3, E9 (перестраивать стоит новую IA, не старую) | не начат (нужен shape с пользователем) |
| E11 | **Инженерный фундамент: god-файлы + дубли + типы** | Без него переплавка плавит god-файлы; drift-дубли и 165 any в web — источник будущих багов | Трек A: 0/12 порезано — первыми Recon.tsx 1305, rows.ts 1121, useFilteredData 909 (замер на HEAD), затем 4 страницы >1000; свеп-хвосты: SIGNAL_LABELS ×3 (`product-dictionary.ts` vs `DataBrowser.tsx:52` vs `RowDetailCard.tsx:22` — живы), QUARTER_MONTHS ×2 (живы), magic-индексы `rows.ts:864,1072,1076,1110` (живы), форматтеры денег/% → `lib/format.ts`, classifySheet-литералы ×5, `normalize.ts` serial-порог 50000→60000; типизация `api.ts` (гасит корень 165 any и 279 lint-warnings); snapshot-тест DTO `dashboard.ts` (600 LOC, 0 тестов) | 5-8 | — (параллельно, но rows.ts/useFilteredData — ДО E8/E9) | частично (свеп-баги 3/3 закрыты; безобидные семьи 0/5; резка 0/12) |
| E12 | **Кросс-анализ + simplify + ревизия ядра** | Эпик пользователя: полномасштабный кросс-анализ + simplify + code review; каждый модуль «доказан», каждый сигнал измерен | Фаза 5.1-5.3: ревизия ядра модуль-за-модулем (математика на реальных данных / право 44-ФЗ актуальной редакции) с вердиктом и тест-свидетельством; единый тип «процент» (чинит «1 вместо 100%»); прогон code-review/simplify по пакетам; инвариант-тесты `rule-book.ts`/`check-registry.ts` (реестры без тестов); сужение публичной поверхности core (37/56 экспортов без потребителей), словари-заглушки shared (27 TODO, 0 потребителей) | 3-4 | E1 (сигналы уже перемерены) | частично (аудиты 15.07 = разведка; исполнение не начато) |
| E13 | **Целевая схема данных + уход от таблиц** | Эпик пользователя «целевая схема»: dash — источник истины, лист — выгрузка. Настоящий блокер — идентичность строки (позиционный ключ) | Этапы `docs/PLAN.md` §4: двусторонний журнал (`_ChangeLog` читать/писать), устойчивый ключ строки (ИКЗ где есть — заполнен на 24%, иначе суррогат, записанный в лист), `procurement_rows` в дело (автор/версия/время), ретенция снапшотов (рост до 878 МБ уже случался), решение по PG (ADR-0001) | 4-6 | E8 (Реестр+), E14 (RBAC — предпосылка владения) | не начат (модель описана, кода 0) |
| E14 | **RBAC + прод-платформа** | Без логина и прав 8 ГРБС не могут владеть данными; прод не должен умирать молча (502 висел незамеченным 3 недели) | Фаза 7: ADR-0008, users/roles/sessions, login-flow вместо localStorage (Known Residual), RBAC-middleware per-ГРБС; прод: TLS, убрать Caddy-инъекцию ключа, внешний healthcheck, rollback-теги, backup aemr.db; до оплаты VPS можно: rate-limit на публичный `/api/refresh` (спорное решение №4 — тихий DoS квоты Sheets), ретенция, схема users | 4-6 | внешний блокер: VPS не оплачен (часть — pre-VPS) | не начат |
| E15 | **ЕИС-слой** | Стадии закупки (строки 8/16 списка), поставщики/ИНН, single-bid, совместные закупки — то, чего в книгах ГРБС нет в принципе | Фаза 6: импорт xlsx OrderSearch/ContractSearch из `ПЛАН-РЕЕСТР…`, сопоставление строка↔ЕИС (ИКЗ — главный рычаг), стадии в отчёте, концентрация поставщиков, совместные закупки как эффект (УО бумага −24.7% на 35) | 3-5 | E13 (ключ ИКЗ), решение 15.07 «потом» | **отложен** решением пользователя 15.07 |

Суммарная оценка активного фронта (E1-E14, без отложенного E15): **~40-55 сессий**;
критический путь до «продукт виден» (E1→E2→E3) — **8-12 сессий**.

---

## §2. Дедуп: судьба пунктов старых реестров

### 2.1 Закрыто кодом (не числить в долге)

| Пункт старого реестра | Коммит-доказательство |
|---|---|
| Мастер-план фаза 0 целиком (0.1-0.9: год/бюджет Реестра, сигнал-виджет по ГРБС, «Δ кв.», тексты Экономии, баннер года, перепись и разбор сверки) | `8795e25`, `b41f71c`, `78c48c6`, `a53a4ad`, `7cb877e`, `14a91fd`, `58d0d29`, `9938bbc`; чекбоксы в мастер-плане не проставлены — doc-drift, не работа |
| Мастер-план фаза 1.1-1.3 (словарь продукта, орг-классификатор, quarterExecution) | `df55907`, `443faa1`+`7f658b7`+`8476c34`, `afbb771` |
| docs/PLAN.md дорожка A (рефакторы G/E/F/D/C-narrow, 6 пунктов) | закрыта 07-11, `f4a56b0`…`abc94fd` |
| docs/PLAN.md дорожка B (14 подтверждённых багов B-1…B-14) | закрыта 07-11, `5485fce`…`6560b3a` |
| BUG_BLOCKER: 16 CONFIRMED первой волны + все P0 | через дорожку B; B-10 опровергнут перепроверкой (`eced5ee`-нота) |
| Свеп семья №1 (parseSheetDate-канон, seasonal serial) | `f8ae81a`, `a01581e` |
| Свеп семья №2 полностью, включая write-хвост `getDeptSheetName` | `3c073c5`, `f8e775a` (греп на HEAD: функции больше нет) |
| Свеп семья №4 (snapshot latinize → findDept) | `c8cfb1e` |
| Код-аудит топ-1: Settings.tsx голый fetch без Bearer (прод-401) | `6a118bc` |
| Трек F п.1: централизация на экране | `96d95f0` (Analytics.tsx:1040, api.ts:217) |
| Ретро §2а-2: триаж 1528 срабатываний по 4 корзинам | написан `qa/signal_audit_2026-07-14.md` (1632 срабатывания, 4 корзины, пакет LLM-исполнителю, поручения по ГРБС) |
| Ретро §2в: `docs/superpowers/` в .gitignore («фабрика потеряшек») | `f72a4af` |
| Ретро §3-7: ЦДТ | `8476c34` — учреждение УО (решение пользователя) |
| Один путь резолва листа + канон-валидация + ШДЮ→«СВОД с месяцами» | `3800ac1` |
| Публичный read-only деплой (Render) + пожарные фиксы | `f8c7233`, `f3e39e6`, `dbd15d3`, `2b4b88a` |

### 2.2 Отвечено пользователем 15.07 (вопросы ретро §3 — сняты)

D1-канон = отдельные книги (→ E2); Реестр = Google Sheets+ (→ E8); ЦДТ =
учреждение УО (закрыто); приватность — сайт остаётся открытым (вопрос снят;
техостаток — rate-limit на `/api/refresh` — в E14); ЕИС — потом (E15 отложен);
recommendations.ts — не удалять, сначала вывести ядро (→ E4/E7); claude-mem —
«пока не трогай» (заморожен, из реестра исключён).

### 2.3 Слито в эпики (маппинг старое → новое)

- Мастер-план фазы 1.4-1.6 и 2 → **E3**; фаза 3 → **E7**; фаза 4 → **E8+E9**;
  фаза 5 → **E1** (5.2, 5.4) + **E12** (5.1, 5.3); фаза 6 → **E15**; фаза 7 → **E14**.
- Поднятый план: Трек A → **E11**; Трек B хвосты (5 безобидных семей) → **E11**;
  Трек C → **E5**; Трек D (Δ1-Δ8) → **E3** (Δ1, Δ7, Δ8), **E6** (Δ4, Δ5), **E7**
  (Δ2, Δ3, Δ6); Трек E (триаж) → выполнен, техзадачи → **E1**; Трек F → **E4**.
- EXECUTION_PLAN_2026-07-10 (сам помечен SUPERSEDED → docs/PLAN.md): фаза 2
  (anchor-assertion, subordinate-registry 3/72 → реальные 77, geometry-validation,
  google-sheets hardening-остаток) → **E12/E2**; фаза 3 → **E14**; фаза 4 →
  **E3/E6/E7/E15** (4.9 экспорт → **E8**); фаза 5 → **E9/E11**; фаза 6 → **E13/E14**.
- docs/PLAN.md дорожка C: C-1 → **E6/E8**; C-2 → **E4/E7**; C-3 → **E3** (блок ЕП);
  C-4 → **E6**.
- BUG_BLOCKER остаток: P0-4, P0-9 → **E1**; P0-7 (DataBrowser period-фильтр без
  даты) → **E8**; 6 plausible (Caddy-инъекция, TLS, rollback, deploy-скрипт печатает
  ключ, branch-protection) → **E14**; subordinate-registry → **E2/E12**;
  mapping-editor no-op → **E12**.
- Ретро §4 спорные решения: №4 refresh-квота → **E14**; №5 метрика «в срок» (D8) →
  вопрос §4-3; №1/№2/№8 юр-канон орг-классификатора → **E12** (fixture-таблица
  пользователю); №7 attachUnifiedGrid тихий skip → **E12** (счётчик).
- Mode B «СВОД с месяцами» как режим страницы (tombstone `svod-modes`) → **E9**
  (переплавка Свода; кирпичи в родителе tombstone-коммита).
- CLAUDE.md Known Residual Work: `any` → **E11**; browser login → **E14**;
  bundle split → **E9**.
- SYSTEM_SEAMS ждущие: #9/#10 прод → **E14**; #12 deploy-ключ → **E14**;
  #13-#15 (SessionStart, vault-синк, stale-green CI) — процессные, вне продукт-реестра.

### 2.4 Отброшено как устаревшее (с причиной)

- **EXECUTION_PLAN_2026-07-10 как оперативный документ** — сам заголовок:
  «SUPERSEDED → docs/PLAN.md»; оценки и фазы поглощены этим реестром.
- **B-10 savings_pct** — опровергнут перепроверкой (колонка Q «Потрачено, %»,
  документировано + guard-тесты).
- **ОКПД2/ОКВЭД/КТРУ-справочник** — не нужен (решение 14.07: свой справочник по
  фактическим названиям; коды ЕИС — вспомогательный сигнал).
- **EXECUTION_PLAN фаза 7 «генератор отчётов»** — dash не генерирует .docx
  (docs/PLAN.md §0); «может никогда».
- **G-4 (остаток dataset-signals 660)** — помечен опциональным, ниже порога god.
- **budgetMismatch** — deprecated-сигнал, честный ноль; удаление — мелкий пункт E1.
- **План 2026-06-05-change-history-phase-c** — реализован (changeWindow в store,
  DeltaBadge, /api/history/*), но фича мертва в UI; возрождение — пункт E3
  («что изменилось за неделю»), сам план закрыт.
- **Файл `reports/all_user_msgs.txt`** — устарел (обрыв 06-14), регенерировать
  при следующей ретро, в долг не числить.

---

## §3. Критический путь

Что блокирует что (цепочка ценности «пользователь видит продукт»):

```
E1 (сигналы честные, in-progress)
 └→ E2 (D1: отдельные книги — канон)          ← решение уже есть, только исполнение
     └→ E3 (Отчёт 2.0: проекция + страница)    ← сердце продукта
         ├→ E9 (переплавка IA)  └→ E10 (конструктор)
         └→ E7 (рекомендации+реакции) ← E4 (проводка ядра) + E5 (совмещение)
E8 (Реестр Sheets+) ← E2                        └→ E6 (незаключённые) ← E5
E13 (целевая схема) ← E8 + E14 (RBAC)           E15 (ЕИС) ← E13, отложен
Сквозные, параллельно: E4 (дёшево, сразу), E11 (резка ДО переплавки), E12
```

Словами: **E1 → E2 → E3** — обязательная последовательность (нельзя строить
отчёт-проекцию на двух источниках строк и лгущих сигналах). E4 не блокирован
ничем и даёт максимум ценности за минимум работы — запускать параллельно
немедленно. E5→E6/E7 — продуктовая ветка после проводки. E11 (резка rows.ts,
Recon.tsx, useFilteredData) обязана предшествовать E8/E9, иначе переплавка
плавит god-файлы. E13→E14 — хвост дуги, E15 отложен пользователем.

**Первые 3 конкретных шага:**

1. **Добить E1 до конца** (сейчас in-progress): принять фиксы §3.1-3.8 параллельного
   агента (первый — `economyFlag` в `packages/core/src/pipeline/signals.ts:343` на
   канон AD «да/нет», гасит 77 ложных), поверх — плашка 5.4-A в recon-DTO
   («расчёт за 2025 не построен — сравнение неприменимо» вместо 861 high) + TDD;
   заодно точечная доверификация P0-4/P0-9. Полсессии-сессия.
2. **Экспорт полных данных при активных фильтрах** — самая старая живая просьба
   пользователя (дважды: 06-14 и 07-09): править export-пути
   `packages/web/src/api.ts:186-190` (сервер уже отдаёт всё) + тест. Полсессии,
   вне критического пути, но это прямой долг перед пользователем — закрыть до
   больших эпиков.
3. **Начать E2**: характеризационный тест чисел Пульта/Свода на текущем источнике
   → пересадка dashboard-DTO на отдельные книги через `readDeptSheet` → сводная
   книга переводится в роль сверки; расхождения УЭР 35/76 и МКУ «ЦЭР» исчезают
   по построению. Затем сразу E3 (фаза 1.4 buildReport).

---

## §4. Открытые вопросы пользователю (реально неотвеченные после 15.07)

1. **Прод-VPS 502.** Сайт решено держать открытым (Render крутится), но VPS-прод
   лежит с ≤07-09. Считать ли Render заменой до оплаты VPS и снять красный флаг
   из пульса осознанно — или выполнить одну ssh-команду подъёма? И внесены ли
   на Render живые Google-креды (`render.yaml:28-32`) — крутится он на реальных
   данных или на демо?
2. **Допотчёт, 2 оставшихся вопроса** (из 3 в дельта-спеке §7; про ОКПД2 снят
   решением «свой справочник»): (а) показывать раздельно «вся экономия»
   (57,7 млн) и «экономия от рекомендаций» (1,48 млн)? (б) почему в таблице
   допотчёта 293 ЭА, а в шапке 294 — какая цифра истинная?
3. **Метрика «исполнено В СРОК» (QA D8).** `quarterExecution` воспроизводит
   формулу листа (факт засчитывается кварталу плана без сравнения дат) — нужна ли
   рядом вторая, строгая метрика «в срок к плановой дате», где 244 просрочки не
   соседствуют со 100%?
4. **Семантика сортировки Реестра** «по большему количеству пунктов» (наблюдение
   07-13) — что именно должно быть наверху? Нужно для E8.
5. **Dash-конструктор (E10): что настраивается?** Минимальный shape-бриф: набор и
   порядок блоков? выбор метрик/осей в каждом виджете? пороги сигналов? именованные
   пресеты на пользователя? Без этого эпик не проектируется.
6. **«Не заключён» / silent-loss-правка** — вопрос, который до пользователя так и
   не был донесён (след потерян, греп по репо пуст): требуется восстановить
   формулировку из сессии-источника и задать заново — фиксирую как долг агента,
   не пользователя.
7. **Пакет подтверждений юр-канона** (одним взглядом, из спорных решений ретро §4):
   (а) таблица «строка C → класс» орг-классификатора (аппарат/ПБС), (б) куда класть
   суммы «совместных закупок» при агрегации по сети, (в) новая разбивка сети УО
   после МКОУ/МКДОУ→казённое.


---

## Поправка 2026-07-16 — рамка «++турбо мега ремейк» (слово пользователя)

1. **Канон-поправка:** СВОД ТД-ПМ содержит ВСЕ 4 квартала (ЭА/ЕП × ВСЕ+8 ГРБС)
   + итоги «2025+2026» и «2026» (лист двухлетний). report-map покрывает ~треть
   (Q1+год) — прочее никогда не мапилось. → **Ре-реверс report-map** входит в E3.
2. **Рамка переплавки ужесточена:** вывод ядра (страницы, KPI-витрины, карточки)
   после прокачки движка до 100/100 переплавляется ЦЕЛИКОМ от продукта
   (эталонный отчёт + решения пользователя), не инкрементально. Куски
   переиспользовать можно, структуру вывода по инерции — нельзя. Пример
   протухшего слоя: KPI-строка Аналитики = зеркало Q1-ячеек официала (снять
   с витрины, оставить в сверке).
3. Сверка-2025 (задача #5) получает контекст: официал двухлетний (2025+2026) —
   категоризацию 970 вести с учётом двухлетней системы координат листа.

## Поправка 2026-07-16 (вечер) — принцип «связно под дизайн» (слово пользователя)

«Организовать все страницы и все элементы на них связно под дизайн, а не ловить
поштучно/разбросано по коду». E9-переплавка ужесточается до **Page Contract**:
1. Канонический реестр элементов (KPI-плитка, карточка, таблица, сигнал-бейдж,
   плашка) — единственный строительный материал страниц.
2. Каждый элемент ОБЯЗАН по типу: FilterContext (не добровольно), бейдж
   источника (Расчёт/СВОД/Комби), подписи из словаря продукта, дизайн-токены.
   «Забыл ось»/«сырой ключ» = ошибка компиляции, не находка QA.
3. Порядок: FilterContext-контракт (волна 16.07) → реестр элементов + шаблон
   страницы → ВСЕ страницы пересобираются из реестра связно (не по одной как
   получится). Дизайн-язык один, поведение фильтров одно.
