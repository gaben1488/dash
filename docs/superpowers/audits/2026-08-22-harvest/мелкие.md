---
title: Разбор оборванных агентов — доля «мелкие» (записи 169–251)
date: 2026-08-22
status: готов
---

# Разбор оборванных агентов: мелкие (записи 169–251 реестра)

Доля охватывает 83 записи реестра `dead-agents.txt` — все транскрипты размером
от 101 до 107 КБ. Это самый хвост распределения: агенты, чей журнал по объёму
равен собственному системному приглашению и поручению, без единой строки работы.

## Как проверялось

Транскрипты найдены по идентификатору агента в
`C:/Users/filat/.claude/projects/C--Users-filat-dash/*/subagents/workflows/wf_*/agent-*.jsonl`
(всего в проекте 741 файл журналов субагентов; для всех 83 записей доли файл
нашёлся, потерянных нет). Чтение потоковое, построчным разбором JSON, скрипт —
`C:/Users/filat/AppData/Local/Temp/claude/C--Users-filat-dash/b3efda50-05f5-4fc7-a780-803fa40b68ed/scratchpad/scan_small.py`,
выгрузка — `small_scan.txt` в том же каталоге. Из каждого журнала извлекались:
число строк, число сообщений исполнителя, все вызовы инструментов, все адреса
записи (Write/Edit) и весь текст, включая размышления.

## Главный результат замера

Картина по доле однородна до буквальности:

| Замер | Значение |
|---|---|
| Записей в доле | 83 |
| Журналов найдено на диске | 83 из 83 |
| Строк в журнале | 4 (у записи 250 — 3) |
| Сообщений исполнителя | 1 (у записи 250 — ноль) |
| Вызовов инструментов | 0 у всех 83 |
| Записей в файлы | 0 у всех 83 |
| Объём осмысленного текста | 0 символов у всех 83 |

Единственное сообщение исполнителя в каждом журнале — это строка отказа среды.
Всего восемь разных строк на 83 журнала:

| Строка обрыва | Сколько агентов | Номера записей |
|---|---|---|
| `You've reached your Fable 5 limit. Run /usage-credits to continue or switch models with /model.` | 47 | 169–190, 192–196, 198–210, 213, 218, 223–227 |
| `You've hit your session limit · resets 12:40am (Asia/Kamchatka)` | 11 | 212, 222, 230, 237, 243–249 |
| `API Error: Connection dropped (ECONNRESET)` | 10 | 215, 217, 232, 236, 238–242, 251 |
| `You've hit your weekly limit · resets Aug 13, 9am (Asia/Kamchatka)` | 5 | 191, 214, 216, 219, 221 |
| `You've reached your Fable 5 limit. Switch to another model, or manage usage credits…` | 5 | 211, 220, 228, 229, 231 |
| `You've hit your session limit · resets 2pm (Asia/Kamchatka)` | 3 | 233, 234, 235 |
| `API Error: Unable to connect to API (ECONNRESET)` | 1 | 197 |
| текста нет вовсе | 1 | 250 |

Отсюда вывод, который снимает пункт «что успел» для всей доли: **ни один из 83
агентов не успел ничего**. Не было ни одного чтения файла, ни одной команды, ни
одной мысли по существу задачи. Семьдесят один агент из 83 (все, кроме
одиннадцати с разрывом соединения и записи 250) погиб не от связи, а от
исчерпанного лимита:
поручение доехало, а права на первый же вызов модели уже не было.

Поэтому графа «ценность находок» для всех 83 записей — **ПУСТО**, и дальше
документ отвечает только на четвёртый вопрос: жива ли ещё сама задача.

## Разбор по волнам

Задачи в доле повторяются: одна волна плодила десятки одинаково устроенных
исполнителей, и погибали они пачками. Поэтому разбор идёт по волнам, а
поимённая таблица всех 83 записей — ниже.

### 1. «report-full-eval» — 31 запись (169, 171, 175, 177–182, 185, 186, 189, 190, 192–195, 198–210, 213)

**Кто это был.** Придирчивый проверяющий: каждому давали одну готовую находку по
тракту вкладки «Отчёт» — с адресом до строки, сценарием и предложенным
исправлением — и требовали не согласиться, а попытаться её опровергнуть, читая
код и прогоняя пробы. Тридцать один агент — тридцать одна разная находка.

**Что успел.** Ничего: у всех тридцати одного журнал обрывается на строке об
исчерпанном лимите модели, до первого чтения файла.

**Ценность находок — ПУСТО**, но с оговоркой, которая делает эту группу самой
полезной в доле. Сами находки не потеряны: они целиком лежат в тексте поручений
внутри журналов. Знание принадлежит не погибшему агенту, а волне-охотнику,
которая его добыла раньше; проверка же не состоялась ни по одной находке.
Ниже, в разделе «Что уцелело в поручениях», все тридцать одна сверены с кодом
на 22.08.2026 — часть дефектов с тех пор починена, часть жива дословно.

**Судьба задачи — РАЗДЕЛЬНАЯ.** Вкладку «Отчёт» с тех пор переплавили:
`packages/web/src/pages/Report.tsx` вырос до 90 649 байт, интегральная сводка
пересобрана в «четыре яруса» (`Report.tsx:1193`, `lib/report/mappers.ts:314`).
Часть находок этой переработкой закрыта, часть — нет. Поштучные вердикты ниже.

### 2. «week-axis-deep-eval» — 10 записей (170, 172–174, 176, 183, 184, 187, 188, 196)

**Кто это был.** Тот же скептик, но по другому объекту: конструкция «одна ось
недели» — колесо недель, четверговый срез, слепки, хранение и добор истории.
Оценивался незакоммиченный диффs новыми службами `snapshot-retention`,
`weekly-snapshot-cron`, `product-calendar`, `backfill-weekly`.

**Что успел.** Ничего, тот же лимит.

**Ценность — ПУСТО**, находки — в поручениях (см. ниже).

**Судьба.** Конструкция с тех пор закоммичена: все четыре службы лежат в
`packages/server/src/services/`, скрипт добора — в `packages/server/scripts/backfill-weekly-snapshots.ts`.
Три находки из десяти подтверждаются в сегодняшнем коде дословно.

### 3. «grand-remelt» — 8 записей (211, 212, 220, 222, 228–231)

**Кто это был.** Волна большой переплавки, четыре разные роли по две штуки:
консолидация сигналов (211, 212), вывод сирот ядра на экран (220, 222),
критик-приёмщик полноты волны (228, 230), инженеры страниц — свод и реестр (229),
аналитика и таймлайн (231).

**Что успел.** Ничего: пятеро уперлись в лимит модели, трое — в лимит сессии.

**Ценность — ПУСТО.**

**Судьба — СДЕЛАНА ПОЗЖЕ.** По каждой роли есть доказательство в коде:

- консолидация сигналов, пункт «этапность при заключённом переводится в
  информационный»: `packages/core/src/pipeline/comment-consistency.ts:362-374` —
  сигнал `stage_marker_when_signed` теперь несёт текст «Это справка, а не ошибка»,
  а комментарий на строке 349 прямо называет «информационный тон, а не жёлтое
  предупреждение»;
- сироты ядра на экран: каталог `packages/web/src/components/analytics-extra/`
  существует и содержит `AnomalySignsSection.tsx`, `IntegritySection.tsx`,
  `useExtraSignals.ts` и страж `contract.test.ts` — ровно та развязка, которую
  поручали (отдельные компоненты плюс подключение одной строкой);
- режим подведов: `packages/web/src/lib/selectors/org-scope.ts` построен, при нём
  `org-scope.test.ts` и `subordinate-override.test.ts`.

Не подтверждается пресет-срез Реестра «AD пуст при числовой экономии»: поиск по
`packages/web/src/lib/rows/` и `packages/shared/src/` этой строки и близких
идентификаторов не нашёл. Этот пункт остаётся долгом.

### 4. «wf_9cc94119-eca» — 6 записей (238–242, 250)

**Кто это был.** Инженеры вкладок волны 21.08, по одному на зону: контроль и
дисциплина (238), экономия и конкуренция (239), аналитика и таймлайн (240),
отчёт и свод (241), реестр и корзины (242), плюс хранитель атласа (250).
Задача инженеров одна и та же: провести каждую карточку своей зоны через
обязательный минимум — паспорт периметра, момент чтения, происхождение числа,
провал к строкам-основаниям, честная пустота трёх родов, изоляция организаций,
снятые обводки в тёмной теме.

**Что успел.** Пятеро инженеров — ничего, обрыв соединения (ECONNRESET). Хранитель
атласа (250) уникален во всей доле: у него в журнале три строки и **ни одного**
сообщения, даже строки ошибки нет. Он не начал.

**Ценность — ПУСТО.**

**Судьба — РАЗДЕЛЬНАЯ.**

- Инженеры вкладок (238–242): **СДЕЛАНА ПОЗЖЕ** сменившими составами. Механизмы,
  которых требовало поручение, есть и подключены: `perimeter`/`drill` встречаются
  в `Analytics.tsx`, `Dashboard.tsx`, `DataBrowser.tsx`, `Economy.tsx`,
  `Monitoring.tsx`, `Report.tsx`, `Trust.tsx`. Строка 3 того же реестра
  (агент `af68efb9916c`, зона «реестр+корзины») показывает, что в этой же волне
  уцелевший состав успел записать девять файлов — в том числе
  `packages/web/src/lib/rows/registry-view.ts`, `row-status.ts`,
  `registry-perimeter.ts` со стражами.
- Хранитель атласа (250): **НУЖНА**. Проверено дословно по двум признакам его
  поручения. Первый: в каждую карту вкладки полагалось добавить раздел
  «Требования владельца к этой вкладке» — поиск этой строки по всем десяти файлам
  `docs/superpowers/audits/2026-08-20-cards-map/` даёт ноль совпадений во всех
  десяти. Второй: сводная карта `docs/superpowers/audits/2026-08-21-system-atlas.md`
  на диске отсутствует. Требование владельца 21.08 («вчерашние задачи и мой голос
  применить к тем картам… консолидировав и расширив») не закрыто.

### 5. «owner-voice-and-interactions» — 6 записей (244–249)

**Кто это был.** Волна голоса владельца: археолог транскриптов за 15–20 августа
(244), архитектор финального плана (245), три картографа взаимодействий —
действия и состояния (246), опасные сочетания фильтров (247), навигация (249) —
и сверщик, сводящий голос владельца с каноном в единый реестр требований (248).

**Что успел.** Ничего: у всех шестерых лимит сессии.

**Ценность — ПУСТО.**

**Судьба — СДЕЛАНА ПОЗЖЕ, все шесть.** Доказательства — файлы на диске:

| Запись | Что поручалось | Доказательство исполнения |
|---|---|---|
| 244 | реестр голоса владельца за 15–20.08 в файл `август-конец.md` | имя не совпало, но отрезок закрыт: `audits/2026-08-20-owner-voice/август-15-18.md` (63 848 Б) и `август-19-21.md` (44 331 Б) |
| 245 | `specs/2026-08-20-endgame-plan.md` | файл есть, 61 470 Б, «План финала AEMR: тринадцать волн до „продукт готов“» |
| 246 | `cards-map/interaction-действия-состояния.md` | файл есть, 72 536 Б |
| 247 | `cards-map/interaction-сочетания.md` | файл есть, 55 600 Б |
| 248 | `audits/2026-08-20-owner-requirements-register.md` | файл есть, 84 060 Б, 158 требований с вердиктами |
| 249 | `cards-map/interaction-навигация.md` | файл есть, 37 092 Б |

### 6. «course-distill» — 6 записей (218, 223–227)

**Кто это был.** Пятеро вычитывали курс «Клиентоцентричное мышление и ценности»
кусками (227 — страницы 1–90, 226 — 91–180, 223 — 181–270, 224 — 271–360,
225 — 361–436) и вынимали оттуда применимые к дэшу приёмы. Шестой (218) шёл
изнутри: инвентаризовал цели существующих элементов продукта — какое
управленческое решение меняет каждое число.

**Что успел.** Ничего, лимит модели.

**Ценность — ПУСТО.**

**Судьба — СДЕЛАНА ПОЗЖЕ.** Обе половины свелись в один документ:
`docs/superpowers/specs/2026-08-07-product-goals-map.md` (104 942 Б), чья шапка
прямо говорит, что он «собран 07.08.2026 из десяти дистиллятов двух учебных
курсов и инвентаризации целей существующих элементов Пульта и Отчёта».
Покрытие страниц проверено: в документе 46 ссылок вида «КЦ с. N», самая дальняя —
«КЦ с. 413», то есть отрезки погибших агентов (до 436-й страницы) закрыты
уцелевшими.

### 7. «bugfix-wave-a» — 3 записи (214, 219, 221)

**Кто это был.** Точечные починщики: каждому — один баг из охоты 08.08 с готовым
механизмом, адресом и требованием страж-теста.

**Что успел.** Ничего: у всех троих недельный лимит.

**Ценность — ПУСТО.**

**Судьба — РАЗДЕЛЬНАЯ, и здесь самая ценная часть урожая этой доли.**

- **219, баг №21** — таблица `changelog_entries` объявлена в схеме, но не
  создаётся при старте. **СДЕЛАНА ПОЗЖЕ.** Загрузочный DDL переехал из
  `db/index.ts` в отдельный `packages/server/src/db/ddl.ts`, где на строке 147
  стоит `CREATE TABLE IF NOT EXISTS changelog_entries`, на 185–186 — оба индекса
  (`idx_changelog_entries_at`, `idx_changelog_entries_dept`), а рядом лежит страж
  `db/ddl.test.ts`, в шапке которого названа та самая цена дефекта.
- **214, баг №24** — первопричины расхождения экономии считаются мерой «факт».
  **НУЖНА, дефект жив дословно.** Проверено 22.08:
  `packages/core/src/pipeline/recon-classify.ts:30` по-прежнему
  `export type ReconMeasure = 'planMoney' | 'planCount' | 'factMoney' | 'factCount'`
  — меры экономии тип не знает; `packages/server/src/routes/reconciliation.ts:131`
  по-прежнему `const measures: ReconMeasure[] = ['planMoney', 'factMoney', 'factMoney'];`
  — третий элемент дублирует второй.
- **221, баг №9** — оверрайды маппинга сохраняются, но не читаются пайплайном.
  **НУЖНА, дефект жив.** Проверено 22.08: запись в `mapping_overrides` есть
  (`routes/mapping.ts:110-112`), чтение — только там же на строке 31 для выдачи
  списка; сборка адресов снимка на `services/snapshot.ts:448` по-прежнему зовёт
  `getAllCellAddresses()` из статичной `REPORT_MAP` (`shared/report-map.ts:768`),
  функции `effectiveCell` в репозитории нет вовсе.

### 8. «monitoring-bi-and-triple-check» — 2 записи (215, 217)

**Кто это был.** Строитель аналитической витрины книги «Ежедневный мониторинг»
(215) и продуктовый инженер тройной сверки — движок против книги мониторинга
против рабочих листов ГРБС (217). Оба по прямым требованиям владельца от 21.08.

**Что успел.** Ничего, разрыв соединения.

**Ценность — ПУСТО.**

**Судьба — СДЕЛАНА ПОЗЖЕ, обе.**
`packages/web/src/components/monitoring/AnalyticsSection.tsx:194` — секция
«Аналитика мониторинга», в шапке файла названа «семь блоков»; при ней страж
`AnalyticsSection.test.tsx`. `packages/web/src/components/monitoring/TripleCheck.tsx`
— раздел «Сверка трёх источников», подключён в `pages/Monitoring.tsx:448`, при нём
`TripleCheck.test.tsx`. Обе честные пустоты, которых требовало поручение, на
месте: `TripleCheck.tsx:89` («сервером ещё не отдаётся») и `:100` («не ответила»)
— это разные сообщения. Коммит `7e2696d` в названии несёт «тройная сверка и BI
мониторинга».

### 9. «fresh-book-and-live-changes» — 2 записи (232, 236)

**Кто это был.** Инженер бесшовности (232): по уведомлению вебхука дочитывать
только изменившееся, а не всю книгу, ускорить загрузку с замером до и после и
обновлять экран без потери места. И строитель журнала изменений двух глубин
(236): кратко в узле провенанса, подробно — списком правок с адресами.

**Что успел.** Ничего, разрыв соединения.

**Ценность — ПУСТО.**

**Судьба — СДЕЛАНА ПОЗЖЕ (журнал) и ЧАСТИЧНО (бесшовность).** Журнал построен:
`packages/web/src/components/live/ChangeJournal.tsx` со стражем,
`change-story-text.ts` со стражем, `provenance-text.ts` со стражем; на сервере —
`routes/journal.ts`, `routes/changes.ts`, `journal-stats.test.ts`,
`changes-serialization.test.ts`. Коммит `7e2696d` несёт «живой дифф и журнал
изменений». По части «дифф вместо полной перечитки» и замерам скорости
доказательства в этой доле не проверялись — они относятся к чужой доле разбора.

### 10. «design-arsenal» — 3 записи (233, 234, 235)

**Кто это был.** Разведка дизайн-арсенала: сборщик итогового документа (233),
охотник за референсами тёмных аналитических дашбордов (234) и добытчик
документации через Context7 — темизация shadcn, стоит ли переезжать на Tailwind
v4, паттерны motion для числовых дашбордов (235).

**Что успел.** Ничего, лимит сессии до 14:00.

**Ценность — ПУСТО.**

**Судьба — СДЕЛАНА ПОЗЖЕ.** Целевой документ существует:
`docs/superpowers/specs/2026-08-07-design-arsenal.md`, 30 064 Б, записан
07.08 в 14:21 — то есть сразу после снятия лимита, уцелевшим составом. Вопрос
Tailwind v4 разобран отдельно: `specs/2026-08-18-tailwind-v4-migration.md`.

### 11. «enterprise-backend» — 2 записи (237, 243)

**Кто это был.** Инженер зоны «скорость веба» (237): разрезание бандла,
предзагрузка, кэш-политика, шрифты, замер до и после. И адверсарный критик волны
укрепления сервера (243): честные гейты, заявления о скорости без чисел, ломка
контрактов, выдуманные API, подогнанные тесты.

**Что успел.** Ничего, лимит сессии.

**Ценность — ПУСТО.**

**Судьба — ЧАСТИЧНО СДЕЛАНА ПОЗЖЕ.** Разрезание бандла сделано:
`packages/web/vite.config.ts:160` — `output: { manualChunks: chunkForModule }`,
и рядом на 158 строке комментарий про устаревание `manualChunks` в пользу
`output.advancedChunks`, то есть решение принято осознанно. А вот разрезание по
маршрутам — нет: единственный `lazy()` во всём вебе стоит на служебной странице
`packages/web/src/main.tsx:29` (`./pages/Kit`), маршруты продукта грузятся разом.
Пункт «Split the large web bundle with route-level dynamic imports» так и стоит в
`CLAUDE.md` в разделе Known Residual Work. Роль критика волны (243) не
переигрывалась — доказательств отдельного адверсарного разбора
enterprise-контура в `docs/superpowers/` нет.

### 12. «remelt-stage-a» — 2 записи (191, 216)

**Кто это был.** A5 — серверные маршруты `GET /api/competition` и
`GET /api/discipline/actions` (191). A6 — записи базы знаний для четырёх новых
метрик: премия за конкуренцию, цена отказа, кандидаты на централизацию ЕП,
эффект действия дисциплины (216).

**Что успел.** Ничего, недельный лимит до 13 августа.

**Ценность — ПУСТО.**

**Судьба — УСТАРЕЛА (191) и НУЖНА в изменённом виде (216).**

- 191: серверных роутов `competition.ts` и `discipline-actions.ts` в
  `packages/server/src/routes/` нет и они не понадобились — механика уехала на
  клиент. Вкладка построена: `packages/web/src/pages/Competition.tsx` собирает
  четыре блока (`CostOfRefusal`, `MergeCandidates`, `EpShare`, `EpJustification`),
  счёт ЕП/КП делается один раз через `sumEpKp` и раздаётся блокам, режим подведов
  подключён через `useOrgScope`. Функций `computeCompetitionRefusalCost` и
  `computeCompetitivePremiumByCategory`, на которые роут должен был опираться, в
  `packages/core/src` и `packages/server/src` нет вовсе — то есть проектное
  решение сменилось целиком, и серверный контур этой вкладки не строился.
- 216: четырёх ключей в реестре нет — поиск
  `competitive_premium|competition_refusal_cost|ep_centralization_candidates|discipline_action_effect`
  по `packages/core/src/metrics/registry.ts` даёт ноль. Паспорт под цену отказа
  завёлся в другом месте и под другим ключом:
  `packages/web/src/lib/kb/rows-door.ts:117` — `refusal_cost`. Требование канона
  п. 135 («у каждой метрики карточка базы знаний») по трём остальным метрикам не
  закрыто; в прежней формулировке — про `METRIC_KB` в ядре — задача устарела, по
  сути — жива.

### 13. «smart-stock-reports-coverage» — 1 запись (197)

**Кто это был.** Строитель карты покрытия: семь групп нашей аналитики против
штатной отчётности Бюджет-СМАРТ, с честным ответом по каждой строке, каких
данных в системе района сегодня нет.

**Что успел.** Ничего, обрыв соединения (ECONNRESET).

**Ценность — ПУСТО.**

**Судьба — СДЕЛАНА ПОЗЖЕ.** `docs/superpowers/specs/2026-08-06-smart-stock-reports.md`
(84 323 Б) содержит на строке 25 раздел «Часть 1. Карта покрытия» и на строке 27
заголовок «КАРТА ПОКРЫТИЯ: аналитика dash × штатная отчётность Бюджет-СМАРТ» —
ровно заказанный вид.

### 14. «borders-and-consolidation» — 1 запись (251)

**Кто это был.** Критик волны обводок: проверить гейты дословно, не сняты ли
обводки там, где они нужны, привести числа «было 274 в 62 файлах — стало
сколько», перемерить контраст в трёх местах, сверить правки с решениями владельца
по п. 137 и предложить проект по трём отложенным вопросам (законность ЕП, дома
вкладок-корзин, сорок восемь сирот).

**Что успел.** Ничего, разрыв соединения.

**Ценность — ПУСТО.**

**Судьба — ЧАСТИЧНО СДЕЛАНА ПОЗЖЕ, приёмка не проведена.** Сама волна обводок
доведена: коммит `7e2696d` («обводки сняты по всему вебу») и следом `dad69a8`
(«обводки поверхностей гаснут настройками темы, а не поштучно»). Числа, которых
требовал критик, сегодня такие: выгрузка волны
`scratchpad/bare-border.txt` (снята 22.08 в 04:21) содержит 162 адреса в 56
файлах при заявленной критику базе «274 в 62 файлах». Отдельного документа
приёмки волны в `docs/superpowers/audits/` нет; проект по трём отложенным
вопросам, который владелец разрешил решить самому, не сформулирован нигде —
это остаток задачи.

## Что уцелело в поручениях: 41 адресная находка по «Отчёту» и «оси недели»

Знание не принадлежит погибшим агентам — его добыла предыдущая волна и вложила
в поручение. Но проверка каждой находки была именно их работой и не состоялась,
поэтому она сделана здесь, 22.08.2026, по сегодняшнему коду. Отметки: **жива** —
дефект воспроизводится в коде дословно; **закрыта** — код изменён так, что
находка больше не применима; **не проверено** — почему.

### Тракт «Отчёт»

| № | Находка (сжато) | Состояние на 22.08 |
|---|---|---|
| 169 | «Интегральная сводка» не фильтруется по ГРБС, прочие оси фильтра игнорируются молча | **закрыта**: функции `integralKpiRow` в вебе больше нет; сводка пересобрана в «четыре яруса» (`pages/Report.tsx:1193`, `lib/report/mappers.ts:314`) |
| 171 | `localQuarter` залипает навсегда, вернуться к «авто» нельзя | **закрыта**: `pages/Report.tsx:1089` показывает «квартал из шапки» / «квартал выбран на странице», на `:1095` при `localQuarter !== null` рисуется возврат |
| 175 | «Что изменилось за неделю» говорит ярлыками ячеек СВОД | **не проверено по существу**: функция `weekMetricLabel` жива (`pages/Report.tsx:556`), но сверка её словаря с языком руководителя требует чтения `report-map.ts` целиком |
| 177 | Клиентский кламп `asOf` идёт по локальному календарю браузера, сервер — по календарю продукта | **жива**: `pages/Report.tsx:692` — `reportRequestParams(ctx, dayNumberOf(new Date())!, …)` |
| 178 | Плашка «Слепка нет» вместе с секцией «слепок X → Y» | **закрыта**: строки «Слепка на» в коде нет; роут теперь пишет «Отчёт построен из снимка …» (`routes/report.ts:201`) |
| 179 | ГРБС-фильтр секции недели сравнивает кириллицу с латиницей | **закрыта**: функции `grbsFilter` в вебе больше нет |
| 180 | «Копировать текстом» отдаёт другую правду, чем страница | **не проверено по существу**: `generateReportText` жив (`lib/report/text.ts`), совпадение выборки с `visibleBlocks` не сверялось |
| 181 | Неделя колеса и локальный квартал не сериализуются в ссылку | **закрыта решением**: `hooks/useUrlSync.ts:203` прямо оговаривает «week-режим — не фильтр», то есть неделя не сериализуется намеренно |
| 182 | Две hero-плитки с одинаковой подписью, 11 равновесных compact-плиток | **закрыта**: `scopeTiles` больше нет; `lib/report/mappers.ts:314` — «четыре яруса вместо ряда равных плиток» |
| 185 | Каждый клик по кварталу — полный рефетч без `AbortController`, stale не удерживается | **жива**: `AbortController` в `pages/Report.tsx` не встречается ни разу |
| 186 | Страница без единой фразы-вывода: числа есть, нарратива нет | **не проверено**: наличие вердикт-строки на 90 КБ страницы требует чтения разметки целиком, а не поиска по образцу |
| 189 | Методология печатает читателю внутренние ключи `origin=calc` | **жива**: `routes/report.ts:78` — «Числа origin=calc пересчитаны из строк-атомов книг ГРБС…» |
| 190 | Логика страницы вне досягаемости тестов: рендер-тестов страниц нет | **закрыта частично**: тесты страниц появились (`DataBrowser.subs`, `Discipline.subs`, `Monitoring`, `Settings.logic`), но `Report.tsx` своего теста по-прежнему не имеет |
| 192 | `ReportResponse` объявлен руками дважды и расходится по `methodology` | **жива**: `web/src/api.ts:52` — `methodology?: string`, `routes/report.ts:298` — отдаёт всегда |
| 193 | `svodQuarter` доезжает до клиента, но не рендерится | **закрыта**: `pages/Report.tsx:1201` — `source={report.integralSummary.svodQuarter ? 'mixed' : 'calc'}` |
| 194 | Сверка со СВОД: «=» не расшифрован, первая строка пары не подписана | **не проверено**: требует разбора разметки `DiffText` в контексте |
| 195 | Четыре канона форматирования процента: запятая против точки | **жива**: три разных `fmtPct` — `lib/report/mappers.ts:31` даёт «40,0%» (запятая), `lib/recon/format.ts:18` даёт «40.0%» (точка), `lib/monitoring/format.ts:26` — третий |
| 198 | `fmtPct` занижает точные половинки на 0,1 п.п. двойным округлением | **жива**: `lib/report/mappers.ts:31` — `(Math.round(pct * 10) / 10).toFixed(1)` |
| 199 | `year='all'` молча превращается в запрос будущего пустого года | **не проверено**: `AVAILABLE_YEARS` жив (`components/Header.tsx:328-330`), но ветка `'all'` в `lib/report/request.ts` не сверялась |
| 200 | `localQuarter` — защёлка без сброса | **закрыта** тем же изменением, что и 171 |
| 201 | Отчёт одноквартальный, мультиквартального вида нет | **не проверено**: наличие мультиквартального вида требует чтения разметки страницы |
| 202 | ГРБС не ранжируются от худшего — главный жанровый ход бумаги не сделан | **жива**: `core/report/build-report.ts:543` — `deptOrder`, на `:689` комментарий «Порядок — канонический `deptOrder`», то есть порядок остался реестровым |
| 203 | «Не заключено: 9» — счётчик без перечня и без ссылки | **жива по счётчику**: `lib/report/mappers.ts:557` и страж `mappers.test.ts:167` («Не заключено: 9»); наличие двери к строкам не сверялось |
| 204 | «нет плана» и дельта-бейдж «▲ п.п.» могут стоять на одной плитке | **не проверено**: сценарий требует прогона, а не поиска по образцу |
| 205 | «Слепок» — внутренний термин в пользовательских текстах | **закрыта**: в `pages/Report.tsx` слов «слепок»/«Слепок» ноль |
| 206 | Сдвиг от нуля получает ранг 0 и вылетает из топа | **не проверено до конца**: `weekDeltaRank` жив (`pages/Report.tsx:566`), ветка `deltaPct === null` не читалась |
| 207 | `appeared`/`disappeared` с рангом `Infinity` вытесняют реальные изменения | **жива**: `pages/Report.tsx:567` — `if (d.direction === 'appeared' \|\| d.direction === 'disappeared') return Infinity` |
| 208 | Каноническая оговорка «СВОД онлайн живёт» доступна только по наведению | **не проверено**: требует разбора атрибутов ссылки в разметке |
| 209 | Один блок под двумя именами: «Интегральная сводка» на странице, «ИТОГО ПО УПРАВЛЕНИЯМ» в письме | **жива**: `pages/Report.tsx:1193` против `lib/report/text.ts:34` |
| 210 | Критичность сигнала — только 6-пиксельной точкой цвета, без слова | **жива по механизму**: `pages/Report.tsx:197` — `style={{ backgroundColor: SEVERITY_COLORS[s.severity].text }}`; сопровождающее слово в разметке не искалось |
| 213 | `periodBadge` «2026 · год · 305 из 477» — числа без глагола | **не проверено**: поле контракта обязательно (`components/contract/contract.test.ts:77`), содержание строки не сверялось |

### Конструкция «одна ось недели»

| № | Находка (сжато) | Состояние на 22.08 |
|---|---|---|
| 170 | Полнотабличные `instr`-сканы блобов, индексов на `snapshots` нет вовсе | **закрыта наполовину**: индекс появился — `db/ddl.ts:175`, `CREATE INDEX IF NOT EXISTS idx_snapshots_created ON snapshots(created_at)`; колонок `has_rows`/`product_day` и кэша разобранных слепков по-прежнему нет |
| 172 | Первый тест cron-файла не укладывается в таймаут 20 с | **не проверено**: таймаут остался (`weekly-snapshot-cron.test.ts:53` и `:112` — `}, 20000)`), но подтверждение требует прогона тестов, а он в этой доле не запускался |
| 173 | Пропущенный четверг не навёрстывается | **жива**: `services/weekly-snapshot-cron.ts:193` — `return nowDay % 7 === 0 && !snapshotDays.includes(nowDay);` |
| 174 | Будущий `asOf`: живые данные под будущей датой без плашки и без отказа | **не проверено**: ветвление роута переписано, сверка требует чтения `routes/report.ts` целиком |
| 176 | Дыры тестового покрытия по краям новой механики | **не проверено**: требует прогона покрытия |
| 183 | «Лист СВОД не передан» — формулировка живого мира в слепочном отчёте | **жива дословно**: `core/report/build-report.ts:669` |
| 184 | Прод-сборка Docker зависит от доступности `cdn.sheetjs.com` | **жива**: `packages/server/package.json:38` — `"xlsx": "https://cdn.sheetjs.com/xlsx-0.20.2/xlsx-0.20.2.tgz"` |
| 187 | Разбор листов бэкфилла не защищён: один плохой лист роняет весь прогон | **не проверено**: скрипт на месте (`packages/server/scripts/backfill-weekly-snapshots.ts`), границы `try` не сверялись |
| 188 | Тик cron объявляет «слепок снят», не проверяя факт записи в БД | **не проверено**: файл переработан, в шапке на строке 6 прямо оговорено «полагаться на то, что…»; сверка требует чтения тела `tick` |
| 196 | Фолбэк даты слепка — сырой ISO в плашке и в письме | **жива**: `routes/report.ts:201` — `${snapshotDay !== null ? ruDateOfDay(snapshotDay) : snapshot.createdAt}` |

## Поимённая таблица 83 записей

Графы «что успел» и «ценность» одинаковы у всех и вынесены в шапку документа:
ноль вызовов инструментов, ноль записей на диск, ноль содержательного текста —
**ПУСТО** у всех 83. Ниже — кто это был и судьба задачи.

| № | Волна | Кто это был | Судьба задачи |
|---|---|---|---|
| 169 | report-full-eval | скептик находки: сводка «Отчёта» не слушается фильтра ГРБС | СДЕЛАНА ПОЗЖЕ — сводка пересобрана в четыре яруса, находка неприменима |
| 170 | week-axis-deep-eval | скептик находки: сканы блобов без индексов на каждом запросе | НУЖНА наполовину — индекс появился, кэша и колонок нет |
| 171 | report-full-eval | скептик находки: залипший локальный квартал | СДЕЛАНА ПОЗЖЕ — возврат к «кварталу из шапки» появился |
| 172 | week-axis-deep-eval | скептик находки: тест недельного cron не укладывается в таймаут | НУЖНА — не проверено прогоном |
| 173 | week-axis-deep-eval | скептик находки: пропущенный четверг не навёрстывается | НУЖНА — дефект жив |
| 174 | week-axis-deep-eval | скептик находки: будущая дата среза принимается молча | НУЖНА — не проверено |
| 175 | report-full-eval | скептик находки: недельная секция говорит ярлыками ячеек СВОД | НУЖНА — проверка не состоялась, по существу не сверено |
| 176 | week-axis-deep-eval | скептик находки: дыры покрытия по краям новой механики | НУЖНА — не проверено |
| 177 | report-full-eval | скептик находки: клиент и сервер клампят дату по разным календарям | НУЖНА — дефект жив |
| 178 | report-full-eval | скептик находки: плашка «слепка нет» рядом с секцией слепков | УСТАРЕЛА — тексты роута переписаны |
| 179 | report-full-eval | скептик находки: фильтр ГРБС сверяет кириллицу с латиницей | УСТАРЕЛА — функция удалена при переплавке |
| 180 | report-full-eval | скептик находки: копия письмом расходится со страницей | НУЖНА — по существу не сверено |
| 181 | report-full-eval | скептик находки: ссылка не восстанавливает неделю и квартал | УСТАРЕЛА — неделя не сериализуется намеренно |
| 182 | report-full-eval | скептик находки: вердикт-числа тонут среди равных плиток | СДЕЛАНА ПОЗЖЕ — ярусная сводка |
| 183 | week-axis-deep-eval | скептик находки: формулировка живого мира в слепочном отчёте | НУЖНА — формулировка жива дословно |
| 184 | week-axis-deep-eval | скептик находки: прод-сборка зависит от внешнего CDN | НУЖНА — зависимость жива |
| 185 | report-full-eval | скептик находки: рефетч без отмены и без удержания старых данных | НУЖНА — дефект жив |
| 186 | report-full-eval | скептик находки: у страницы нет фразы-вывода | НУЖНА — не проверено |
| 187 | week-axis-deep-eval | скептик находки: один битый лист роняет весь добор истории | НУЖНА — не проверено |
| 188 | week-axis-deep-eval | скептик находки: cron рапортует о слепке, не проверив запись | НУЖНА — не проверено |
| 189 | report-full-eval | скептик находки: методология печатает внутренние ключи | НУЖНА — дефект жив |
| 190 | report-full-eval | скептик находки: логика страницы вне досягаемости тестов | НУЖНА частично — тесты страниц появились, у «Отчёта» нет |
| 191 | remelt-stage-a | строитель серверных маршрутов конкуренции и дисциплины | УСТАРЕЛА — механика уехала на клиент, роутов не строили |
| 192 | report-full-eval | скептик находки: тип ответа отчёта объявлен дважды и разошёлся | НУЖНА — расхождение живо |
| 193 | report-full-eval | скептик находки: официальный квартал доезжает, но не рисуется | СДЕЛАНА ПОЗЖЕ — рисуется |
| 194 | report-full-eval | скептик находки: знак равенства в сверке не расшифрован | НУЖНА — не проверено |
| 195 | report-full-eval | скептик находки: четыре канона записи процента | НУЖНА — три разных форматирователя живы |
| 196 | week-axis-deep-eval | скептик находки: сырая дата в плашке при сбое разбора | НУЖНА — дефект жив |
| 197 | smart-stock-reports-coverage | строитель карты покрытия нашей аналитики штатной отчётностью Бюджет-СМАРТ | СДЕЛАНА ПОЗЖЕ — specs/2026-08-06-smart-stock-reports.md, «Часть 1. Карта покрытия» |
| 198 | report-full-eval | скептик находки: округление процента занижает половинки | НУЖНА — дефект жив |
| 199 | report-full-eval | скептик находки: «все годы» превращаются в пустой будущий год | НУЖНА — не проверено |
| 200 | report-full-eval | скептик находки: локальный квартал перекрывает шапку навсегда | СДЕЛАНА ПОЗЖЕ — как и 171 |
| 201 | report-full-eval | скептик находки: отчёт одноквартальный | НУЖНА — не проверено |
| 202 | report-full-eval | скептик находки: управления не ранжируются от худшего | НУЖНА — порядок остался реестровым |
| 203 | report-full-eval | скептик находки: счётчик незаключённого без перечня | НУЖНА — счётчик жив, дверь не сверена |
| 204 | report-full-eval | скептик находки: «нет плана» и дельта на одной плитке | НУЖНА — не проверено |
| 205 | report-full-eval | скептик находки: «слепок» — внутреннее слово в текстах для людей | СДЕЛАНА ПОЗЖЕ — слово вычищено |
| 206 | report-full-eval | скептик находки: сдвиг от нуля вылетает из топа недели | НУЖНА — не проверено до конца |
| 207 | report-full-eval | скептик находки: «появилось/убыло» вытесняет реальные сдвиги | НУЖНА — дефект жив |
| 208 | report-full-eval | скептик находки: главная оговорка спрятана в подсказку наведения | НУЖНА — не проверено |
| 209 | report-full-eval | скептик находки: один блок под двумя именами | НУЖНА — расхождение живо |
| 210 | report-full-eval | скептик находки: критичность сигнала только цветной точкой | НУЖНА — механизм жив |
| 211 | grand-remelt | консолидатор сигналов: один флаг экономии, этапность в информационные, дубли имён | СДЕЛАНА ПОЗЖЕ частично — этапность стала справкой; пресет-срез Реестра не найден |
| 212 | grand-remelt | то же поручение по консолидации сигналов, второй заход | СДЕЛАНА ПОЗЖЕ частично — как 211 |
| 213 | report-full-eval | скептик находки: подпись плитки — числа без глагола | НУЖНА — не проверено |
| 214 | bugfix-wave-a | починщик бага №24: первопричины экономии считаются мерой «факт» | НУЖНА — дефект жив дословно |
| 215 | monitoring-bi-and-triple-check | строитель аналитической витрины книги мониторинга | СДЕЛАНА ПОЗЖЕ — components/monitoring/AnalyticsSection.tsx |
| 216 | remelt-stage-a | автор паспортов базы знаний для четырёх метрик конкуренции и дисциплины | НУЖНА в изменённом виде — ключей нет, цена отказа завелась под другим именем |
| 217 | monitoring-bi-and-triple-check | инженер сверки трёх источников: движок, мониторинг, листы ГРБС | СДЕЛАНА ПОЗЖЕ — components/monitoring/TripleCheck.tsx |
| 218 | course-distill | инвентаризатор целей: какое решение меняет каждое число продукта | СДЕЛАНА ПОЗЖЕ — specs/2026-08-07-product-goals-map.md |
| 219 | bugfix-wave-a | починщик бага №21: таблица журнала правок не создаётся при старте | СДЕЛАНА ПОЗЖЕ — db/ddl.ts:147 плюс страж db/ddl.test.ts |
| 220 | grand-remelt | выводчик сирот ядра на экран: аномалии, пропавшие строки, целостность | СДЕЛАНА ПОЗЖЕ — components/analytics-extra/ |
| 221 | bugfix-wave-a | починщик бага №9: оверрайды маппинга пишутся, но не читаются | НУЖНА — дефект жив |
| 222 | grand-remelt | то же поручение по сиротам ядра, второй заход | СДЕЛАНА ПОЗЖЕ — как 220 |
| 223 | course-distill | чтец курса клиентоцентричности, страницы 181–270 | СДЕЛАНА ПОЗЖЕ — вошло в карту целей продукта |
| 224 | course-distill | чтец курса клиентоцентричности, страницы 271–360 | СДЕЛАНА ПОЗЖЕ — то же |
| 225 | course-distill | чтец курса клиентоцентричности, страницы 361–436 | СДЕЛАНА ПОЗЖЕ — то же |
| 226 | course-distill | чтец курса клиентоцентричности, страницы 91–180 | СДЕЛАНА ПОЗЖЕ — то же |
| 227 | course-distill | чтец курса клиентоцентричности, страницы 1–90 | СДЕЛАНА ПОЗЖЕ — то же |
| 228 | grand-remelt | критик-приёмщик полноты волны переплавки | НУЖНА — документа приёмки волны нет |
| 229 | grand-remelt | инженер страниц свода и реестра до 100/100 | СДЕЛАНА ПОЗЖЕ — перечень карточек доводили сменившие составы |
| 230 | grand-remelt | критик-приёмщик полноты волны, второй заход | НУЖНА — как 228 |
| 231 | grand-remelt | инженер страниц аналитики и таймлайна до 100/100 | СДЕЛАНА ПОЗЖЕ — perimeter и drill подключены в Analytics.tsx |
| 232 | fresh-book-and-live-changes | инженер бесшовности: дифф вместо полной перечитки и замер скорости | ЧАСТИЧНО — журнал есть, дифф и замеры в этой доле не проверялись |
| 233 | design-arsenal | сборщик документа «Дизайн-арсенал дэша» | СДЕЛАНА ПОЗЖЕ — specs/2026-08-07-design-arsenal.md |
| 234 | design-arsenal | охотник за референсами тёмных аналитических дашбордов | СДЕЛАНА ПОЗЖЕ — вошло в тот же документ |
| 235 | design-arsenal | добытчик документации: темизация shadcn, Tailwind v4, motion | СДЕЛАНА ПОЗЖЕ — плюс specs/2026-08-18-tailwind-v4-migration.md |
| 236 | fresh-book-and-live-changes | строитель журнала изменений двух глубин | СДЕЛАНА ПОЗЖЕ — components/live/ChangeJournal.tsx, routes/journal.ts |
| 237 | enterprise-backend | инженер скорости веба: разрезание бандла, предзагрузка, замеры | ЧАСТИЧНО — бандл режется, маршруты не ленивые |
| 238 | wf_9cc94119-eca | инженер вкладок контроля и дисциплины | СДЕЛАНА ПОЗЖЕ — сменившими составами |
| 239 | wf_9cc94119-eca | инженер вкладок экономии и конкуренции | СДЕЛАНА ПОЗЖЕ — сменившими составами |
| 240 | wf_9cc94119-eca | инженер вкладок аналитики и таймлайна | СДЕЛАНА ПОЗЖЕ — сменившими составами |
| 241 | wf_9cc94119-eca | инженер вкладок отчёта и свода | СДЕЛАНА ПОЗЖЕ — сменившими составами |
| 242 | wf_9cc94119-eca | инженер вкладки реестра и корзин | СДЕЛАНА ПОЗЖЕ — уцелевший состав той же волны записал девять файлов |
| 243 | enterprise-backend | адверсарный критик волны укрепления сервера | НУЖНА — отдельного разбора enterprise-контура нет |
| 244 | owner-voice-and-interactions | археолог голоса владельца за 15–20 августа | СДЕЛАНА ПОЗЖЕ — owner-voice/август-15-18.md и август-19-21.md |
| 245 | owner-voice-and-interactions | архитектор единого плана доведения продукта до готового | СДЕЛАНА ПОЗЖЕ — specs/2026-08-20-endgame-plan.md |
| 246 | owner-voice-and-interactions | картограф интерактивных действий и состояний экранов | СДЕЛАНА ПОЗЖЕ — cards-map/interaction-действия-состояния.md |
| 247 | owner-voice-and-interactions | картограф опасных сочетаний фильтров и переходов | СДЕЛАНА ПОЗЖЕ — cards-map/interaction-сочетания.md |
| 248 | owner-voice-and-interactions | сверщик: единый реестр требований владельца против кода | СДЕЛАНА ПОЗЖЕ — audits/2026-08-20-owner-requirements-register.md |
| 249 | owner-voice-and-interactions | картограф навигации: граф экранов, тупики, потеря контекста | СДЕЛАНА ПОЗЖЕ — cards-map/interaction-навигация.md |
| 250 | wf_9cc94119-eca | хранитель атласа: разложить голос владельца по картам и свести систему | НУЖНА — раздела требований в картах нет, сводной карты нет |
| 251 | borders-and-consolidation | критик волны обводок и автор проекта по трём отложенным вопросам | ЧАСТИЧНО — волна доведена, приёмки и проекта нет |

## Итог доли

**Разобрано записей:** 83 из 83 (169–251). Журнал нашёлся у всех, потерянных нет.

**Ценность находок:** НОВОЕ — 0 · ДУБЛЬ — 0 · **ПУСТО — 83**. Это не оценка
качества исполнителей: ни один из них не получил ни одного шанса на действие.
Семьдесят один погиб на лимите (модели, сессии или недели), одиннадцать —
на разрыве соединения, один не оставил даже строки ошибки.

**Судьба задач:** НУЖНА — 39 · СДЕЛАНА ПОЗЖЕ — 37 · УСТАРЕЛА — 4 ·
ЧАСТИЧНО (сделано наполовину, остаток жив) — 3.

> Сверка 22.08. Сводка урожая (`сводка.md`, раздел 1) объявила этот счёт
> ошибочным и назвала другой — 37 · 35 · 4 · 7. Пересчёт поимённой таблицы
> строк 452–535 машиной, берущей последнюю графу каждой строки, подтвердил
> счёт этого абзаца: 83 строки, из них НУЖНА 39, СДЕЛАНА 37, УСТАРЕЛА 4,
> ЧАСТИЧНО 3 (записи 232, 237, 251 — единственные три «частично» во всём
> файле, проверено сплошным поиском слова). Ошибся разбор сводки, а не эта
> доля; сводка исправлена.

Из тридцати девяти живых задач тридцать две — непроверенные находки по «Отчёту» и
«оси недели»; по четырнадцати из них дефект подтверждён сегодня в коде дословно
(записи 173, 177, 183, 184, 185, 189, 192, 195, 196, 198, 203, 207, 209, 210).
Остальные семь — настоящие продуктовые долги: два бага из охоты 08.08 (№24 —
запись 214 и №9 — запись 221), паспорта базы знаний по метрикам конкуренции и
дисциплины (216), приёмка волны переплавки (228 и 230), адверсарный разбор
enterprise-контура (243) и сводная карта системы с разделом требований владельца
в картах вкладок (250). К ним примыкают три частично закрытые задачи: дифф вместо
полной перечитки (232), ленивая загрузка маршрутов веба (237) и проект по трём
отложенным вопросам владельца (251).

**Три самые ценные находки доли.**

1. **Баг №24 жив дословно, спустя две недели после того, как был описан с точным
   исправлением.** `packages/core/src/pipeline/recon-classify.ts:30` — тип
   `ReconMeasure` до сих пор не знает меры экономии, а
   `packages/server/src/routes/reconciliation.ts:131` до сих пор содержит
   `['planMoney', 'factMoney', 'factMoney']`. Следствие видит начальница
   управления: карточка первопричины расхождения экономии перечисляет строки с
   адресами колонки Y и суммами факта, тогда как утверждённая экономия живёт в
   колонках Z–AC под гейтом AD='да'. Это прямое нарушение канона п. 119 («по
   каждому сигналу виден ответ: какая строка, что в ней, почему») — ответ
   показывается, но неверный. Исправление известно до строки и не требует
   исследования.

2. **Баг №9 жив: настройка, которую продукт разрешает делать, ни на что не
   влияет.** `PUT /api/mapping/:metricId` пишет оверрайд ячейки в
   `mapping_overrides` (`routes/mapping.ts:110-112`) и отвечает успехом, но сборка
   снимка на `services/snapshot.ts:448` берёт адреса исключительно из статичной
   `REPORT_MAP` через `getAllCellAddresses()` (`shared/report-map.ts:768`).
   Функции `effectiveCell` в репозитории нет. Человек переносит метрику на другую
   ячейку, получает «изменено» — и ничего не меняется. Это худший род дефекта:
   молчаливое враньё интерфейса, которое невозможно заметить изнутри продукта.

3. **Прод-сборка сервера зависит от внешнего CDN.**
   `packages/server/package.json:38` — `"xlsx": "https://cdn.sheetjs.com/xlsx-0.20.2/xlsx-0.20.2.tgz"`.
   Сборка образа ставит зависимости вместе с dev-набором, значит недоступность
   `cdn.sheetjs.com` в момент сборки останавливает выкладку целиком. Для продукта,
   который «скоро запуск», это единственная точка отказа, не принадлежащая ни
   нам, ни нашему облаку.

**Урок доли, который стоит записать отдельно.** Эти 83 записи — не история
неудачных работ, а история неудачного планирования нагрузки: в одной волне
запускали по тридцать одному проверяющему разом, и лимит модели съедал их
пачками до первого действия. Дешевле было бы запустить троих подряд с записью
результата на диск после каждой находки. Сама конструкция «одна находка — один
агент-скептик» здравая: она и дала те 41 адресную находку, которые до сих пор
работают как готовый список дел. Погубил её масштаб одновременного запуска.
