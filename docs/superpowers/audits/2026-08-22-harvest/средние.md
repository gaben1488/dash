# Разбор оборванных агентов: доля «средние» (записи 85–168)

Дата разбора: 22.08.2026. Источник — реестр оборванных агентов
`scratchpad/dead-agents.txt` (251 запись, отсортирована по размеру транскрипта
по убыванию). Моя доля — записи с 85-й по 168-ю включительно: 84 агента,
транскрипты от 431 КБ до 107 КБ.

Метод: каждый транскрипт читался потоково скриптом
(`scratchpad/mid-digest.py`), в контекст целиком не грузился; извлекались
речь и раздумья агента, вызовы инструментов, адреса записей. Каждый файл,
в который агент писал, проверялся на диске: есть ли он, какого размера,
дописан ли до конца.

Обозначения ценности находок:

- **НОВОЕ** — знание, которого нет в готовых документах проекта.
- **ДУБЛЬ** — то же самое уже записано, адрес указан.
- **ПУСТО** — содержательного не успел ничего.

Обозначения судьбы задачи: **НУЖНА** (владелец ставил, не выполнена) ·
**СДЕЛАНА ПОЗЖЕ** (с доказательством) · **УСТАРЕЛА** (с причиной).

---

## Сводка

Разобрано 84 записи (85–168). По ценности находок:

| Степень | Сколько | Кто |
|---|---|---|
| **НОВОЕ** | 14 | 86, 91, 94, 95, 105, 106, 115, 117, 122, 124, 127, 129, 131, 136 |
| **ДУБЛЬ** | 19 | 85, 89, 90, 96, 97, 99, 100, 101, 102, 103, 109, 113, 116, 118, 120, 123, 130, 142, 145 |
| **ПУСТО** | 51 | остальные |

Задача остаётся **НУЖНОЙ** у 20 записей: 86, 94, 95, 103, 104, 105, 109,
115, 116, 120, 121, 124, 127, 131, 132, 133, 136, 139, 147, 153.

Что видно по доле в целом. Половина смертей (51 из 84) — не обрыв связи, а
упор в потолок расхода: агент получал поручение и не делал ни одного шага.
Семнадцать таких смертей подряд в хвосте (152–168) — один веер, запущенный
шире остатка лимита. Это потеря не работы, а возможности: поручения
сохранились, работа не начиналась.

Вторая половина умерла на обрыве связи, и почти все — после того, как
довели разбор до вывода, но до того, как его записали. Из четырнадцати
находок НОВОЕ ни одна не была записана автором: все они восстановлены из
раздумий перед обрывом. Прямое подтверждение правила «пиши на диск после
каждого шага».

Пять дефектов, найденных этими агентами, **живы на 22.08.2026** и проверены
сегодня по коду: задвоение push-каналов Drive (#86), сигналы, читающие
столбец программы вместо предмета (#109), 16 предупреждений безопасности
(#95), жёстко зашитая пара лет в разборе листа СВОД с фитилём на 2027-й
(#94), две календарные оси на одной странице отчёта (#131).


---

## Таблица разбора

### Записи 85–92

| № | Волна | Кто это был | Что успел | Ценность | Судьба задачи |
|---|---|---|---|---|---|
| 85 | cards-atlas | картограф вкладки «Реестр» и корзин «без финансирования» / «переходящие» | установил, что обе корзины — не отдельные страницы, а режимы `DataBrowser`; нашёл несогласие: счётчики корзин в шапке считают по всем управлениям за весь год независимо от выбранных фильтров, а страница показывает отфильтрованное — числа на одном экране спорят; отметил отсутствие чередования строк в таблице | ДУБЛЬ | СДЕЛАНА ПОЗЖЕ — `audits/2026-08-20-cards-map/reestr.md` |
| 86 | enterprise-backend | инженер push-каналов Google Drive | **нашёл четыре дефекта в живом регистраторе каналов и написал ему замену** — `webhook-channel.ts` (41 427 б на диске): учёт настоящего срока от Drive, гашение прежнего канала, отступ при неудаче, состояние наружу | **НОВОЕ, дефект жив** (см. заметку О) | **НУЖНА**: замена написана и подключена только со стороны приёма уведомлений (`routes/webhook.ts:41`); регистрацию по-прежнему ведёт старый `drive-watch.ts` (`app.ts:40,372`) |
| 87 | signal-case-by-case | адверсарный проверяющий сигнала «факт без квартала» | **перепроверил все восемь книг независимым извлечением**: шесть книг сошлись с отчётом сразу, две крупные (УО — 2 ГБ, УКСиМП) домерил байтовой нарезкой листа «ВСЕ», чтобы уложиться в предел фонового задания; подтвердил адрес подсказки базы знаний в `check-registry.ts` | ПУСТО (вывод не записан) | СДЕЛАНА ПОЗЖЕ — `audits/2026-08-20-signal-inventory.md`, `specs/2026-08-21-signal-consolidation.md`; приём «байтовая нарезка листа перед разбором» сохранён в `adv_slice_uo.py` (3 295 б) |
| 88 | harvest-and-finish | сборщик урожая по четырём каталогам волн | написал `digest.py` (6 341 б) и `journals.py` (1 337 б), оба целы | ПУСТО | ДУБЛЬ задачи #141 |
| 89 | milestone-everything-on-screen | строитель провенанса и подсказок базы знаний на экране | **работа дошла до диска**: пять файлов написаны и живут — `MetricProvenance.tsx`, `metric-provenance-view.ts`, `DivergenceMark.tsx`, `KbHover.tsx`, `kb-tooltip.tsx`; каждый на диске крупнее записанного, то есть был подхвачен и дописан | ДУБЛЬ | СДЕЛАНА — коммит `90d47a3` «провенанс в шапке» |
| 90 | dash-target-vision | автор документа целевого видения | записал 54 526 б в `specs/2026-07-25-dash-target-vision.md` одним заходом, ни слова в переписке | ДУБЛЬ | СДЕЛАНА ПОЗЖЕ — файл на диске 89 613 б |
| 91 | aemr-report-final | ревизор потока регресса «расчёт против ручного отчёта» | **проверил честность допусков вместо того, чтобы поверить**: нашёл, что упавший тест устарел, а не код — по живым книгам B = «Наименование управления», U = «Причина отклонения»; по 64 книгам за 8 недель AE = «Комментарий ГРБСа» везде; замерил наибольшее расхождение денег — 0,005 при допуске 0,05, то есть ровно предел округления ручного отчёта | **НОВОЕ** (см. заметку П) | СДЕЛАНА — фикстуры недель в `packages/core/src/report/__fixtures__/`; его пробники (`hdr.mjs`, `tails.mjs`, `__audit-tmp.test.ts`) убраны с диска, как и положено |
| 92 | svod-divergence-proof | первый скептик сверки со СВОДом | написал семь разборщиков (`extract_svod.py`, `replicate.py`, `gaps.py` и другие — все целы на диске), ни слова в переписке | ПУСТО | СДЕЛАНА ПОЗЖЕ — `audits/2026-08-21-svod-divergence-verdict.md` |

### Записи 93–100

| № | Волна | Кто это был | Что успел | Ценность | Судьба задачи |
|---|---|---|---|---|---|
| 93 | remelt-stage-a | строитель механики «цена отказа от конкуренции», поток A3 | целиком продумал формулу премии за конкуренцию, порог значимости и честный отказ считать при нехватке данных; уперся в то, что поток A1 (`computeCompetitivePremiumByCategory`) ещё не написан, и разобрал, как жить без него, не выдумывая чисел | ПУСТО (ни строки кода) | СДЕЛАНА ПОЗЖЕ — `packages/web/src/components/competition/CostOfRefusal.tsx` |
| 94 | concept-maturity-audit | оценщик зрелости шести понятий пакета `shared` | **самый длинный разбор доли**: раздвоение подписей сигналов, зеркало подписей метрик без потребителей, круговой сторожевой тест, утечка внутренних ключей на экран «Доверие», жёстко зашитый год в разборе листа СВОД, расхождение знака отклонения | ДУБЛЬ по трём + **НОВОЕ** по году (см. заметку М) | ЧАСТЬЮ СДЕЛАНА: подписи сигналов сведены в один дом (`dataset-signals.ts:494`, `rows-dto.ts`), знак отклонения выровнен (`calc-engine.ts:452,460`); **год НУЖНА** — `svod-grid.ts:317,393` жив |
| 95 | tech-debt-full-sweep | сметчик долга, зона безопасности и швов | **разобрал предупреждения безопасности по существу**, отделив опасные от транзитивных, и починил четыре вещи: явный потолок тела запроса с проверкой на 413, защита `.env` от подстановки, сравнение ключа без утечки длины, сторож на `window.open` без `noopener` | ДУБЛЬ по правкам + **НОВОЕ** по разбору (см. заметку Н) | ЧАСТЬЮ СДЕЛАНА: все четыре файла на диске и выросли; **НУЖНА** — `pnpm audit --audit-level moderate` 22.08 по-прежнему даёт 16 предупреждений (1 низкое, 2 средних, 13 высоких) |
| 96 | aemr-data-truth-wave | строитель сторожей периметра и канонических домов | написал два сторожа и проверил, что они кусаются: `packages/server/src/perimeter-guard.test.ts` (7 284 б) и `packages/shared/src/canon-homes.test.ts` (10 411 б); замерил цену чтения всех вкладок вместо «ВСЕ» — 2 729 строк против 6 106 | ДУБЛЬ | СДЕЛАНА — оба сторожа в дереве, реестр долга ведёт списком известных нарушений |
| 97 | aemr-data-truth-wave | строитель «остатка к заключению» в отчёте | довёл до зелёных тестов: тип `PendingRemainder`, поля `pending` и `pendingByMethod` у квартальных срезов, выбор «Расч. экономии» из районного периметра `ВСЕ`; прогон 788 тестов в 24 файлах — зелено, ошибки только при завершении рабочих потоков на Windows | ДУБЛЬ | СДЕЛАНА — `packages/core/src/report/types.ts` и `build-report.ts` в дереве |
| 98 | fresh-book-and-live-changes | строитель журнала изменений | обнаружил, что предыдущий состав той же волны уже построил `ProvenanceHub.tsx`, `ChangeJournal.tsx` и маршрут истории правок; сверял, чего не хватает | ПУСТО | СДЕЛАНА ПОЗЖЕ — коммит `7e2696d` «живой дифф и журнал изменений» |
| 99 | owner-voice-and-interactions | археолог голоса владельца, отрезок 15–20 августа | нашёл 70 сообщений владельца и провал: 15 и 16 августа сообщений нет вовсе, работа возобновляется 18-го; успел разложить длинные сообщения на подпункты | ДУБЛЬ | СДЕЛАНА ПОЗЖЕ — `audits/2026-08-20-owner-voice/август-15-18.md` (63 848 б); там же исправлена его временная гипотеза: сдвиг UTC+12, а не +3 |
| 100 | wf_9cc94119-eca | инженер мониторинга, задача «стопроцентная обработка» | **разобрал книгу мониторинга по столбцам потоково** и нашёл, что столбец A листа «25-26» подписан «№ п/п», а держит родословную процедуры: новая закупка, доработка, повторный аукцион, отмена с номером замены, жалоба в ФАС, отклонение с участниками | ДУБЛЬ | СДЕЛАНА ПОЗЖЕ и точнее — `audits/2026-08-21-monitoring-attribute-types.md:245`: «ни одного номера: 44 текста, 11 чисел 2026 (разделители года), 2 ячейки с одним пробелом, пусто 336» |

### Записи 101–108

| № | Волна | Кто это был | Что успел | Ценность | Судьба задачи |
|---|---|---|---|---|---|
| 101 | enterprise-backend | инженер серверного контура: сжатие, кэш, схемы ответов, готовность | составил разбор с обоснованием каждого решения — почему схемы ответов опасны для динамических выдач (`fast-json-stringify` молча срезает необъявленные поля), почему `/api/events` не тронет сжатие (`reply.hijack()` минует `onSend`), какие потолки частоты безопасны при нескольких читателях за одним прокси | ДУБЛЬ | СДЕЛАНА ПОЗЖЕ — коммит `9979e3a` «enterprise-контур: вебхук с продлением каналов, схемы ответов, кэш, сжатие, аккуратное завершение» |
| 102 | owner-voice-and-interactions | археолог голоса владельца, отрезок июнь–июль | **установил границу источника**: самая ранняя запись во всей папке проекта — 13.07.2026, июньских транскриптов нет вовсе; собрал 96 сообщений владельца за 13–31.07, из них 2 — запуски по расписанию, значит настоящих 94; успел записать первую половину реестра (22 566 б) | ДУБЛЬ | СДЕЛАНА ПОЗЖЕ — `audits/2026-08-20-owner-voice/июнь-июль.md` дописан до 39 043 б |
| 103 | wf_9cc94119-eca | инженер вкладки «контроль + дисциплина» | подробно спроектировал паспорт периметра для вкладки «Доверие» (какие оси применимы, какие нет и почему), нашёл, что канонический `Button` уже есть и сырые кнопки надо заменить им | ДУБЛЬ | НУЖНА — карточки К-1 и К-3 вкладки «Доверие» по определению готовности не предъявлены |
| 104 | grand-remelt | переплавщик зоны «дисциплина + качество» | разобрал, каким карточкам зоны режим подведов осмыслен, а каким нет (нагрузка сравнивает управления между собой, гигиена текста привязана к ячейкам) | ПУСТО | НУЖНА — тот же список карточек |
| 105 | svod-programs-tab | аналитик программной структуры для новой вкладки свода | **разведал предметную область**: программы нигде не служат осью группировки; из девяти книг слово «программ» встречается только в книге УО; строка 2 книги УО несёт объединённую надшапку «Объём средств, предусмотренный муниципальной программой на 2026 год» над колонками H–K, то есть плановые деньги — это и есть программные средства; написал извлекатель (`prog_extract.py`, 4 809 б) и разборщик (`prog_analyze.py`, 11 519 б), оба целы | **НОВОЕ** (см. заметку К) | НУЖНА — прямая просьба владельца, спеки вкладки программ в `docs/superpowers/specs/` нет |
| 106 | hardening-and-progress | укрепитель: строгость типов и единицы денег | **замерил цену каждого флага строгости** по обоим пакетам и нашёл настоящую границу единиц — `THOUSANDS_TO_RUB` в `pipeline/monitoring-match.ts`, где `compareMoney(bookThousands, monitoringRub)` принимает два соседних `number \| null` в разных единицах | ДУБЛЬ + **НОВОЕ** побочное (см. заметку Л) | СДЕЛАНА ПОЗЖЕ — `tsconfig.strict.json` в корне с ровно этими флагами и записанными отсрочками, `packages/shared/src/money-units.ts` с типами `ThousandRub`/`Rub`; коммит `0ac7538` |
| 107 | launch-sprint | исполнитель потока запуска | ни одной правки | ПУСТО | ДУБЛЬ задачи #119 |
| 108 | fresh-book-and-live-changes | разборщик свежего дампа книги мониторинга от 21.08 | написал уплотнитель `mon_compact.py`, обрыв связи | ПУСТО | СДЕЛАНА ПОЗЖЕ — `audits/2026-08-21-monitoring-attribute-types.md` (70 439 б), `specs/2026-08-21-monitoring-directory.md` (100 883 б) |

### Записи 109–116

| № | Волна | Кто это был | Что успел | Ценность | Судьба задачи |
|---|---|---|---|---|---|
| 109 | concept-maturity-audit | оценщик зрелости понятий: сколько независимых ответов на один вопрос живёт в коде | **дошёл до сути**: у сигналов своя карта колонок, противоположная канонической, — `signals.ts` читает D как «предмет», хотя D по `column-map.ts:21` — название программы; из этого следует и мёртвый отсев строк-итогов, и ложные сигналы качества на каждой строке текущей деятельности; отдельно поймал, что `signals.ts` заводит собственный `hasFactDate`, затеняя общий | ДУБЛЬ | **НУЖНА, дефект жив три недели**: описан в `audits/2026-07-30-workflow-harvest-raw.md:831-832,853-854` с готовой правкой в одну строку; на 22.08 в коде всё по-прежнему (`signals.ts:278,387,393`) — см. заметку З |
| 110 | week-axis-deep-eval | проверяющий очередную находку по оси недели | написал пробник `axis-probe.mjs` (на диске нет), ни слова вывода | ПУСТО | ДУБЛЬ задачи #127 |
| 111 | cards-atlas | сборщик сводного плана прокачки по семи картам | обнаружил, что карт в каталоге шесть, а не семь, и верно догадался, что седьмая — соседний `2026-08-20-sheet-rules-inventory.md`; на этом лимит модели | ПУСТО | СДЕЛАНА ПОЗЖЕ — `specs/2026-08-20-remelt-plan-by-atlas.md` (136 634 б) |
| 112 | harvest-and-finish | сборщик урожая по волнам | написал разборщики `j_dump.py` (1 375 б) и `a_dump.py` (4 037 б) — оба целы на диске и работают | ПУСТО по содержанию, инструменты целы | СДЕЛАНА ПОЗЖЕ — этими же скриптами построен реестр, по которому идёт нынешний разбор |
| 113 | aemr-blind-spots-audit | охотник за недочитанными вкладками книг | установил живым перечислением, что продукт читает ровно одну вкладку на книгу ГРБС, а у УО их 52; заметил расхождение имени УКСиМП в реестре и в самой книге | ДУБЛЬ | СДЕЛАНА ПОЗЖЕ — `audits/2026-07-30-blind-spots-registry.md:149`, `audits/2026-08-08-divergence-register.md:159` (34 строки на 10 243,25 тыс. руб. вне листа «ВСЕ»), `audits/2026-07-30-workflow-harvest-raw.md:1677` |
| 114 | dash-target-vision | автор целевого видения продукта | два вызова PowerShell — собирался сверить размеры файлов и базы, чтобы числа в документе не были «навскидку» | ПУСТО | СДЕЛАНА ПОЗЖЕ — `specs/2026-07-25-dash-target-vision.md` (89 613 б) |
| 115 | svod-divergence-proof | второй скептик сверки со СВОДом | **воспроизвёл независимо все заголовочные числа** и нашёл в самом документе сверки ошибку: §3 утверждает, что «разница = сумма строк с пустым P» сходится точно, а для УО не сходится — 181 299,81 против 180 375,46, ровно на те же 924,35; отдельно показал, что совпадение тождества держится на случайности сегодняшних данных | **НОВОЕ** (см. заметку И) | НУЖНА — поправка в `audits/2026-08-21-svod-proof/comparison.md` §3 не внесена |
| 116 | aemr-blind-spots-audit | охотник за обрезанием данных по дороге к экрану | нашёл потолок 2 500 точек у облака `/api/rows/scatter` и то, что клиентские фильтры «подвед», «период», «месяцы» на сервер не доезжают, а года у облака нет вовсе | ДУБЛЬ | ЧАСТЬЮ СДЕЛАНА: сервер теперь честен — `truncated`, `pointLimit`, `unreadDepartments` в ответе (`rows.ts:923-929`); **остаток НУЖНА** — запрос по-прежнему принимает только `type`, `activity`, `dept` (`rows.ts:797-799`), и атлас это фиксирует: `audits/2026-08-20-cards-map/analytics-timeline-system.md:756` «отправляются, сервером игнорируются» |

### Записи 117–124

| № | Волна | Кто это был | Что успел | Ценность | Судьба задачи |
|---|---|---|---|---|---|
| 117 | svod-divergence-proof | скептик, независимо перепроверявший расхождение расчёта со СВОДом | пересчитал зеркала своим извлечением, подтвердил равенство 2026 года и остаток 294 592,43 на 554 строках, разобрал строение строк 28/29 листа («ИТОГО 2025+2026» и «ИТОГО 2026», разница ≈ 511 466 — доля 2025), нашёл занижение УО на 924,35 и УД 129,91, поймал ловушку точности при чтении `formattedValue` вместо `effectiveValue` | ДУБЛЬ по числам + **НОВОЕ** по знаменателю (см. заметку Д) | СДЕЛАНА ПОЗЖЕ — `audits/2026-08-21-svod-divergence-verdict.md` содержит те же числа с адресами; геометрия зеркал — `audits/2026-08-21-svod-proof/formulas.md:117-119` |
| 118 | pulse-2-0-design | археолог облика: какой была база до наслоений | успел разделы 1 и часть 2 документа `audits/2026-08-22-design-baseline.md` (записал 17 958 б) | ДУБЛЬ (работа продолжена) | СДЕЛАНА ПОЗЖЕ — документ на диске полон: 28 004 б, четыре раздела, кончается порядком работ на согласование |
| 119 | launch-sprint | исполнитель потока запуска в общем дереве | двадцать вызовов Bash, ни одной правки | ПУСТО | СДЕЛАНА ПОЗЖЕ — `plans/2026-08-07-launch-readiness.md` |
| 120 | week-axis-deep-eval | скептик по обвинению «восполнение недель убивает историю метрик» | **подтвердил всю цепочку по коду**: критерий `beats` знает только строконосность; снимок восполнения несёт пустые `officialMetrics`; сравнение снимков отдаёт 404; на экране гаснут значки дельты и спарклайны мая–июня; восстановить нечем — выгрузки xlsx официальных метрик не содержат | ДУБЛЬ | **НУЖНА, дефект жив**: `audits/2026-07-30-workflow-harvest-raw.md:1294` описывает его подробнее (8 недель 07.05–25.06, прогон модели «было 13 недель с метриками → стало 5») и помечен «НЕ исправлено»; на 22.08 в коде всё по-прежнему: `snapshot-retention.ts:47-48`, `backfill-weekly.ts:126`, `routes/history.ts:39-45` |
| 121 | report-full-eval | проверяющий обвинение по тракту «Отчёт» | одиннадцать чтений, семь поисков, вывода нет | ПУСТО | НУЖНА — сквозной оценки вкладки «Отчёт» в `docs/` нет; ближайшее `qa/report-family-product-review-2026-07-23.md` |
| 122 | borders-and-consolidation | чистильщик обводок в зоне «контроль + дисциплина + аналитика» | посчитал контрасты и нашёл две «слишком тёмные» плитки: `Settings` L739 (`zinc-900/30`) и `ScorecardSection` L133 (`zinc-900/40`) дают контраст к карточке около 1,01 — то есть невидимы; вывел рабочие числа: карточка к странице 1,158, плитка к карточке 1,139, плитка к странице 1,319; завёл словарь `components/control/surfaces.ts` | **НОВОЕ** (см. заметку Е) | СДЕЛАНА ПОЗЖЕ в главном — обводки сняты коммитами `7e2696d` и `dad69a8`; словарь на диске вырос до 11 532 б |
| 123 | wave-2-signals-monitoring | инженер сигнала «факт ЕП отклоняется от плана» | обнаружил, что работа уже сделана параллельным составом, проверил все семь мест реализации, прогнал типизацию и тесты — зелено | ДУБЛЬ (ценна как приёмка) | СДЕЛАНА — сигнал `epFactDeviation` в дереве, проверен им же |
| 124 | graph-and-skills | подборщик внешних скиллов под нужды проекта | разобрал кандидатов с обоснованием: взять `drizzle-safe-migrations`, `typescript-refactor`, `javascript-testing-expert` (fast-check); отказать `legacy-modernizer`, `virtual-lists`, `vm0 google-sheets`, `data-quality-checker`, `zod-schema-validation` — с причиной по каждому | **НОВОЕ** (см. заметку Ж) | НУЖНА — решения нигде не записаны, ни один из трёх не установлен |

### Записи 125–132

| № | Волна | Кто это был | Что успел | Ценность | Судьба задачи |
|---|---|---|---|---|---|
| 125 | aemr-blind-spots-audit | охотник за молчаливым недочтением источника, область ШДЮ и листа «СВОД с месяцами» | дошёл до чтения живого листа, написал три пробника (`_audit_shdyu.mts`, `packages/server/_audit_shdyu.mts`, `_audit_meta.mts` — ни одного на диске нет, убраны), последняя мысль: лист крупнее ожидаемого, `JSON.stringify` захлёбывается, надо снимать геометрию и лить NDJSON построчно | ПУСТО (метод без результата) | СДЕЛАНА ПОЗЖЕ — `audits/2026-07-30-blind-spots-registry.md`, `audits/2026-08-21-svod-divergence-verdict.md`, каталог `audits/2026-08-21-svod-proof/` |
| 126 | week-debt-execution | исполнитель долга «база знаний метрик» | одиннадцать вызовов Bash, ни слова, ни записи | ПУСТО | СДЕЛАНА ПОЗЖЕ — `packages/core/src/metrics/kb-uplift.ts` с тестом, `packages/web/src/pages/kb-additions.ts` и `kb-additions-control.ts`, спека `specs/2026-08-20-kb-uplift.md` |
| 127 | week-axis-deep-eval | скептик по обвинению «чужая неделя молча становится неделей N» | прошёл `getSnapshotAtOrBefore` (нижней границы нет), путь отчёта прошлой недели, разобрал сценарий десяти четвергов 07.05→16.07.2026, проверил тесты и нашёл, какие случаи ими не покрыты | **НОВОЕ** частично (см. заметку Б) | ЧАСТЬЮ УСТАРЕЛА: кросс-недельный случай теперь закреплён тестом (`packages/server/src/services/snapshot.test.ts:85-100`), плашка называет дату снимка (`routes/report.ts:200-202`); **остаток НУЖНА** — верхней границы давности у подстановки нет, снимок десятинедельной давности подаётся так же, как вчерашний |
| 128 | report-full-eval | проверяющий сценарий с контекстом фильтров | шесть чтений, две реплики, вывода нет | ПУСТО | ДУБЛЬ задачи #121 |
| 129 | week-axis-deep-eval | измеритель цены хранения и выборки снимков | **живой замер по базе 24.07.2026**: 8 снимков, 46,8 МБ суммарно, средний 5,85 МБ, максимум 6,19 МБ; строк со строками 7 из 8; индексов на таблице не было (только автоиндекс первичного ключа), план запроса — `SCAN snapshots` + временное B-дерево; выборка 24,7–39,2 мс даже при нулевой выдаче против 0,2 мс без предиката `instr`; разбор самого крупного документа — 175,4 мс | **НОВОЕ** (см. заметку В) | СДЕЛАНА ПОЗЖЕ по главному: индекс `idx_snapshots_created` появился, перепроверено 22.08 на живой базе — план боевого запроса `SEARCH snapshots USING INDEX idx_snapshots_created` |
| 130 | concept-maturity-audit | перепись потребителей: какие модули ядра и маршруты сервера никем не вызваны | назвал `linearForecast`, `classifySubject`, `ewmaDetection` без потребителей; маршруты `/analytics/anticorruption`, `/analytics/scorecard`, отладочный sheets и деталь issues — без обёртки в клиенте | ДУБЛЬ | СДЕЛАНА ПОЗЖЕ и подробнее — `audits/2026-07-30-workflow-harvest-raw.md:268` («два контура аналитики, один почти целиком не подключён»), `audits/2026-08-18-debt-inventory.md:159,268` |
| 131 | report-full-eval | скептик по обвинению «две календарные оси на одной странице» | **довёл доказательство до чисел и адресов**: веб метит снимки UTC-сутками `dayNumberOf(createdAt)`, сервер — продуктовым днём (+12); снимок 2026-07-23T22:00Z для веба четверг 23-е, для сервера пятница 24-е; показал, что камчатское рабочее утро попадает в UTC-вечер предыдущих суток, то есть случай не редкий, а типичный | **НОВОЕ** (см. заметку Г) | НУЖНА — `packages/web/src/lib/report/week-delta.ts:9,32` по-прежнему считает `dayNumberOf`, хотя канонический `productCalendarDay` уже переехал в `packages/shared/src/time-selection.ts:35` |
| 132 | economy-trend | автор честного ответа «насколько выросла экономия с начала проекта» | ничего, недельный лимит | ПУСТО | НУЖНА — прямой вопрос владельца; документа с разделением «рост экономии / рост охвата / рост точности» в `docs/` нет |

### Записи 133–140

| № | Волна | Кто это был | Что успел | Ценность | Судьба задачи |
|---|---|---|---|---|---|
| 133 | course-distill | автор учебного курса по продукту для нового человека | ничего, лимит модели | ПУСТО | НУЖНА — вводного курса в `docs/` нет; ближайшее по смыслу `specs/2026-08-18-work-harness.md` (11 679 б) описывает харнесс работы, а не устройство продукта |
| 134 | aggregation-pyramid | проверяющий полноту «Пирамиды агрегации» по линзе ПОЛНОТА | усомнился, что параметр `year` у `GET /api/rows/:deptId` (`rows.ts:79`) описан в документе | ПУСТО (подозрение не подтвердилось) | СДЕЛАНА ПОЗЖЕ — документ этот адрес называет: `specs/2026-08-07-aggregation-pyramid.md:159` |
| 135 | monitoring-2-0 | строитель книги «Ежедневный мониторинг 2.0» | прочёл входные паспорта, ни одной правки книги | ПУСТО | СДЕЛАНА ПОЗЖЕ — `specs/2026-08-21-monitoring-book-perfect.md` (27 747 б), `specs/2026-08-21-monitoring-fix-addresses.md` (19 031 б), `audits/2026-08-21-protections-applied.md` (17 701 б) |
| 136 | report-full-eval | скептик, проверявший обвинение расчёта экономии в потере отрицательных величин | **самая содержательная гибель доли**: полностью прошёл путь отрицательной экономии от `calc-engine` до плитки и текста, назвал адреса и числа, опроверг одну часть обвинения и подтвердил другую | ДУБЛЬ по главному + **НОВОЕ** по остатку (см. заметку А) | ЧАСТЬЮ СДЕЛАНА ПОЗЖЕ: обрезка `Math.max(0, …)` снята и в `approvedEconomy` (`calc-engine.ts:386-389`), и в `economyTotalMath` (`calc-engine.ts:760-765`); **остаток НУЖНА** — строка экономии в отчёте по-прежнему прячет отрицательную сумму (`packages/web/src/lib/report/mappers.ts:664`) |
| 137 | r2a-fixes | исполнитель находок ponytail-ревью диффа R2a | семь чтений, ни слова | ПУСТО | УСТАРЕЛА — дифф R2a давно влит, ревью относилось к незакоммиченному состоянию |
| 138 | launch-prep | сборщик документа «К запуску: метрики, чепуха, потеряшки» | ничего, лимит модели | ПУСТО | СДЕЛАНА ПОЗЖЕ — `plans/2026-08-07-launch-readiness.md` (42 839 б) |
| 139 | week-debt-execution | исполнитель долга «мобильная вёрстка» | написал измеритель `packages/web/scripts/measure-adaptive.mjs` (1 351 б, на диске есть, в git не заведён) | ПУСТО по вёрстке, инструмент цел | НУЖНА — мобильная вёрстка по `specs/2026-08-19-layout-three-modes.md` не предъявлена; измеритель готов к работе |
| 140 | design-arsenal | разведчик библиотек для облика дэша | восемь веб-поисков: shadcn-реестры, OriginUI, Magic UI, Tremor, TanStack Table, замены Framer Motion, моноширинные с табличными цифрами, NumberFlow для анимации чисел | ПУСТО (имена без оценки и без записи) | СДЕЛАНА ПОЗЖЕ — `specs/2026-08-07-design-arsenal.md` (30 064 б) |

### Записи 141–151: погибшие на разгоне

Одиннадцать агентов, каждый оборвался в первые несколько вызовов. Одна
находка среди них настоящая (#142), остальные не дошли даже до чтения
своих документов.

| № | Волна | Кто это был | Что успел | Ценность | Судьба задачи |
|---|---|---|---|---|---|
| 141 | harvest-and-finish | сборщик урожая по своим четырём каталогам волн | восемь вызовов Bash, ни одной записи | ПУСТО | СДЕЛАНА ПОЗЖЕ — `audits/2026-08-21-agents-harvest.md` (1 607 б) и текущая волна разбора |
| 142 | enterprise-backend | оптимизатор фронтового бандла и первого кадра | нашёл, что `packages/web/index.html` тянет шрифты с `fonts.googleapis.com`, тогда как `src/index.css` прямо объявляет самохост «за периметром без интернета»; успел объяснить механизм отказа, замерить не успел | ДУБЛЬ (исправлено) | СДЕЛАНА ПОЗЖЕ — ссылка снята коммитом `90d47a3`; на её месте в `index.html:11-13` стоит комментарий с той же причиной |
| 143 | design-arsenal | разведчик чужих дизайн-скиллов | шесть веб-поисков, назвал кандидатов (LibreUIUX-Claude-Code, Sumi-1.0, наборы Marie Claire Dean), проверил существование репозиториев; списка на диск не положил | ПУСТО (имена кандидатов без оценки) | СДЕЛАНА ПОЗЖЕ — `specs/2026-08-07-design-arsenal.md` (30 064 б) |
| 144 | week-debt-execution | исполнитель долга «объединение закупок» | четыре вызова Bash, увидел готовый `MergeCandidates.tsx` | ПУСТО | СДЕЛАНА ПОЗЖЕ — `packages/web/src/components/competition/MergeCandidates.tsx` и `packages/core/src/analytics/subject-fuzzy.ts` в дереве, последняя правка коммитом `7e2696d` |
| 145 | aemr-blind-spots-audit | сводчик пяти областей аудита слепых зон в один документ | ни одного вызова: обрыв на первой же реплике. Но в его поручении дословно приведены находки пяти областей — это единственное место, где сохранён их полный текст | ДУБЛЬ (в реестре) | СДЕЛАНА ПОЗЖЕ — `audits/2026-07-30-blind-spots-registry.md` (29 482 б) |
| 146 | aggregation-pyramid | проверяющий полноту документа «Пирамида агрегации» | три Glob, два Read, два Grep — ни слова | ПУСТО | СДЕЛАНА ПОЗЖЕ — `specs/2026-08-07-aggregation-pyramid.md` (58 404 б) |
| 147 | wf_9cc94119-eca | хранитель атласа карточек | три Bash, обрыв связи | ПУСТО | НУЖНА — требование владельца 21.08 «расширить карты до полного понимания системы»; атлас `audits/2026-08-20-cards-map/` есть, но консолидации по голосу владельца в нём нет |
| 148 | economy-coefficient-and-tables-2-0 | искатель происхождения коэффициента экономии 8 % | два Bash, только план работ | ПУСТО | СДЕЛАНА ПОЗЖЕ — `specs/2026-08-18-living-economy-coefficient.md` (86 663 б) |
| 149 | monitoring-2-0 | автор описания книги «Ежедневный мониторинг 2.0» в docx | один Bash и вызов скилла | ПУСТО | СДЕЛАНА ПОЗЖЕ — `C:/Users/filat/Documents/материалы/Мониторинг 2.0 — что сделано.docx` (24 016 б, 21.08 17:34) |
| 150 | aggregation-pyramid | второй проверяющий пирамиды агрегации | один Bash | ПУСТО | ДУБЛЬ задачи #146 |
| 151 | remelt-stage-a | строитель механики «цена отказа от конкуренции» | ничего, недельный лимит | ПУСТО | СДЕЛАНА ПОЗЖЕ — `packages/web/src/components/competition/CostOfRefusal.tsx` в дереве |

### Записи 152–168: хвост, срезанный лимитом модели

Семнадцать агентов подряд одного размера (107–108 КБ — это вес одного лишь
поручения) погибли, не сделав ни одного вызова инструмента. Пятнадцать из
них — параллельный веер оценки «одной оси недели» (волна `week-axis-deep-eval`,
каталог `wf_550e4d12-76f`), два — веер оценки вкладки «Отчёт»
(`report-full-eval`, каталог `wf_2b4698e7-d58`). Единственная реплика в
каждом транскрипте: «You've reached your Fable 5 limit». Исключение — #153,
успевший один вызов Bash.

Это не обрыв связи, а упор в потолок расхода: оркестратор запустил веер
шире, чем позволял остаток лимита, и весь хвост веера умер на первом вдохе.

| № | Волна | Кто это был | Что успел | Ценность | Судьба задачи |
|---|---|---|---|---|---|
| 152 | week-axis-deep-eval | оценщик конструкции «одна ось недели» | ничего, лимит модели | ПУСТО | СДЕЛАНА ПОЗЖЕ — «ось недели» закоммичена, живёт в `packages/server/src/services/weekly-snapshot-cron.ts` и соседях |
| 153 | wf_9cc94119-eca | инженер вкладки «экономия+конкуренция» | один вызов Bash | ПУСТО | НУЖНА — карточки Economy/Competition по определению готовности (`specs/2026-08-21-card-done-definition.md`) не предъявлены |
| 154 | week-axis-deep-eval | тот же веер оценки оси недели | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 155 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 156 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 157 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 158 | report-full-eval | оценщик тракта вкладки «Отчёт» | ничего | ПУСТО | ДУБЛЬ задачи #121 |
| 159 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 160 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 161 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 162 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 163 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 164 | report-full-eval | оценщик тракта «Отчёт» | ничего | ПУСТО | ДУБЛЬ задачи #121 |
| 165 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 166 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 167 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |
| 168 | week-axis-deep-eval | тот же веер | ничего | ПУСТО | ДУБЛЬ задачи #152 |


---

## Развёрнутые заметки по находкам НОВОЕ

### О. Замена написана, но не включена: каналы Drive продолжают плодиться (#86)

Самая дорогая незакрытая работа доли. Инженер волны `enterprise-backend`
разобрал живой регистратор push-каналов Google Drive и назвал четыре
дефекта:

1. срок жизни канала берётся из собственного запроса, а не из того, что
   вернул Drive;
2. прежний канал не гасится через `channels.stop` — каждая перерегистрация
   оставляет новый идентификатор канала на тот же файл;
3. при неудаче регистрации нет отступа с нарастанием;
4. срок нигде не хранится, поэтому молчаливый отказ канала не виден.

Он написал замену — `packages/server/src/services/webhook-channel.ts` —
и честно предупредил в разборе: пока запуск не переключат, будут работать
оба, и каналы задвоятся.

Проверено 22.08.2026:

- замена на диске и полна: 41 427 б, с `drive.channels.stop` на строке 644,
  с `startWebhookChannels` (:817), `rescheduleWebhookChannels` (:856),
  `stopWebhookChannels` (:866);
- подключена она **только со стороны приёма** уведомлений —
  `packages/server/src/routes/webhook.ts:41` берёт из неё разбор заголовков,
  отсев повторов, счётчики и состояние;
- **регистрацию по-прежнему ведёт старый модуль**: `app.ts:40` импортирует
  `startDriveWatch`, `app.ts:372` его запускает;
- старый модуль всё так же просит срок сам —
  `drive-watch.ts:73` `expiration: String(Date.now() + CHANNEL_TTL_MS)`
  при `CHANNEL_TTL_MS = 24 ч` (:38) и перерегистрации каждые
  `RENEW_EVERY_MS = 20 ч` (:106);
- слова `channels.stop` в `drive-watch.ts` нет вовсе.

Что это значит на работающем сервере. Каждые двадцать часов появляется новый
канал на тот же файл, а прежний живёт ещё четыре часа. В окне пересечения
одна правка книги порождает два уведомления. Отсев повторов на стороне приёма
их не склеит: он ключуется парой «канал + номер сообщения», а у двух каналов
номера свои. Значит окно в четыре часа каждые сутки продукт перечитывает
книги вдвое чаще, чем нужно, — и это при том, что чтения ограничены квотой
Google.

Работа сделана на девяносто процентов и стоит одной строки в `app.ts`
плюс удаления `drive-watch.ts`. Перед переключением стоит проверить, что
`startWebhookChannels` принимает тот же вид журнала, что и старая функция
(судя по сигнатуре `WatchLog` — да), и прогнать тесты вебхука.

### П. Допуски регресса честны — с числом (#91)

Ревизор потока «расчёт против ручного отчёта» не поверил отчёту потока, а
перепроверил. Три его результата стоит сохранить.

Первое: упавший тест был устаревшим, а не код. По живым книгам колонка B —
«Наименование управления», U — «Причина отклонения»; тест ждал другого.
Проверку он сделал не по памяти, а чтением заголовков живой книги.

Второе, редкой основательности: он проверил хвостовые колонки по **64 книгам
за восемь недель** и получил, что AE — «Комментарий ГРБСа» везде, без
исключений. Это то самое основание, которого обычно не хватает, чтобы
объявить колонку каноничной.

Третье и главное: он замерил, не прячут ли допуски регресса расхождение.
Наибольшее отклонение по деньгам — **0,005 тыс. руб.**, при допуске **0,05**.
Причём 0,005 — это ровно предел округления, с которым составлен ручной отчёт.
Вывод: допуск в десять раз шире наибольшего наблюдаемого расхождения и при
этом заведомо у́же любой содержательной ошибки. Допуски честны.

Ценность в том, что вопрос «а не спрятан ли за допуском настоящий разрыв»
задают каждой волне регресса, и теперь на него есть ответ числом. В
`specs/2026-07-29-report-calculation-rules.md` и в фикстурах недель этого
числа нет.

### М. Жёстко зашитый год в разборе листа СВОД: фитиль на 2027-й (#94)

Оценщик зрелости понятий (агент `a6509823a081`) разбирал разборщик
официального листа и нашёл в нём привязку к паре лет.

`packages/shared/src/svod-grid.ts`, две одинаковые развилки — строки 312-318
для сводных итогов и 390-395 для итогов блока:

```
if (a.includes('2025+2026')) block.totalBothYears = metrics;
else                         block.totalY2026    = metrics;
```

Разбор идёт по подписи строки. Пока на листе ровно две итоговые строки —
«Итого … 2025+2026» и «Итого … 2026» — развилка работает. В 2027 году на
листе появятся «Итого … 2026+2027» и «Итого … 2027»: первая не содержит
подстроки «2025+2026», значит уйдёт в `else`; вторая тоже уйдёт в `else`.
Обе лягут в одно поле, победит последняя — и двухлетний итог молча займёт
место годового.

Автор находки полагал дефект спящим, потому что итоги «никто не читает в
проде». Проверено 22.08 — читают: `packages/core/src/report/build-report.ts:222`
(`extras?.scopes.find(…)?.totalY2026`) и `:738` (`district?.totalY2026`).
То есть в 2027-м число уедет прямо в отчёт.

Починка, которую он же и назвал: различать двухлетний итог по наличию знака
«+» в подписи, а не по конкретным цифрам, и писать в журнал строку с
неопознанной подписью вместо молчаливого падения в `else`.

Рядом он назвал вторую хрупкость того же разборщика: если строка периода
встречается при закрытом блоке — например, ГРБС переименовали и его имя
пропало из реестра, — строки исчезают без единого предупреждения.

### Н. Разбор предупреждений безопасности: 16 живы на 22.08 (#95)

Сметчик долга (агент `abcbaf311d10`) не просто прогнал `pnpm audit`, а
разобрал каждое предупреждение по существу — что редкость.

Его разбор по существу:

- **`@fastify/static`** — установлена 9.1.3, уязвимость закрыта только в
  10.1.2, то есть требуется переход на следующую большую версию. Он
  проследил риск до конца: раздача статики включается, когда рядом лежит
  каталог `public/`, а перехватчик доступа стережёт только пути с `/api/`,
  поэтому статика открыта по построению. Опасность в том, что обход каталога
  может увести за пределы `public/` — к файлу вроде `.env.production`
  в каталоге развёртывания. Это не транзитивная мелочь, а высокий риск,
  и он назвал его главным.
- `fast-uri` 3.1.2 при нужных 3.1.5+, причём в корне стоит закрепление
  `"fast-uri": "^3.1.2"`, которое само по себе держит старую версию.
- `nanoid` — уязвимость касается нулевого и отрицательного размера, а вызов
  в проекте идёт без аргумента, значит не задет.
- `find-my-way`, `postcss`, `brace-expansion`, `shell-quote` — транзитивные
  либо только сборочные.

Проверено 22.08.2026 прогоном `pnpm audit --audit-level moderate`:
**16 предупреждений — 1 низкое, 2 средних, 13 высоких**. Среди них
`@fastify/static` (уязвимы ≤ 10.1.1, закрыто в 10.1.2, путь
`packages__server>@fastify/static`) и `postcss` (уязвимы ≤ 8.5.22).
Закрепление `fast-uri: ^3.1.2` в корневом `package.json:54` на месте.

При этом `docs/REVIEW.md:58-61` описывает остаток предупреждений на
24.07.2026 и называет только `fast-uri`, `find-my-way`, `brace-expansion`
и `shell-quote` — самого опасного, `@fastify/static`, там нет. Правило
репозитория требует держать `docs/REVIEW.md` в согласии с состоянием
безопасности; сегодня он от него отстал.

### К. Программы: что уже известно для будущей вкладки (#105)

Владелец просил вкладку свода «экономические показатели по программам и
подпрограммам». Аналитик волны `svod-programs-tab` умер на разведке, но три
его установленных факта экономят следующему составу день работы.

Первое: программы сегодня не служат осью нигде. Ни в расчёте, ни в отчёте,
ни в аналитике программа не является ключом группировки — он проверил это
и по коду отчёта, и по `classifyActivity`, и по единой сетке.

Второе: официальный лист «СВОД ТД-ПМ» и «СВОД с месяцами» не упоминают
программы вовсе. Из девяти книг слово «программ» встречается только в книге
УО, и то в скопированных строках ГРБС. То есть программную вкладку не из
чего собрать сверху — только снизу, из строк книг по колонкам D и E
(название программы и подпрограммы).

Третье и самое ценное: строка 2 книги УО несёт объединённую надшапку над
колонками H–K — «Объём средств, предусмотренный муниципальной программой на
2026 год». Это значит, что плановые деньги строки и есть программные
средства, и отдельного источника «сколько дала программа» искать не нужно.
Тот же заголовок, кстати, служит основанием вопроса о знаменателе
законтрактованности (заметка Д) — колонка H по своему названию относит все
свои суммы к 2026 году.

Готовые к работе инструменты: `prog_extract.py` (4 809 б) достаёт из дампов
строки с программой, подпрограммой, деньгами, флагом экономии, способом и
годом; `prog_analyze.py` (11 519 б) сводит их с нормализацией названий для
поиска вариантов написания одной программы. Оба лежат в
`…/25733ef5-…/scratchpad/` и переживают своего автора.

### Л. Цена строгости замерена, но один артефакт волны попал в репозиторий (#106)

Укрепитель прогнал `tsc` с каждым флагом строгости по отдельности и получил
цену каждого. Его числа легли в `tsconfig.strict.json`, который сегодня стоит
в корне и от которого наследуют `shared` и `core`: восемь флагов включены,
два отложены с записанной ценой — `noUncheckedIndexedAccess` (1 102 места:
shared 154, core 948) и `noPropertyAccessFromIndexSignature` (113 мест:
shared 14, core 99). Его же вторая работа — брендированные единицы денег —
живёт в `packages/shared/src/money-units.ts` (`ThousandRub`, `Rub`,
`toRub`, `toThousandRub`).

Рабочая мера, которая пригодится следующим: один прогон `tsc` на этой
машине занимает около двух минут, поэтому веер флагов он гонял параллельно.

Побочная находка, которая пережила волну как дефект. Разбираясь с базовой
линией `core`, он наткнулся на чужой пробный файл, ломавший проверку типов,
и правильно решил его не трогать. Файл с тех пор **попал в git**:

`packages/core/src/__scan-all.test.ts` (38 строк, коммит `0ac7538`)

Это не тест продукта. Он читает
`C:/Users/filat/AppData/Local/Temp/claude/C--Users-filat-dash/1eec411b-…/scratchpad/all-books-rows.json`
и пишет туда же `signal-scan.json`. Проверено 22.08: файл-источник на этой
машине пока существует, поэтому `pnpm -r test` зелен по случайности. На любой
другой машине и после очистки временных каталогов прогон упадёт с ошибкой
чтения файла.

Отдельно это прямое нарушение правила репозитория из `CLAUDE.md`: «Do not put
… local agent artifacts into git». Проверка типов при этом зелёная —
`npx tsc --noEmit -p packages/core/tsconfig.json` возвращает ноль
(проверено 22.08), так что опасность только в тестах и в чистоте дерева.

Что делать: файл убрать из git. Если живой прогон сигналов по всем книгам
нужен как жанр, он должен читать фикстуру из репозитория (как это уже сделано
для недельных выжимок `packages/core/src/report/__fixtures__/week-*.json`),
а не из чужого временного каталога.

### З. Сигналы читают чужую колонку — правка в одну строку, лежит три недели (#109)

Оценщик зрелости понятий (агент `a9787915ef10`) пришёл к тому же выводу, что
и разбор от 30.07, и это единственная находка доли, где ошибка не только
описана, но и живёт в коде на день разбора.

Проверено 22.08.2026 по файлам:

- `packages/shared/src/column-map.ts:21` — `PROGRAM_NAME: 3, // D —
  Наименование программы (X/Х/пусто = нет программы → ТД вне рамок ПМ)`;
  `:24` — `SUBJECT: 6, // G — предмет закупки`;
- `packages/core/src/pipeline/signals.ts:386-387` — доккомментарий:
  «есть предмет закупки (D) или суммы (K)»;
- `signals.ts:390,393` — `const d = cellText(cells, 'D')` и отсев
  `['итого','всего','раздел','блок']` по этой же D;
- `signals.ts:278` — `REQUIRED_COLUMNS = ['D', 'K', 'L']`.

Два следствия, оба видны человеку:

Первое — отсев строк-итогов не работает. Слова «итого» и «всего» стоят в
столбце предмета (G) или в подведе (C), а ищут их в столбце программы.
Строка-итог с числом в K проходит как строка данных, и на неё считаются
сигналы.

Второе — ложные сигналы качества. `REQUIRED_COLUMNS` требует непустую D,
а канон колонок прямо говорит: пусто или «Х» в D — законное состояние для
текущей деятельности вне программных мероприятий. Значит каждая такая строка
с датой факта или просроченной плановой датой получает жёлтый значок
«Пустые обязательные поля». Значки видны в таблице строк (через `rows-dto`)
и уходят в снимок как Issue (через `orchestrator`).

Правка, предложенная ещё 30.07 и не сделанная: заменить `'D'` на `'G'` в
проверке предмета и убрать `'D'` из `REQUIRED_COLUMNS`, шапку `signals.ts`
переписать ссылкой на `DEPT_COLUMNS` вместо своей копии карты. Один тест:
строка с пустой D и заполненной G проходит `isDataRow`.

### И. Поправка к документу сверки: §3 сходится не для всех (#115)

Второй скептик волны `svod-divergence-proof` (агент `a0e4620b29ed`)
воспроизвёл сверку своим извлечением и подтвердил все заголовочные числа.
Ценность его гибели — в двух замечаниях к самому документу сверки, которые
он успел сформулировать и не успел записать.

Первое, проверяемое прямо в тексте. Раздел `comparison.md` §3 назван
«Гипотеза "разница = сумма строк с пустым P": сходится точно». Для УО она не
сходится: §2 (строка 82) даёт разницу **+181 299,81458**, §3 (строка 115) —
сумму строк с пустым P **180 375,46458**. Разрыв — ровно 924,35, те самые,
которым §4 посвящён отдельно. Заголовок §3 верен для семи ГРБС из восьми;
для восьмого его надо оговорить, иначе документ противоречит сам себе через
две страницы.

Второе, важнее для будущего. Тождество «сырой итог минус официальный = сумма
строк с пустым P» держится потому, что сегодня ни у одного ГРБС, кроме УД и
УО, нет строк с парой (1 квартал, 2025), и нет пар (2, 2025) и (3, 2025)
вовсе. Это свойство данных на дату дампа, а не свойство конструкции: появится
у любого управления строка первого квартала 2025-го — и тождество разойдётся
молча. Сверка, которая держится на совпадении, должна об этом сказать вслух
и получить сторожа.

### Д. Знаменатель законтрактованности: 64 % посчитаны по смешанным периметрам (#117)

Вердикт сверки (`audits/2026-08-21-svod-divergence-verdict.md:150-160`) честно
ставит вопрос: официальные 78,29 % законтрактованности (`Q29`) считаются от
1 332 448,46 тыс. руб. — плана 2026 года, — а 294 592,43 тыс. руб. на 554
строках без года в знаменатель не входят вовсе; если отнести их к 2026 году,
как велит заголовок колонки H, законтрактованность падает примерно до 64 %.

Скептик #117 заметил то, чего в вердикте нет: остаток 294 592,43 измерялся
против совокупного итога 2025 + 2026 (`K28` = 1 843 914,69), а знаменатель
64 % берётся только за 2026 (`K29` = 1 332 448,46). Разница строк 28 и 29 —
около 511 466 тыс. руб. — это доля 2025 года, которая в книгах есть. Значит
часть из 554 бесхозных строк может относиться к 2025-му, и тогда 64 % —
оценка снизу, а не второе честное число.

Что из этого следует: прежде чем ставить обе величины рядом на экране (а
вердикт требует именно этого), надо разложить 554 строки по годам — по имени
предмета, по датам, по книге. Проверка, которую скептик собирался запустить и
не успел: пересечение имён бесхозных строк с датированными, чтобы исключить
двойной счёт в знаменателе.

### Е. Невидимые плитки: посчитанные контрасты (#122)

Чистильщик обводок посчитал контрасты сложением полупрозрачных слоёв, а не
на глаз. Его рабочие числа для тёмной темы:

| Пара поверхностей | Контраст |
|---|---|
| Карточка к странице | 1,158 |
| Плитка `white/[0.05]` к карточке | 1,139 |
| Плитка к странице | 1,319 |
| Плитка `zinc-900/30` к карточке | ≈ 1,01 |

Последняя строка — это дефект: подложка темнее карточки, на которой лежит,
и плитка исчезает. Он нашёл две такие: `Settings` строка 739 (`zinc-900/30`)
и `ScorecardSection` строка 133 (`zinc-900/40`). Порог, которым он мерил, —
1,12; `white/[0.04]` дал 1,1105 и не прошёл, поэтому в словаре осталось
`white/[0.05]`.

Ценность этих чисел не в них самих, а в методе: система облика
(`specs/2026-08-18-design-system.md`) считает контрасты для текста, а для
вложенных поверхностей тёмной темы порога не объявляет. Здесь он выведен и
проверен — 1,12 между соседними ступенями. Такой порог стоит внести в систему
облика, иначе следующий состав снова подберёт подложку на глаз.

### Ж. Внешние скиллы: разбор кандидатов с отказами (#124)

Подборщик волны `graph-and-skills` не установил ничего, но провёл разбор,
которого в проекте больше нет. Итог его работы:

Взять — три:

- `drizzle-safe-migrations` — проект живёт на drizzle-kit и better-sqlite3,
  локального скилла на безопасные миграции нет, а впереди была работа
  по снимкам и хранению;
- `typescript-refactor` — против разбухших `Settings.tsx`, `Analytics.tsx`
  и остатков `any`; общий `simplify` этого не покрывает;
- `javascript-testing-expert` (от автора fast-check) — свойственное
  тестирование поверх vitest для расчётных модулей (движок расчёта, сверка,
  пороги 44-ФЗ); локальный `test-driven-development` этого не даёт.

Отказать — пять, с причиной:

- `legacy-modernizer` — про удушающий фикус на монолитах и микросервисах,
  наш монорепозиторий мал; полезен только характеризационными тестами,
  которые уже покрыты;
- `virtual-lists` — устарел (react-virtualized), а `DataBrowser` и так
  листает по 25 строк с потолком 1000, виртуализация не нужна;
- `vm0 google-sheets` — обёртка чужой платформы вокруг curl и токенов;
- `data-quality-checker` — про рыночные и блоговые документы на Python;
- `zod-schema-validation` — избыточен, проверка на zod 3.25 уже стоит,
  а скилл заточен под четвёртую версию и внесёт путаницу.

Проверено 22.08: ни один из трёх «взять» не установлен, решения нигде
не записаны. Разбор стоит перенести в реестр инструментов, чтобы следующий
состав не искал заново — и не установил отвергнутое.

### Б. Подстановка снимка без верхней границы давности (#127)

Скептик волны `week-axis-deep-eval` (агент `a12411a68d89`) разбирал обвинение
«чужая неделя молча становится неделей N». Его разбор:

- `getSnapshotAtOrBefore` ограничивает выборку только сверху
  (`packages/server/src/services/snapshot.ts:924-933`), снизу — ничем;
- разобранный сценарий: снимки есть только по 07.05.2026, читатель просит
  отчёт на 16.07.2026 (десять четвергов спустя) — вернётся майский снимок;
  плановые колонки в мае уже заполнены, а факт третьего квартала в майском
  снимке физически отсутствует, поэтому отчёт выглядит правдоподобно:
  план полон, факт почти нулевой;
- восполнение прошлых четвергов (`backfill-weekly`) дыру не закрывает: оно
  запускается по вызову, а недели до развёртывания снимков не имеют.

Что стало потом. Кросс-недельный возврат теперь закреплён тестом как
намеренное поведение (`packages/server/src/services/snapshot.test.ts:85-100`:
снимок 12.03 возвращается на запрос 19.03, а на 11.03 — честный `null`), и
маршрут ставит плашку с датой снимка (`routes/report.ts:198-202`). То есть
подмена не молчаливая — дата названа.

Что осталось. Верхней границы давности нет: снимок недельной и снимок
десятинедельной давности подаются одинаково и одинаково подписаны. Плашка
говорит «построен из снимка такого-то числа», но не говорит, что число
отстоит от запрошенного среза на десять недель. По канону п. 53
(механизм, адрес, действие) читателю не хватает именно последнего шага.

### В. Цена снимка: замеренные числа (#129)

Агент `afaf657c2d5a` — единственный в доле, кто снял живые числа по базе
снимков (`packages/server/data/aemr.db`, состояние на 24.07.2026):

| Что мерилось | Число |
|---|---|
| Снимков в таблице | 8 |
| Суммарный вес поля `data` | 46,8 МБ |
| Средний снимок | 5,85 МБ (от 3,63 до 6,19 МБ) |
| Снимков со строками | 7 из 8 |
| Разбор самого крупного документа (6,19 МБ) | 175,4 мс |
| Боевой запрос выборки, три прогона | 38,8 · 39,2 · 24,7 мс |
| Тот же запрос без предиката `instr`, только идентификаторы | 0,2 мс |
| План запроса на тот момент | `SCAN snapshots` + временное B-дерево |

Перепроверено 22.08.2026 на той же базе (чтение только): индекс
`idx_snapshots_created` появился, и боевой запрос — с предикатом `instr` —
теперь идёт по нему (`SEARCH snapshots USING INDEX idx_snapshots_created`).
Главное узкое место, которое он замерил, закрыто.

Что из его чисел не потеряло силы: снимок стоит около шести мегабайт, а его
разбор — около 175 миллисекунд. Это цена одного отчёта прошлой недели, и в
документах проекта она нигде не записана; политику хранения снимков стоит
обсуждать с этими числами на руках.

Побочное наблюдение: сторожевой тест плана запроса
(`packages/server/src/db/ddl.test.ts:124-136`) утверждает в комментарии
«Запрос — тот же, что в `getSnapshotAtOrBefore`», но проверяет запрос без
предиката `instr`. Сегодня планы у обоих вариантов совпадают (проверено),
так что вреда нет, — но сторож стережёт не ту строку, которую называет.

### Г. Две календарные оси на одной странице отчёта (#131)

Скептик волны `report-full-eval` (агент `a58d593b9d89`) проверял обвинение в
том, что блок «Что изменилось за неделю» и тело отчёта живут по разным
календарям. Он довёл проверку до арифметики и умер на лимите модели, ничего
не записав.

Что он установил:

- веб выбирает пару снимков по номеру **UTC-суток** из `createdAt`
  (`packages/web/src/lib/report/week-delta.ts:9,32` — `dayNumberOf`);
- сервер для отчёта прошлой недели берёт границу **продуктового дня** с
  сдвигом +12 (`getSnapshotAtOrBefore` → `productDayEndIso`,
  `packages/server/src/services/snapshot.ts:924-933`);
- снимок, созданный 2026-07-23T22:00Z, для веба — четверг 23-е (день 20 657),
  для сервера — уже пятница 24-е, и в срез четверга он не попадает;
- 2026-07-23 действительно четверг: 20 657 делится на 7 нацело — арифметику
  он проверил вручную, а не «навскидку»;
- главное: камчатский рабочий день ложится на UTC-интервал с 20:00
  предыдущих суток до 06:00 текущих. Обычное утреннее обновление данных
  в Елизове (10:00 местного) даёт снимок с UTC-меткой предыдущего вечера.
  То есть расхождение осей — не редкий угловой случай, а обычный режим.

Следствие, которое он назвал: на отчёте прошлой недели дельты разделов и
значки у плиток считаются от снимка, который тело отчёта считает уже
следующей неделей. Два числа на одном экране отвечают на разные вопросы.

Проверено 22.08.2026: расхождение живо. При этом канонический дом уже
построен — `productCalendarDay` переехал в
`packages/shared/src/time-selection.ts:35`, а карта времени прямо
предупреждала: «иначе клиент и сервер посчитают четверг недели по-разному»
(`specs/2026-08-07-time-axis-map.md:150`). Переезд сделан, потребитель
`week-delta.ts` не переключён. Починка — одна строка импорта плюс сдвиг,
и тест на снимок 22:00Z.

### А. Отрицательная экономия: ядро её больше не прячет, отчёт — прячет (#136)

Скептик волны `report-full-eval` (транскрипт
`…/3d9967c0-…/subagents/workflows/wf_2b4698e7-d58/agent-a4f9d6b85a657cfee.jsonl`)
проверял обвинение: расчёт экономии якобы теряет отрицательные величины. Он
прошёл путь целиком и умер на лимите модели ровно перед тем, как написать
вывод. Его разбор:

- метрики экономии складываются простой суммой без обрезки: `extractValue`
  возвращает сырое `num()`, `accumulate` складывает как есть;
- `approvedEconomy` в его день применял `Math.max(0, …)` — то есть два канала
  одной величины отвечали на разные вопросы;
- разобранный им пример: строка с AD = «да», Z = −50, AA = 10, AB = 0 даёт
  `economy_total = −40`, тогда как `approvedEconomy` возвращал 0;
- счётчик конфликтов ловит только `(утверждена && ecoTotal === 0)` либо
  `(не утверждена && ecoTotal > 0)`; строка с −40 не попадает ни в одну ветвь.
  Здесь он прямо опроверг часть исходного обвинения: рекомендация «конфликты
  это поймают» неверна.

Что стало потом. Обрезка снята с обоих каналов, и в коде остался разбор
причины — `packages/core/src/pipeline/calc-engine.ts:380-389`: «перерасход над
лимитом (замер: −150,00327 тыс. руб. по МБ) превращался в ноль и порождал
ложный антикоррупционный флаг… прятать её значит скрывать ровно то, ради чего
продукт и строится». То же и у `economyTotalMath` (`calc-engine.ts:760-765`,
со ссылкой на реестр 08.08 §2).

Что уцелело и осталось дефектом. Строка экономии в блоке отчёта по-прежнему
живёт под условием «больше нуля»:

`packages/web/src/lib/report/mappers.ts:664`
`economyLine: block.economy.total > 0 ? 'Утверждённая экономия: …' : null`

Получается расхождение внутри одного продукта: ядро сознательно перестало
прятать перерасход, а экран блока ГРБС его прячет — строки просто нет,
как будто экономии не считали вовсе. При этом отрицательное число продолжает
уменьшать сводный итог, который читатель видит на плитке. Это ровно тот класс
дефекта, который владелец называет «показывает правдоподобное и молчит».

Проверено 22.08: в атласе карточек эта карточка помечена «СООТВЕТСТВУЕТ»
(`audits/2026-08-20-cards-map/otchet-svod.md:98`) — то есть карта дефекта не
видит, и починка требует одновременной правки карты.

