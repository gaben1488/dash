# Реестр найденных багов — глубокая охота 08.08

29 подтверждённых находок (адверсариальная проверка каждой: скептик
пытался опровергнуть по коду). Полный текст каждой (механизм, доказательство,
severity после проверки) — в журнале воркфлоу `wf_7bac7647-3ee`, ключевой файл
`w3z49n8jn.output`. Здесь — сводка для планирования починки.

## Критический (1)

1. **Пороги 44-ФЗ в рублях, данные книг — в тысячах.** Весь слой контроля
   44-ФЗ и антикоррупции молчит: 94 реальных нарушения лимита ЕП (>600 тыс.)
   на живых данных показываются как ноль. `compliance-44fz.ts:95,170`,
   `anticorruption.ts:100,110,114,179`, `splitting.ts:30,50,94`,
   `signals.ts:149,430`. Верный двойник порога уже есть:
   `method-families.ts:95 thresholdKopUnit: 600`.

## Высокий (8)

2. Сезонный прогноз завышен кратно — масштабируется по числу ненулевых
   месяцев вместо истёкших. `forecast.ts:94-97`.
3. (дубль-подтверждение #1 независимым разведчиком, те же файлы)
4. Фильтр по подведам отменяет выбранный период — годовые числа под
   заголовком квартала. `totals-aggregation.ts:98-110`.
5. «Сбросить период» ставит фильтр текущего месяца и делает его невидимым
   для чипов/URL. `store.ts:574,587,611,635,653,663`.
6. Смена года во время загрузки молча теряется (булев страж без очереди).
   `store.ts:723-724`.
7. Дешёвые закупки (план ≤100 тыс.) классифицируются как строка-шапка и
   выпадают из всех правил контроля — треть реестра без единого замечания.
   `classify.ts:44,72`, `validate.ts:70`.
8. Со второго снимка `saveSnapshot` падает на UNIQUE `issues.id`: атомы не
   пишутся, ретеншен не запускается никогда. `snapshot.ts:505-510`.
9. Оверрайды маппинга сохраняются и подтверждаются, но пайплайн их не
   читает — числа продолжают идти со старой ячейки. `mapping.ts:96-131`,
   `snapshot.ts:219`.

## Средний (16)

10. Экономия за выбранный месяц считается за весь квартал —
    `economy-metrics.ts:48-55`.
11. quickRefresh не обновляет `dataYear` — баннер врёт после автообновления.
12. «Первичка сейчас» устаревает после автообновления (нет вызова
    `setOfficialProvenance` в quickRefresh).
13. Колесо недель меняет год и перезагружает данные в режиме явного выбора.
14. «Δ кв.» в рейтинге на годовом виде сравнивает пустой IV квартал с III.
15. Ссылка с `?months=` восстанавливает данные, но не барабан.
16. Отрицание в комментарии читается как подписанный контракт (сигналы).
17. Доля просроченных считается по двум разным множествам строк.
18. Ответ `/api/dashboard` подписывает годом сумму за все годы; невалидный
    год молча принимается.
19. Журнал фильтрует и считает итог по обрезанному окну (LIMIT до фильтра).
20. Смена источника через `PUT /api/sources` не сбрасывает кэши.
21. Таблица `changelog_entries` объявлена, но не создаётся при старте —
    журнал правок не переживает рестарт.
22. Диаграмма рассеяния молча обрезается на 2500 точках, total из обрезка.
23. Комментарий к замечанию не читается обратно в карточку/список/CSV.
24. Первопричины расхождения экономии считаются мерой «факт» — доказательство
    ведёт не в те ячейки (колонка Y вместо Z–AC). `reconciliation.ts:126-135`.

## Низкий (4)

25. Способ закупки сравнивается сырой строкой вместо канона
    (алиасы ЕП не встречаются на живых данных — латентно).
26. Два таймера автообновления при старте в фоновой вкладке.
27. Дата с двузначным годом читается по-американски (латентно — в данных
    только serial-числа).
28. (учтён в общем счёте — see доля просроченных, дубль формулировки)

## Порядок починки

**Волна А (core/server, не пересекается с web-агентами):** #1/#3 (единицы
44-ФЗ), #2 (прогноз), #7 (классификация шапки), #8 (UNIQUE issues.id),
#9 (оверрайды маппинга), #21 (changelog_entries), #24 (мера экономии в
сверке).

**Волна Б (web/store, после освобождения store.ts/totals-aggregation.ts):**
#4, #5, #6, #10, #11, #12, #13, #14, #15.

**Волна В (сервер, независимая):** #18, #19, #20, #22, #23.
