# Карта вкладки «Пульт» — картограф, 20.08.2026

> Читающий аудит. Код не менялся. Параллельно идёт волна правок — пункты,
> которые она может закрыть, помечены «в движении». Канон — реестр
> `docs/superpowers/audits/2026-08-14-interview-register.md` (пп.53, 58, 104,
> 115, 119, 127, 129 и профильные).

## §0. Состав вкладки (что читалось)

| Слой | Файлы |
|---|---|
| Страница | `packages/web/src/pages/Dashboard.tsx` (1318 строк) |
| Компоненты | `HeroKPICard.tsx` (плитка+DeptBreakdown+BudgetBreakdown+TrustComponents), `CriticalBannerV2.tsx`, `DiagnosticCards.tsx`, `RatingTableV2.tsx`, `charts/DrillPieChart.tsx`, `FilterBreadcrumb.tsx`, `PeriodBadge.tsx`, `PlanSemanticsNote.tsx`, `FigureView.tsx`, `Skeleton.tsx`, `ui/kb-tooltip.tsx` |
| Данные | `hooks/useFilteredData.ts` (+ `lib/selectors/*`), `hooks/useMultiDimMetrics.ts`, `lib/perimeter.ts`, `lib/figure.ts`, `lib/provenance-registry.ts`, `lib/economy-metrics.ts`, `lib/signal-counts.ts`, `pages/kb-additions.ts` |
| Сервер | `packages/server/src/routes/dashboard.ts` — `GET /api/dashboard` (снимок → kpiCards, departmentSummaries c quarters/months, summaryByPeriod, recentIssues, signalCounts, issueSummary, trust, lastRefreshed, year), `POST /api/refresh`, `GET /api/trust/:deptId` |
| Ядро | `@aemr/core computeTrustScore`, calc-engine (`grbs.<id>.<период>.*` в `calculatedMetrics`), сигналы конвейера (`snapshot.issues`) |

Порядок экрана: 1 паспорт данных → 2 крошка фильтров → 3 полоса замечаний →
4 плашка расхождения года → 5 ряд из четырёх плиток → 6 рейтинг управлений →
7 план/факт по кварталам → 8 исполнение по управлениям → 9 круг долей →
10 сигналы проверок. Итого **10 блоков, ~46 самостоятельных элементов-цифр**.
«Живой ленты» как отдельного компонента на Пульте нет — её роль несут пульс
«Эфир» в паспорте и `LiveUpdateBar` уровня приложения (вне этой страницы).

---

## §1. Паспорт данных (StatusLine)

`Dashboard.tsx:99-160`.

| Элемент | Откуда | Смысл | Периметр/момент | Канон | Состояние | Усиление |
|---|---|---|---|---|---|---|
| Пульс «Эфир» | статический, title на `:127` | числа считаются по текущему состоянию книг, не по срезу | момент «на сейчас» — объявлен | п.58 (момент) | СООТВЕТСТВУЕТ | клик → журнал чтений источников (когда какая книга прочитана) |
| «Период: …» | `periodDataLabel({dataYear, period, activeMonths})`, подписка на store `:109-110` | за какой период посчитана страница | год ДАННЫХ (`fd.dataYear`), эффективный период | п.58(а) | СООТВЕТСТВУЕТ | — |
| «Источник: N книг» | `fd.allDepts.length ?? fd.depts.length` `:117` | сколько книг ГРБС прочитано | нарочно НЕфильтрованный счёт | п.58 | СЛАБОСТЬ: при фильтре управления читается «8 книг», хотя числа страницы — по одной; двусмысленно | добавить «из них в расчёте: M» при активном фильтре |
| «Прочитано: дата» | `dashboardData.lastRefreshed` = `snapshot.createdAt` (`routes/dashboard.ts:419`) | момент чтения источника | — | п.58 (момент) | СООТВЕТСТВУЕТ (пустота объяснена словами `:148`) | кликом — разложить по книгам (`getDeptLoadMeta`) |
| «K ячеек» | `snapshot.metadata.cellsRead ?? rowCount` `:116` | объём чтения снимка | — | — | **НЕСООТВЕТСТВИЕ (Н4)**: фолбэк на `rowCount` печатается со словом «ячейка» — при отсутствии `cellsRead` строки выдаются за ячейки | при фолбэке склонять «строка», либо не показывать |

## §2. Крошка активных фильтров (FilterBreadcrumb)

`FilterBreadcrumb.tsx`. Чипы: управления (`selectAllDepartments` на снятие),
подведы, месяцы (`hasExplicitPeriodFilter` — недельные месяцы-фантомы не
показываются, баг #5 закрыт), способ КП/ЕП, вид ТД/ПМ (легаси
`current_program`→«ТД» схлопнут по п.30), бюджеты; «Сбросить» → `resetAllFilters`.

- СООТВЕТСТВУЕТ п.58/№Д10: всё, что режет числа, видно и снимается.
- СЛАБОСТЬ: чип **периода-квартала** (когда выбран «3 кв» без месяцев) в крошке
  не рисуется — квартал живёт только в шапке и в плашках карточек. Для полной
  симметрии «всё, что режет — в крошке» стоит добавить чип квартала.
- СЛАБОСТЬ: чип бюджета печатает сырое `b.toUpperCase()` — работает, пока ключи
  «фб/кб/мб», но словарный путь (`BUDGET_SOURCE_META.abbr`) честнее.

## §3. Полоса замечаний (CriticalBannerV2 + DiagnosticCards)

Данные: `fd.criticalIssues/warningIssues` ← `filterIssues` (оси: управление,
подвед, поиск, вид деятельности; **период и способ НЕ применяются**) ←
`snapshot.issues` (все сигналы конвейера). Сборка `bannerIssues`
`Dashboard.tsx:270-288` передаёт полный состав карточки диагноста.

| Элемент | Откуда | Смысл | Периметр | Канон | Состояние | Усиление |
|---|---|---|---|---|---|---|
| «N замечаний требуют решения» | `criticalCount = fd.criticalIssues.length` | критические сигналы по строкам книг | управления — фильтруются; период — нет | п.53, 127 | СООТВЕТСТВУЕТ (склонение, счётчик, aria) | — |
| «и ещё M предупреждений» | `fd.warningIssues.length` | warning+significant | то же | п.53 | СООТВЕТСТВУЕТ | — |
| Подпись периметра панели `:180-182` | статическая строка «Проверки всех строк книг · …» | замечания не подчиняются периоду | — | п.58(а) | **НЕСООТВЕТСТВИЕ (Н3)**: текст жёсткий; при выбранном управлении числа сужены до него, а подпись продолжает утверждать «всех строк книг» — ось органов не объявлена данными | собирать подпись через `buildPerimeter({ignoresPeriodFilter:true, departments})` — единый шаблон п.58(д) |
| Карточки диагноста | `DiagnosticCardList` ← `groupIssuesByMechanism` | механизм + kbHint + «Что делать» + адреса (лимит 30, «…и ещё N») | — | п.53 | СООТВЕТСТВУЕТ (三 части карточки есть; свёртка адресов честная) | адрес строки сделать ссылкой в Реестр/книгу (двойной адрес «строка · № п/п» уже канон) |
| «Все замечания на странице „Контроль“» | `onNavigate → navigateTo('quality',{qualityTab:'issues'})` | явный переход | — | закон «уход — отдельной кнопкой» | СООТВЕТСТВУЕТ | передавать выбранное управление в параметры перехода |

## §4. Плашка расхождения года

`Dashboard.tsx:452-457`. `fd.yearMismatch` ← `shouldShowYearMismatch(year, dataYear, loading)`.
Текст называет оба года и причину. СООТВЕТСТВУЕТ п.58(б) — образец честного
неподчинения. Усиление: кнопка «вернуть год {dataYear}» прямо в плашке.

## §5. Ряд ключевых показателей (4 × HeroKPICard)

Сборка `buildHeroKPIs` `Dashboard.tsx:759-923`. Общий периметр плиток —
`buildPerimeter` `:187-197`: год данных, эффективный `periodKey`, месяцы
(в week-режиме — пусто), управления, подведы, момент live. Печать чисел —
контракт `Figure`/`formatFigure`, пустой знаменатель = причина, не ноль.

### 5.1 «Исполнение» (двойная: количество + сумма)

| Элемент | Откуда | Смысл | Канон | Состояние | Усиление |
|---|---|---|---|---|---|
| Главный % (кол-во) | `ratio(fd.totalFactCount / fd.totalPlanCount)` ← `aggregateTotals` (оси: период, месяцы, способ) | заключено ÷ план по позициям | п.58, 104 | СООТВЕТСТВУЕТ; провенанс `tileProvenance('exec_count_pct')` → строки СВОДа | — |
| Дополнительный % (сумма) | `ratio(fd.totalFact / fd.totalPlan)` — учитывает и бюджет/вид деятельности | факт ÷ план по деньгам | п.58 | СООТВЕТСТВУЕТ | — |
| Кривые по кварталам `:803-818` | ручной обход `fd.depts → d.quarters[qk].planCount/factCount` (сырые кварталы) | динамика 1–4 кв | п.58(а) | **НЕСООТВЕТСТВИЕ (Н7)**: спарклайн не знает осей способа/бюджета/вида/месяцев — при фильтре «только ЕП» главное число сужено, кривая — нет; две правды на одной плитке | считать кривую тем же селектором с `showKP/showEP` (или честная пометка «кривая — по всем способам») |
| Состояние (цвет/точка) | min из двух известных %, пороги 50/80 `:822-826` | худшее из измерений | — | СООТВЕТСТВУЕТ (отсутствующее измерение не считается провалом) | пороги брать из metrics-registry, не литералами |
| Разворот: DeptBreakdown | `KPIExpandPanel` `:974-984`: `d.quarters[periodKey]` | вклад управлений | п.58 | СЛАБОСТЬ (родня Н7): разворот читает квартальный блок — фильтр месяцев/бюджета/способа не сужает разбивку, у месяца расходится с плиткой | пропускать разбивку через те же селекторы (`aggregateNodeTotals`) |
| Разворот: BudgetBreakdown | суммы `planFB/KB/MB` по `quarters[periodKey]` | план по уровням бюджета | — | СООТВЕТСТВУЕТ (пустота объяснена) | показать и факт по уровням, не только план |
| «Разобрать исполнение в Аналитике» | `NAV_TARGETS` `:1033-1039` | явный переход с целью в подписи | — | СООТВЕТСТВУЕТ | передавать periodKey в параметры |

### 5.2 «Критические замечания»

| Элемент | Откуда | Смысл | Канон | Состояние | Усиление |
|---|---|---|---|---|---|
| Число | `fd.criticalIssues.length`, `figure({unit:'шт'})` `:850-865` | критические сигналы | п.53 | счёт верный (управления фильтруются) | — |
| Периметр-подпись | общий `perimeter` (с периодом!) | — | п.58(б) | **НЕСООТВЕТСТВИЕ (Н2)**: замечания периоду не подчиняются, а фигура носит подпись «2026 · 3 кв · …» — ровно «унаследованный бейдж», запрещённый каноном; полоса замечаний рядом честно говорит обратное | собрать периметр этой плитки с `ignoresPeriodFilter:true` — авто-пометка «выбран 3 кв, но числа за весь год» уже реализована в `buildPerimeter` |
| Разворот по управлениям | `d.criticalIssueCount ?? null` | null = «не посчитано», не ноль | п.53 | СООТВЕТСТВУЕТ | клик по управлению — сразу в Контроль с фильтром, а не только фильтр страницы |
| Порог состояния | >3 critical → «критично» `:863` | — | — | СЛАБОСТЬ: порог 3 — магическое число без родословной | завести в metrics-registry с обоснованием |

### 5.3 «Экономия»

| Элемент | Откуда | Смысл | Канон | Состояние | Усиление |
|---|---|---|---|---|---|
| % экономии | `getFilteredEconomyTotal(fd) ÷ fd.totalPlan` `:870-889` | признанная экономия (флаг «да, является экономией») к плану | п.102/126 | СООТВЕТСТВУЕТ; разворот считает тем же правилом (`q.economyTotal`) — прежний дубль формулы убит (`:990-997`) | показать вторym числом абсолют в деньгах — % без суммы начальству мало |
| Порог >25 % → warning | `:888` | «слишком хорошая» экономия — сигнал качества данных | — | СЛАБОСТЬ: порог локальный, без ссылки на highEconomy-порог реестра проверок | один дом порога с сигналом `highEconomy` |
| Отсутствие сноски семантики | — | план в знаменателе — три рода K (НМЦК/правленный/лимит) | п.102 | СЛАБОСТЬ: `PlanSemanticsNote` стоит у графика план/факт, но не у плитки экономии, чей знаменатель — тот же смешанный план | добавить компактную `PlanSemanticsNote compact` в разворот плитки |

### 5.4 «Доверие к данным»

| Элемент | Откуда | Смысл | Канон | Состояние | Усиление |
|---|---|---|---|---|---|
| Вердикт «Норма/Требует проверки» | `fd.trust.overall` = `dashboardData.trust` — **глобальный** балл снимка (`useFilteredData.ts:93` — не фильтруется) | двоичный итог, порог 75 | п.58, 127 | **НЕСООТВЕТСТВИЕ (Н1)**: при выбранном управлении плитка объявляет периметр «УКСиМП…», а балл остаётся общесистемным — число не подчиняется собственной подписи; при этом per-dept балл УЖЕ считается сервером (`departmentSummaries[].trustScore`, `routes/dashboard.ts:226`) | при фильтре одного управления брать его `trustScore/trustComponents`; при нескольких — честная пометка «балл общий, фильтру не подчиняется» |
| Пустота | `emptyReason:'Оценка ещё не рассчитана'` | не посчитано ≠ провалено | — | СООТВЕТСТВУЕТ (порядок ветвей в HeroKPICard чинён) | — |
| Слагаемые (5 полос) | `trust.dataQuality…operationalRisk`, веса 30/25/20/15/10 | из чего сложен балл | — | СООТВЕТСТВУЕТ (KBTooltip на каждом) | — |

## §6. Рейтинг управлений (RatingTableV2)

Данные `ratingDepts` `Dashboard.tsx:291-363` ← `useMultiDimMetrics`
(тот же `useFilteredData` внутри — одна правда, общий кэш расчёта).

| Элемент | Откуда | Смысл | Канон | Состояние | Усиление |
|---|---|---|---|---|---|
| «N управлений в расчёте» + PeriodBadge | `ratingDepts.length`; плашка периода с недельной оговоркой | — | п.58 | СООТВЕТСТВУЕТ | — |
| Заголовок-утверждение «M отстают…» | `counted = execCountPct != null`, порог <50 `:505-531` | вывод только из управлений С планом | скрин-разбор №4 («вывод из отсутствия данных») | СООТВЕТСТВУЕТ — прочерки в «отстающие» не попадают | назвать отстающих по именам в подсказке |
| Место + знаки тройки | ранг в текущей сортировке; медали только при merit-сортировке по убыванию | — | — | СООТВЕТСТВУЕТ | — |
| Кол-во % / Сумма % | `dm.total.execCountPct/executionPct` при ненулевом плане квартала `:335-350` | исполнение за выбранный период | — | СООТВЕТСТВУЕТ (null=«нечего исполнять», сортировка кладёт null вниз в обе стороны) | — |
| ФБ % | `quarterPct(q.factFB, q.planFB)` `:307-310` | исполнение федерального источника | — | СООТВЕТСТВУЕТ (ключ порогов `dept_fb_pct` чинён) | добавить КБ/МБ по клику на заголовок (переключатель источника) |
| Динамика (кривая) | `dm.quarters → null при плане 0` | 4 квартала | — | СООТВЕТСТВУЕТ (расшифровка в title/aria) | — |
| Доверие | `d.trustScore` (per-dept, сервер) | балл из 100 | скрин-разбор №4: «доверие 91 рядом с 547 замечаниями» | СЛАБОСТЬ: противоречие метрик глазам читателя не объяснено — нет подсказки, почему замечания не топят балл | в подсказку балла добавить состав («замечания входят с весом …») |
| Замечания (+ кружок критических) | `d.issueCount/criticalIssueCount` | все сигналы книги | п.53 | СООТВЕТСТВУЕТ | клик по числу → Контроль с фильтром управления (сейчас клетка не кликается) |
| Изм. за кв. | `dm.delta.execPctChange`, только когда план был в ОБОИХ кварталах `:339-343` | п.п. к прошлому кварталу | скрин-разбор №4 («Δ КВ. неинтерпретируема») | СООТВЕТСТВУЕТ (фантомные −81 п.п. убиты) | в подсказке называть сравниваемые кварталы («3 кв против 2 кв») |
| Раскрытие строки | аппарат (`ORG_ITSELF_LABEL`) отдельно от подведов; `issueCount: null` у подведов — честно «не разложено» | — | орг-канон | СООТВЕТСТВУЕТ | у подведов показывать план/факт деньгами, не только % |
| Клик по строке | `toggleDepartment` (глобальный фильтр) + раскрытие | — | — | СЛАБОСТЬ: один клик делает ДВА действия — фильтрует всю страницу и раскрывает; фильтрация как побочный эффект раскрытия неочевидна | развести: раскрытие — клик, фильтр — явная кнопка в раскрытии (как у круга «Применить как глобальный фильтр») |
| «Открыть реестр строк» | `navigateTo('data',{department})` | явный уход | — | СООТВЕТСТВУЕТ | — |

## §7. План против факта по кварталам

`Dashboard.tsx:546-641`. Данные `planFactData` ← `fd.summaryByPeriod`
(пересчитывается селектором при фильтрах; ключ квартала едет с точкой данных).

| Элемент | Откуда | Смысл | Канон | Состояние | Усиление |
|---|---|---|---|---|---|
| Заголовок-утверждение | `planFactAssertion` — худший квартал по разрыву, из тех же чисел | — | — | СООТВЕТСТВУЕТ | — |
| PeriodBadge + PlanSemanticsNote | store + `summarizePlanSemantics` | «за какое время» + «из чего сложен план» | п.58, 102 | СООТВЕТСТВУЕТ — образцовая пара | — |
| Столбцы плана (стек КП+ЕП / единый) | `q.kpPlan/epPlan` из `summaryByPeriod`; `hide=!showStacked` (фикс фрагмента recharts) | план по способам | п.115 (индиговый столбец — подтверждённое исключение) | СООТВЕТСТВУЕТ | — |
| Клик по столбцу КП | `goToQuarter(quarterKey)` — БЕЗ способа `:597` | провал в Аналитику | — | СЛАБОСТЬ (асимметрия): столбец ЕП передаёт `procurement:'single'` (`:602`), столбец КП способ не передаёт — провалы неравноценны | передавать `'competitive'` |
| Линия «Факт» | `kpFact+epFact` | — | — | СООТВЕТСТВУЕТ | точки линии кликом не проваливаются — дать им тот же goToQuarter |
| Кнопки «Разобрать квартал…» | `:616-629`, только кварталы с данными | клавиатурный дубль клика по столбцу | — | СООТВЕТСТВУЕТ | — |
| Пустое состояние | причина + следующий шаг `:632-640` | — | — | СООТВЕТСТВУЕТ | — |
| Родословная графика | — | откуда кварталы: q1/год — СВОД официально, q2–q4 — движок по книгам (`routes/dashboard.ts:275-372`, `source:'official'/'calculated'`) | п.104 | СЛАБОСТЬ: поле `source` сервер отдаёт, интерфейс его НЕ показывает — читатель не видит, что 1 кв официальный, а 3 кв расчётный | значок происхождения на столбце/в тултипе («СВОД» / «движок») |

## §8. Исполнение по управлениям (столбцы + линия)

`Dashboard.tsx:645-709`. Данные `fd.barData` ← `buildBarData` (все оси
фильтров, месячная ветвь, подвед-ветвь).

| Элемент | Откуда | Смысл | Канон | Состояние | Усиление |
|---|---|---|---|---|---|
| Заголовок-утверждение | `execAssertion`: разрыв лучший/худший, только с планом; особые фразы для 0 и 1 управления | — | — | СООТВЕТСТВУЕТ (`useMemo` без `formatMoney` в зависимостях — безвредно, но неаккуратно) | — |
| Столбец «По сумме» | `d.pct`, цвет `getExecutionBarColor` (пороги 50/80/100, band-модель) | — | — | СООТВЕТСТВУЕТ; легенда `:702-708` совпадает с порогами | — |
| Линия «По количеству» | `d.execCountPct` | второй разрез | — | СООТВЕТСТВУЕТ | — |
| Тултип | явные цвета текста (жалоба п.24-25 закрыта); «плана за период нет» вместо нуля | — | скрин-разбор №8 | СООТВЕТСТВУЕТ | — |
| Клик по столбцу | `toggleDepartment` | фильтр страницы | — | СООТВЕТСТВУЕТ (клавиатурный путь объявлен в рейтинге) | — |
| Периметр | PeriodBadge есть; оси способа/бюджета в подписи нет | п.58(а) | СЛАБОСТЬ (общая Н8): при фильтре «только ЕП» числа сужены, а собственная подпись графика молчит — спасает лишь крошка сверху | микроподпись периметра (`PerimeterCaption`) и сюда |

## §9. Круг долей (DrillPieChart)

`charts/DrillPieChart.tsx`. Девять правил в шапке файла; сверка «срезы против
итога страницы» с честной подписью расхождения; штриховка как второй признак;
недельная оговорка — образец п.58(б).

| Элемент | Откуда | Смысл | Канон | Состояние | Усиление |
|---|---|---|---|---|---|
| Измерения (Способ/Бюджеты/Управления/Исполнение) | локальный state | — | — | СООТВЕТСТВУЕТ | — |
| Срез «Способ»: «Конкурсные (КП)» `:386` | `fd.totalKP/totalEP` | штучный состав | словарь продукта | **НЕСООТВЕТСТВИЕ (Н5)**: везде на странице и в СВОДе — «конкурентные» (процедуры), здесь — «конкурсные»; это разные вещи в 44-ФЗ (конкурс — один из конкурентных способов) | «Конкурентные (КП)» из словаря @aemr/shared |
| Центр круга «Исполнение» | `Σфакт/Σплан`, не среднее долей | — | правило 8 | СООТВЕТСТВУЕТ | — |
| Сверка с итогом `gap` | `mdm.totals` тем же ядром | «срезы дают X, итог страницы Y» | правило 9 | СООТВЕТСТВУЕТ | — |
| Скрытые срезы | отрицательные названы вслух | — | — | СООТВЕТСТВУЕТ | — |
| «Применить как глобальный фильтр →» | `onDeptToggle` | явное действие | — | СООТВЕТСТВУЕТ — образец для рейтинга (§6) | — |
| Состояние раскрытия | локальное, сбрасывается при уходе со страницы | — | — | СЛАБОСТЬ: стек разрезов/измерение не переживают навигацию | поднять в store/URL |
| `onNavigate` проп | принят от Dashboard, не используется | — | — | ЛИШНЕЕ (см. §12) | убрать из вызова и из пропсов |

## §10. Сигналы проверок (BlindSpotsWidget)

`Dashboard.tsx:1125-1317`. 10 карточек (tdWithProgram удалена каноном п.30).
Счётчики: `fd.signalCounts` ← `aggregateSignalCounts(depts, серверный полный)`.

| Элемент | Откуда | Смысл | Канон | Состояние | Усиление |
|---|---|---|---|---|---|
| Подпись периметра `:1257-1259` | статическая: «Проверки всех строк книг · … период не действует» | неподчинение периоду — объявлено | п.58(б) | частично СООТВЕТСТВУЕТ; но как и Н3 — ось органов не названа: при фильтре управления счётчики сужены (суммирование по `depts`), а подпись твердит «всех строк книг» | та же правка, что Н3: подпись из `buildPerimeter` |
| Фолбэк счётчиков | `aggregateSignalCounts`: нет per-dept → полный серверный счёт | защита от старого снимка | п.127 | **НЕСООТВЕТСТВИЕ (Н6)**: на старом снимке при выбранном управлении карточки показывают ЧУЖИЕ срабатывания без единой пометки — прямое нарушение изоляции | при фолбэке под фильтром — не подставлять полный счёт, а писать «по управлениям не разложено» |
| Слияние счётчиков `:1130-1137` | `{...из issues, ...apiSignalCounts}` — сервер перекрывает локальный счёт | — | — | СЛАБОСТЬ: два источника одного числа; расхождение молча гасится порядком спреда | оставить один источник (fd.signalCounts) |
| Заголовок-утверждение | самый частый сигнал из тех же чисел | — | — | СООТВЕТСТВУЕТ | — |
| 10 карточек | подписи из словаря (`signalLabel`, латиница гасится честной заглушкой); 4 сигнала — KB из `kb-additions.ts` (долг словаря объявлен) | счётчик + переход в Контроль с поиском | п.53, 91 | СООТВЕТСТВУЕТ; неактивная карточка не выключается (aria-disabled, причина в title) | п.119: карточке нужен ОТВЕТ на месте — раскрытие топ-адресов (лист · строка · предмет · почему) без ухода в Контроль, как у полосы замечаний |
| Переход | `navigateTo('quality',{category:signal, search})` | поиск по русской подстроке `:1149-1190` | — | СЛАБОСТЬ: связь карточка→список держится на текстовом поиске («просрочк») — переименование текстов сигналов молча рвёт провал | фильтровать по `signal`-ключу, поиск оставить дублем |

---

## §11. Фильтры и сортировки вкладки — сводно

**Применяется** (через `useFilteredData`): управления, подведы, период
(квартал), месяцы (только явные — недельные фантомы отсечены), способ КП/ЕП,
вид ТД/ПМ, бюджеты, поиск (по имени управления), год.

**Игнорируется — и как об этом сказано:**

| Ось | Кто игнорирует | Объявлено? |
|---|---|---|
| Период | замечания (полоса, плитка §5.2), сигналы §10 | полоса и §10 — да, словами; плитка §5.2 — НЕТ (Н2) |
| Неделя | весь расчёт | да — янтарная оговорка PeriodBadge/круга (образец) |
| Управления | плитка «Доверие» (Н1); фолбэк сигналов (Н6) | НЕТ |
| Способ/бюджет/вид | спарклайны плитки исполнения (Н7), разворот KPIExpandPanel, счётчики процедур при бюджет/вид-фильтре (по контракту «счётчики не пересчитываются») | НЕТ — подпись периметра этих осей не знает (Н8) |
| Подведы (org-scope) | сигналы/замечания не разложены по организациям — показан честный `null` в рейтинге | да |

**Комбинируемость**: оси складываются корректно (бюджет∩вид∩способ∩период —
отдельные ветви пересчёта в селекторах). **Сохранение состояния**: глобальные
фильтры живут в store/URL; но сортировка рейтинга, раскрытая плитка, стек
круга, раскрытые карточки — локальны и теряются при уходе со страницы (P2).
**п.127**: основной путь чист (замечания, счётчики, рейтинг фильтруются по
управлению); дыры — Н1 (доверие) и Н6 (фолбэк сигналов).

## §12. Мёртвое и лишнее

| Что | Где | Вердикт |
|---|---|---|
| `onNavigate` у DrillPieChart | `Dashboard.tsx:717`, `DrillPieChart.tsx:141` («оставлен для совместимости») | мёртвый проп — убрать |
| `secondaryMetricKey` | передаётся плиткой исполнения (`Dashboard.tsx:844`), в `HeroKPICard` объявлен, но не деструктурируется и нигде не читается | мёртвый проп |
| `sourceCell`/`origin`/`displayValue`/`delta` у HeroKPICard | Пульт их не передаёт ни одной плитке (живут для других потребителей? проверить Report) | кандидат на чистку после grep потребителей |
| DEPRECATED-блок topKpis (spark/trend, слепой фолбэк «первые 6 карточек») | `useFilteredData.ts:127-221` | Пульт `topKpis` не читает — груз для FilterContext-переезда; помечен в коде, «в движении» |
| `summaryByPeriod['year']` | сервер собирает, страница берёт только q1–q4 | не мёртвое (другие вкладки), но для Пульта — лишний расчёт |
| Легенды-вруны | «K ячеек» при фолбэке на строки (Н4); «Проверки всех строк книг» под фильтром (Н3); «Конкурсные» (Н5) | см. §1, §3, §9 |
| Дубли смыслов | пороги 50/80 продублированы литералами в плитке (5.1), заголовке рейтинга (§6) и легенде (§8) при живом `metrics-registry` | свести к одному дому порогов |

---

## §13. Итог: приоритизированный список работ

**Счёт: 10 блоков · 8 несоответствий (Н1–Н8) · ~14 слабостей.**

### P0 — несоответствия канону

1. **Н1. Доверие — глобальный балл под фильтрованным периметром** (п.58, п.127).
   `Dashboard.tsx:899-920` + `useFilteredData.ts:93`. При одном выбранном
   управлении брать `departmentSummaries[].trustScore/trustComponents`
   (сервер уже считает, `routes/dashboard.ts:216-227`); при нескольких —
   пометка неподчинения в периметре.
2. **Н2. Плитка «Критические замечания» носит период, которому не подчиняется**
   (п.58(б)). `Dashboard.tsx:850-865` — периметр с `ignoresPeriodFilter:true`
   (`lib/perimeter.ts:239-246` уже умеет авто-пометку).
3. **Н3+§10. Статические подписи «Проверки всех строк книг» врут под фильтром
   управления** (п.58(а,д)). `CriticalBannerV2.tsx:180-182`,
   `Dashboard.tsx:1257-1259` — собирать подпись `buildPerimeter`-ом из
   фактических осей.
4. **Н6. Фолбэк счётчиков сигналов подставляет чужие управления** (п.127).
   `lib/signal-counts.ts:6-14` — под dept-фильтром фолбэк запретить, писать
   причину.
5. **Н7. Спарклайны и разворот плитки «Исполнение» игнорируют оси
   способа/бюджета/вида/месяцев** (п.58(а)) — `Dashboard.tsx:803-818, 974-997`;
   считать теми же селекторами, что главное число.
6. **Н4. «K ячеек» печатает строки под именем ячеек** — `Dashboard.tsx:116,150-157`.
7. **Н5. «Конкурсные (КП)» → «Конкурентные (КП)»** — `DrillPieChart.tsx:386`;
   подпись из словаря продукта.
8. **Н8. Периметр-подпись не знает осей способа/бюджета/вида** — расширить
   `Perimeter` пятой осью «срез» (`lib/perimeter.ts`) и печатать её на плитках
   и графиках §7–§9.

### P1 — усиления

9. Карточкам сигналов (§10) — раскрытие адресов на месте по п.53/119: механизм
   + топ-строки (лист · строка · предмет · почему) + действие; переход по
   `signal`-ключу вместо текстового поиска (`Dashboard.tsx:1149-1190`).
10. Родословная п.104 на графиках: значок «СВОД / движок» у кварталов графика
    план/факт (`source` уже в `summaryByPeriod`, сервер `routes/dashboard.ts:300,370`).
11. Симметрия провала: клик по столбцу КП передавать `'competitive'`
    (`Dashboard.tsx:597`); кнопкам кварталов — выбор способа.
12. Развести клик строки рейтинга: раскрытие ≠ глобальный фильтр
    (`RatingTableV2.tsx:500-503`); фильтр — явной кнопкой, как у круга.
13. Экономия: абсолют деньгами вторым числом + `PlanSemanticsNote compact`
    в развороте (`Dashboard.tsx:867-890`).
14. Клетка «Замечания» рейтинга — провал в Контроль по управлению
    (`RatingTableV2.tsx:620-637`).
15. Подсказка «Изм. за кв.» называет сравниваемые кварталы; подсказка балла
    доверия объясняет соседство с сотнями замечаний (§6).
16. Пороги 50/80/25/3 — из `metrics-registry`, литералы убрать (§5, §6, §8).

### P2 — идеи

17. Сохранение локальных состояний (сортировка рейтинга, стек круга, раскрытая
    плитка) в store/URL — вкладка возвращается в тот же вид.
18. Чип квартала в крошке фильтров (`FilterBreadcrumb.tsx`).
19. Паспорт данных: клик по «Прочитано» — разбор по книгам из
    `getDeptLoadMeta`; «из них в расчёте: M» при фильтре.
20. Мини-динамика сигналов на карточках §10 (сравнение со вчерашним снимком —
    сырьё уже в снимках).
21. Переключатель источника ФБ/КБ/МБ в колонке рейтинга (§6).
22. Чистка мёртвых пропсов: `onNavigate` (DrillPieChart),
    `secondaryMetricKey` (HeroKPICard); судьба DEPRECATED-блока `topKpis` —
    «в движении», ждёт FilterContext.
