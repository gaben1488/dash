# Интервью с заполняющими таблицы — 23 пункта (14.08.2026)

Замечания реальных пользователей (исполнители ГРБС + аналитик), собраны
владельцем. Триаж по кластерам; «→» — куда уходит работа.

## Кластер А — период непрозрачен или неверно применён (доминирующий)

- **5, 6, 11, 12.** УЭР/УО: расчёт КП 7/2, ЕП 6/1 против свода 11/3, 9/1;
  «по ЭА должно было быть 3». Диагноз-гипотеза: выбрана неделя 37-2 +
  месяц май в барабане → расчёт режется срезом/месяцем, свод-колонка
  показывает «на сейчас». Совпадает с известными багами #4 (подведы
  сбрасывают период), #5 (сброс периода ставит невидимый месяц) и
  пунктом «денежные пары сверки перевести на live-проход» (реестр
  расхождений, волна 1 п.10). На скриншоте п.20 сверка «на текущий
  момент» уже сходится 11=11, 3=3 — правильное направление.
  → волна Б багфиксов (#4, #5, #6) + live-проход сверки.
- **14, 15, 16.** Рейтинг: проценты только у 3 ГРБС из 8, у остальных
  прочерки; непонятен период рейтинга и Пульта. Прочерки — следствие
  честного «нет плана» при срезанном периоде. Нужна подпись периода на
  каждом блоке + причина прочерка по наведению.
- **17, 18, 19.** График план/факт по кварталам и пирог способов —
  «неверные данные» (3 кв). Тот же периметр среза; пирог 2823 шт. при
  выбранной неделе 3–9 авг.
- **21.** «Можно ли, чтобы по типу СВОДа не суммировал строки без
  плановой даты?» — ПОДТВЕРЖДЕНИЕ волны 0 (строгий год + корзина),
  уже реализовано, ждёт коммита и выката.

## Кластер Б — замечание без действия (упрёк, а не инструмент)

- **1.** Пульт показывает замечания, но непонятно, как с ними работать.
  → переплавка, экран «Дисциплина» (список действий с эффектом) — бриф
  §2.3, §4; до того — минимум: у каждого замечания «что делать» + ссылка
  на строку.
- **10.** Просят колонку комментария-обоснования (почему сроки сдвинуты,
  почему способ). → переплавка D1: пять пояснительных колонок в
  редакторе (бриф §6.2).

## Кластер В — провенанс и адресация

- **3.** Непонятно, к какой книге относится текст замечания «O237
  (факт) = 7138.1467…». Два дефекта: нет имени листа; голый float с
  15 знаками. → имя листа в каждый текст проверки; округление до
  копеек в текстах.
- **8.** Из списков Отчёта («Факт дата раньше плана: …») — ссылку на
  ячейку для поиска в реестре. → sheetRow/cell в сигнальных списках
  Отчёта (у pendingPositions уже есть — распространить).

## Кластер Г — данные и словарь

- **7.** УКСиМП расшифровывался «капитального строительства» — верно
  «Управление культуры, спорта и молодёжной политики». ИСПРАВЛЕНО
  14.08 (4 словаря).
- **23.** Канон имени класса: «закупки, НЕ ОБЕСПЕЧЕННЫЕ
  финансированием» (было «без подтверждённого финансирования»).
  → переименовать лейблы в словаре продукта и текстах.

## Кластер Д — запросы возможностей

- **2.** В Отчёт: события смены способа определения поставщика по
  каждому ГРБС, изменения плановой даты (источник — журнал правок
  _ChangeLog, колонки L и N); письмо на почту при смене способа
  (Apps Script в таблице или серверная рассылка); остаток в плановых
  деньгах с разбивкой по бюджетам (pending fb/kb/mb уже считается —
  проверить вывод в UI Отчёта).
- **9.** Реестр: показать все строки + фильтры по месяцам/кварталам,
  принцип счёта строк непонятен. Частично закрыто волной 5 (описатель
  ступеней отбора); период реестра — на сервер (ось времени, фаза 8).
- **20.** «Поднять снизу разбивку» — порядок блоков в секции ГРБС;
  плюс «неверная сумма» лимита (13 921 на скриншоте — проверить после
  выката волны 0).
- **22.** Блок «Рекомендации УЭР и ответы» в Отчёте НЕ НУЖЕН (в
  комментарии УЭР пишут номера аукционов). → снести блок.

## Кластер Е — надёжность

- **13.** Отчёт падал «TypeError: Failed to fetch» (сырая английская
  ошибка + недоступность с прошлой недели). → русская ошибка с
  действием уже в волне 4; причину недоступности искать в
  производительности /api/report (полное чтение книг).

## Дополнение (пришло после закрытия реестра)

- **24.** Тултип «Исполнение по управлениям»: факт УКСиМП 137 012,7 —
  неверен (кластер А, периметр). Проверить после выката волны 0.
- **25.** Тултип пирога в тёмной теме нечитаем (тёмный текст на тёмной
  подложке). → контраст тултипов графиков.
- **26.** «Остаток к заключению» — просят разбивку КП/ЕП: «я эти цифры
  каждый раз вручную высчитываю» — прямой исход 2. Движок уже считает
  pendingByMethod kp/ep — вывести в блок остатка.
- **27. РЕШЕНИЕ-КАНОН.** «Ожидается 03.09.2025» выведено из свободного
  текста комментария. Владелец: интерпретировать свободный текст
  исполнителей машинно НЕ НАДО НИГДЕ — «то, что они там пишут, отдельный
  смысл». → снять extractForecastDate и все машинные выводы из колонок
  комментариев (то же лечит баг «не заключен» читается как «подписан»);
  комментарий показывать как есть, без выводов.
- **28.** Номер строки в замечаниях не совпадает с нумерацией в Реестре
  (наш номер = позиция листа со шляпкой, Реестр нумерует выборку) —
  путает исполнителей; унифицировать на номер строки ЛИСТА везде.
  Сигнал «факт дата раньше плана» — не ошибка по мнению аудитории →
  понизить до информационного или снять.
- **29.** УИО — нумерация строк тоже не совпадает («у всех видимо так»):
  подтверждение п.28, дефект системный.
- **30. РЕШЕНИЕ-КАНОН.** Срез «ТД-ПМ» упраздняется: остаются только
  ТД и ПМ. Существующие строки ТД-ПМ полностью входят в ТД — по всем
  фильтрам, вкладкам, всей системе. Сигнал «ТД с заполненной графой
  программы» снимается (заполненные наименования программ у ТД — норма).
  → ACTIVITY_SCOPES, classifyActivity, срезы СВОД, сигнал tdWithProgram.
- **31.** УАГЗО — та же нумерация и «ожидается …» из текста: п.27-28 системны.
- **32.** Антидемпинговый сигнал переименовать: «на него повлиять нельзя,
  указывает лишь на высокую экономию по закупке» → лейбл «Высокая
  экономия по закупке (> 25 %)», из тона претензии — в информационный.
- **33.** Имя УКСиМП на проде всё ещё старое — фикс п.7 локален, ждёт выката.
- **34.** Одна строка собирает три сигнала сразу («план без исполнения» +
  «без финансирования» + «задержка финансирования»). Нужна приоритизация:
  первопричина (нет финансирования) подавляет сигналы-следствия той же
  строки.
- **35.** Блок «Что изменилось с последнего среза» — спустить в самый низ.
- **36.** Свод: «Доля ЕП 88,3 %» неверна по мнению аудитории — счётная
  доля (по количеству) против нормативной по деньгам; подписать единицу
  и/или перейти на денежную (известная пара «оба правы» из реестра
  расхождений §4).
- **37.** УКСиМП: непонятные ~50 тыс. отклонения от свода (и серия ошибок
  «Разница: 50.00 руб.» по пяти ячейкам O в п.3) — в самом листе сумма
  трёх бюджетов расходится с ИТОГО на константу. Разобрать первопричиной
  (класс «парсинг»/дефект листа), показать строку-виновницу адресом.
- **38.** Диаграмма рассеяния: незаключённый контракт (цена 0) показан
  как «экономия 100 %» и попадает в «подозрительные». Гейт экономии
  обязан требовать факт заключения (дата), а не считать цену 0 экономией;
  внизу тултипа — «uagzo» латиницей (внутренний ключ наружу). Заодно
  «2394 подозрительных закупок» — следствие того же ложного гейта.
- **39.** То же у УФБП: кредитная линия 32 млн без плановой даты в
  «подозрительных» с экономией 100 % — строка «не обеспеченная
  финансированием» не должна попадать в scatter вовсе (тот же ложный
  гейт, что п.38).
- **40.** «Подписан» при «нет факта» — живое подтверждение бага #16
  реестра охоты (слово «подписан/заключен» из свободного текста
  комментария читается как статус при пустом факте). Закрывается
  решением п.27: свободный текст не интерпретируется машинно.
- **41.** «Отменён» при состоявшейся закупке (есть факт, экономия) — то
  же чтение слова «отмен» из свободного текста; закрывается п.27.
- **42.** Docx-отчёт: добавить плановые данные за 1 и 2 квартал (выше),
  за 4 — ниже; сейчас только год и 3 квартал.
- **44.** Docx: у строки «Всего на год …» добавить разбивку по бюджетам.
- **45.** Docx: строка «Фактически обязательства заключены…» должна быть
  за ГОД, не за 3 квартал.
- **46.** Docx: добавить исполнение плана 1–2 кварталов с разбивкой по
  ГРБС и суммами сверху; расчётная экономия тянется НЕ та, что в своде
  (сверить источник: строка «Расч. экономия» листа против пересчёта).
- **47.** Docx, блок ЕП: та же структура, что для КП (пп. 44–46) —
  годовая и поквартальная плановая информация с разбивкой по бюджетам,
  фактические данные за год. Попутный дефект на том же скриншоте:
  в docx торчат внутренние ключи кластеров ЕП («п1_монополии»,
  «п5_образование», «п8_имущество» с подчёркиваниями) — заменить на
  русские подписи из словаря кластеров.
