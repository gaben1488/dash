# Интервью с заполняющими таблицы — 23 пункта (14.08.2026)

Замечания реальных пользователей (исполнители ГРБС + аналитик), собраны
владельцем. Триаж по кластерам; «→» — куда уходит работа.

## Кластер А — период непрозрачен или неверно применён (доминирующий)

- **5, 6, 11, 12.** УЭР/УО: расчёт КП 7/2, ЕП 6/1 против свода 11/3, 9/1;
  «по ЭА должно было быть 3». Диагноз-гипотеза: выбрана неделя 37-2 +
  месяц май в барабане → расчёт режется срезом/месяцем, свод-колонка
  показывает «на сейчас». Совпадает с известными багами #4 (подведы
  сбрасывают период), #5 (сброс периода ставит невидимый месяц) и
  пунктом «денежные пары сверки перевести на live-проход» (реестр
  расхождений, волна 1 п.10). На скриншоте п.20 сверка «на текущий
  момент» уже сходится 11=11, 3=3 — правильное направление.
  → волна Б багфиксов (#4, #5, #6) + live-проход сверки.
- **14, 15, 16.** Рейтинг: проценты только у 3 ГРБС из 8, у остальных
  прочерки; непонятен период рейтинга и Пульта. Прочерки — следствие
  честного «нет плана» при срезанном периоде. Нужна подпись периода на
  каждом блоке + причина прочерка по наведению.
- **17, 18, 19.** График план/факт по кварталам и пирог способов —
  «неверные данные» (3 кв). Тот же периметр среза; пирог 2823 шт. при
  выбранной неделе 3–9 авг.
- **21.** «Можно ли, чтобы по типу СВОДа не суммировал строки без
  плановой даты?» — ПОДТВЕРЖДЕНИЕ волны 0 (строгий год + корзина),
  уже реализовано, ждёт коммита и выката.

## Кластер Б — замечание без действия (упрёк, а не инструмент)

- **1.** Пульт показывает замечания, но непонятно, как с ними работать.
  → переплавка, экран «Дисциплина» (список действий с эффектом) — бриф
  §2.3, §4; до того — минимум: у каждого замечания «что делать» + ссылка
  на строку.
- **10.** Просят колонку комментария-обоснования (почему сроки сдвинуты,
  почему способ). → переплавка D1: пять пояснительных колонок в
  редакторе (бриф §6.2).

## Кластер В — провенанс и адресация

- **3.** Непонятно, к какой книге относится текст замечания «O237
  (факт) = 7138.1467…». Два дефекта: нет имени листа; голый float с
  15 знаками. → имя листа в каждый текст проверки; округление до
  копеек в текстах.
- **8.** Из списков Отчёта («Факт дата раньше плана: …») — ссылку на
  ячейку для поиска в реестре. → sheetRow/cell в сигнальных списках
  Отчёта (у pendingPositions уже есть — распространить).

## Кластер Г — данные и словарь

- **7.** УКСиМП расшифровывался «капитального строительства» — верно
  «Управление культуры, спорта и молодёжной политики». ИСПРАВЛЕНО
  14.08 (4 словаря).
- **23.** Канон имени класса: «закупки, НЕ ОБЕСПЕЧЕННЫЕ
  финансированием» (было «без подтверждённого финансирования»).
  → переименовать лейблы в словаре продукта и текстах.

## Кластер Д — запросы возможностей

- **2.** В Отчёт: события смены способа определения поставщика по
  каждому ГРБС, изменения плановой даты (источник — журнал правок
  _ChangeLog, колонки L и N); письмо на почту при смене способа
  (Apps Script в таблице или серверная рассылка); остаток в плановых
  деньгах с разбивкой по бюджетам (pending fb/kb/mb уже считается —
  проверить вывод в UI Отчёта).
- **9.** Реестр: показать все строки + фильтры по месяцам/кварталам,
  принцип счёта строк непонятен. Частично закрыто волной 5 (описатель
  ступеней отбора); период реестра — на сервер (ось времени, фаза 8).
- **20.** «Поднять снизу разбивку» — порядок блоков в секции ГРБС;
  плюс «неверная сумма» лимита (13 921 на скриншоте — проверить после
  выката волны 0).
- **22.** Блок «Рекомендации УЭР и ответы» в Отчёте НЕ НУЖЕН (в
  комментарии УЭР пишут номера аукционов). → снести блок.

## Кластер Е — надёжность

- **13.** Отчёт падал «TypeError: Failed to fetch» (сырая английская
  ошибка + недоступность с прошлой недели). → русская ошибка с
  действием уже в волне 4; причину недоступности искать в
  производительности /api/report (полное чтение книг).

## Дополнение (пришло после закрытия реестра)

- **24.** Тултип «Исполнение по управлениям»: факт УКСиМП 137 012,7 —
  неверен (кластер А, периметр). Проверить после выката волны 0.
- **25.** Тултип пирога в тёмной теме нечитаем (тёмный текст на тёмной
  подложке). → контраст тултипов графиков.
- **26.** «Остаток к заключению» — просят разбивку КП/ЕП: «я эти цифры
  каждый раз вручную высчитываю» — прямой исход 2. Движок уже считает
  pendingByMethod kp/ep — вывести в блок остатка.
- **27. РЕШЕНИЕ-КАНОН.** «Ожидается 03.09.2025» выведено из свободного
  текста комментария. Владелец: интерпретировать свободный текст
  исполнителей машинно НЕ НАДО НИГДЕ — «то, что они там пишут, отдельный
  смысл». → снять extractForecastDate и все машинные выводы из колонок
  комментариев (то же лечит баг «не заключен» читается как «подписан»);
  комментарий показывать как есть, без выводов.
- **28.** Номер строки в замечаниях не совпадает с нумерацией в Реестре
  (наш номер = позиция листа со шляпкой, Реестр нумерует выборку) —
  путает исполнителей; унифицировать на номер строки ЛИСТА везде.
  Сигнал «факт дата раньше плана» — не ошибка по мнению аудитории →
  понизить до информационного или снять.
- **29.** УИО — нумерация строк тоже не совпадает («у всех видимо так»):
  подтверждение п.28, дефект системный.
- **30. РЕШЕНИЕ-КАНОН.** Срез «ТД-ПМ» упраздняется: остаются только
  ТД и ПМ. Существующие строки ТД-ПМ полностью входят в ТД — по всем
  фильтрам, вкладкам, всей системе. Сигнал «ТД с заполненной графой
  программы» снимается (заполненные наименования программ у ТД — норма).
  → ACTIVITY_SCOPES, classifyActivity, срезы СВОД, сигнал tdWithProgram.
- **31.** УАГЗО — та же нумерация и «ожидается …» из текста: п.27-28 системны.
- **32.** Антидемпинговый сигнал переименовать: «на него повлиять нельзя,
  указывает лишь на высокую экономию по закупке» → лейбл «Высокая
  экономия по закупке (> 25 %)», из тона претензии — в информационный.
- **33.** Имя УКСиМП на проде всё ещё старое — фикс п.7 локален, ждёт выката.
- **34.** Одна строка собирает три сигнала сразу («план без исполнения» +
  «без финансирования» + «задержка финансирования»). Нужна приоритизация:
  первопричина (нет финансирования) подавляет сигналы-следствия той же
  строки.
- **35.** Блок «Что изменилось с последнего среза» — спустить в самый низ.
- **36.** Свод: «Доля ЕП 88,3 %» неверна по мнению аудитории — счётная
  доля (по количеству) против нормативной по деньгам; подписать единицу
  и/или перейти на денежную (известная пара «оба правы» из реестра
  расхождений §4).
- **37.** УКСиМП: непонятные ~50 тыс. отклонения от свода (и серия ошибок
  «Разница: 50.00 руб.» по пяти ячейкам O в п.3) — в самом листе сумма
  трёх бюджетов расходится с ИТОГО на константу. Разобрать первопричиной
  (класс «парсинг»/дефект листа), показать строку-виновницу адресом.
- **38.** Диаграмма рассеяния: незаключённый контракт (цена 0) показан
  как «экономия 100 %» и попадает в «подозрительные». Гейт экономии
  обязан требовать факт заключения (дата), а не считать цену 0 экономией;
  внизу тултипа — «uagzo» латиницей (внутренний ключ наружу). Заодно
  «2394 подозрительных закупок» — следствие того же ложного гейта.
- **39.** То же у УФБП: кредитная линия 32 млн без плановой даты в
  «подозрительных» с экономией 100 % — строка «не обеспеченная
  финансированием» не должна попадать в scatter вовсе (тот же ложный
  гейт, что п.38).
- **40.** «Подписан» при «нет факта» — живое подтверждение бага #16
  реестра охоты (слово «подписан/заключен» из свободного текста
  комментария читается как статус при пустом факте). Закрывается
  решением п.27: свободный текст не интерпретируется машинно.
- **41.** «Отменён» при состоявшейся закупке (есть факт, экономия) — то
  же чтение слова «отмен» из свободного текста; закрывается п.27.
- **42.** Docx-отчёт: добавить плановые данные за 1 и 2 квартал (выше),
  за 4 — ниже; сейчас только год и 3 квартал.
- **44.** Docx: у строки «Всего на год …» добавить разбивку по бюджетам.
- **45.** Docx: строка «Фактически обязательства заключены…» должна быть
  за ГОД, не за 3 квартал.
- **46.** Docx: добавить исполнение плана 1–2 кварталов с разбивкой по
  ГРБС и суммами сверху; расчётная экономия тянется НЕ та, что в своде
  (сверить источник: строка «Расч. экономия» листа против пересчёта).
- **47.** Docx, блок ЕП: та же структура, что для КП (пп. 44–46) —
  годовая и поквартальная плановая информация с разбивкой по бюджетам,
  фактические данные за год. Попутный дефект на том же скриншоте:
  в docx торчат внутренние ключи кластеров ЕП («п1_монополии»,
  «п5_образование», «п8_имущество» с подчёркиваниями) — заменить на
  русские подписи из словаря кластеров.
- **48.** Docx: блок «фактическое распределение контрактации по ЕП»
  (проценты по ГРБС с кластерами) — УБРАТЬ, больше не нужен. Вместе с
  ним снимается и вопрос внутренних ключей из п.47 — блок уходит целиком.
- **49.** Docx, блоки каждого ГРБС: план и факт ВСЕХ прошедших кварталов
  (1-й и 2-й; с наступлением 4-го — и 3-й) — количество и сумма с
  разбивкой по бюджетам; плюс список незаконтрактованных процедур
  текущего квартала раздельно по блокам ЭА и ЕП. Основа уже есть:
  pendingPositions (незаключённые позиции с пояснениями листа) и
  pendingByMethod kp/ep считаются движком — довести до docx-выгрузки.
- **50.** Карточка строки обрезала предмет закупки («…текущий ремо…») —
  ИСПРАВЛЕНО 14.08: заголовок верстается в несколько строк без обрезания.
- **Эталон docx.** Актуальный вид ручного отчёта — файл владельца
  «C:\Users\filat\Documents\Отчет по закупкам на 07.08.2026.docx»
  (в git не кладём — правило репо). Пакет правок docx (42, 44–49)
  выполнять, читая этот файл skill'ом docx как эталон структуры.
- **51. РАЗОБРАНО 14.08.** Пульт: у УКСиМП счётчик «23 подведа» неверен —
  должно быть 22 (20 подведов + само управление + СЗ «совместные закупки»).
  Вердикт по живым данным: 21 реальная позиция колонки C (20 учреждений +
  СЗ) + ДВА ВЫДУМАННЫХ демо-подведа из справочника-заглушки
  `subordinate-registry.ts` от 18.04 (МКУ «Культурный центр Елизово»/«КЦЕ»
  и МКУ «Спортивная школа» — в книгах не существуют) = 23.
  **Решение-канон владельца:** «X/x/Х/х» в колонке C = отсутствие подведа =
  закупка самой организации (предикат уже есть — `org-itself.ts`); «КЦЕ» —
  лишний, выдуманный. Справочник организаций системы обязан совпадать со
  справочником из таблиц. Сделано 14.08: реестр пересобран дословно по
  колонке C живых книг (УКСиМП 20+СЗ, УО 43 учреждения + категории
  «Администрирование»/«Опека»/«Совместные закупки», УД/УЭР/УАГиЗО по одному
  МКУ), фейки удалены, `canonicalName` = дословное значение ячейки.
  Открытый вопрос владельцу: «Администрирование»/«Опека»/«ЦДТ» у УО —
  категории строк аппарата или отдельные единицы счёта?
- **52. Сверка УКСиМП «Разница: 50.00» — РАЗОБРАНО 14.08.** Первопричина в
  книге УКСиМП: лист «ВСЕ», строка 206 (№ п/п 203), МБУ ДО СШ «Лидер»,
  «Поверка счётчика УУТ», заключён 17.06.2026, кв. 2. Факт МБ (X206) = 50,0
  тыс. руб., факт-ИТОГО (Y206) = 0 — формула `=SUM(V:X)` затёрта. СВОД
  считает итог по колонке Y (не видит 50), движок восстанавливает из
  бюджетов (видит) → константа +50,00 тыс. во всех ячейках с этой строкой.
  Проверено по всем 670 живым строкам книги — строка единственная.
  Действие владельцу книги: вернуть формулу в Y206. Действие системе:
  подпись единиц («тыс. руб.», не «руб.»), каскад одинаковых разниц
  сворачивать в одну карточку с адресом, страж «ИТОГО ≠ ФБ+КБ+МБ».
- **53. РЕШЕНИЕ-КАНОН (стандарт диагноста).** Слова владельца: система
  будет работать тогда, когда по ЛЮБОЙ возможной ошибке продукт сам, в
  стиле разбора п.52, находит первопричину и говорит, что делать. Никто не
  понимает, как работать с системой, — даже протестировать не могут.
  Стандарт: каждая ошибка/расхождение/сигнал предъявляется карточкой из
  трёх частей — (1) механизм простыми словами, (2) строка/ячейка-виновница
  адресом листа, (3) действие с адресатом (владельцу книги: какая ячейка и
  какая формула; либо системе: что чинится). Ошибка без первопричины и
  действия = дефект продукта. Каскад следствий одной причины — одна
  карточка. Плюс встроенный гид «как проверить систему» (смыкается с п.1 —
  «непонятно, как работать с замечаниями»).
- **54. РЕШЕНИЕ-КАНОН (слой целостности источника).** Слова владельца по
  следам п.52: сигнал должен был сказать «в формульной ячейке отсутствует
  формула» — тогда дефект увидели бы сразу; слой сигналов обязан покрывать
  нарушения типов данных и иные неконсистентности, мешающие в т.ч. работе
  с выгрузками, «от А до Я»; правок дальше — минимум, всегда качественная
  приёмка и харнесс. Факт по коду: инварианты значений есть
  (budget_sum_plan, dept_fact_sum — Y206 ловился, но предъявлялся
  нечитаемо), чтение формул batchGetFormulas есть; НЕТ детектора «значение
  вместо формулы», НЕТ детектора обрезанных диапазонов SUMIFS, тексты не
  диагностские, следствия не подавляются. Полная спецификация слоя и
  порядок внедрения: docs/superpowers/specs/2026-08-14-source-integrity-signals.md.
- **55. РЕШЕНИЕ-КАНОН (глубина внедрения).** Поправка владельца: приоритет
  «минимального внедрения поверх готовой инфраструктуры» ОТВЕРГНУТ.
  «Минимум правок» = минимум последующих исправлений благодаря приёмке и
  харнессу, а не экономия на реализации. По мере роста знания о задачах
  проекта функционал спокойно мигрирует на новый код; переписывание
  модулей, ревизия стека и архитектуры с новыми скиллами — разрешены и
  ожидаются. Слой целостности строится как подсистема «диагност
  источника», поглощающая validate + структурные сигналы (спека §4).
- **56. РЕШЕНИЕ-КАНОН (модель данных, уточнение).** Слова владельца:
  возможно, ЯЧЕЙКА — это атом; СТРОКА — контракт с определённой
  стадийностью, зависящей от содержащихся данных; фильтрация по атрибутам
  (значениям ячеек) фильтрует строки и вычленяет информацию. В данных
  сосуществуют две плоскости: тип значения (текст/число/формула) и то, что
  формула вычисляет, — обе плоскости входят в верификацию, провенанс и БЗ.
  Отдельное требование: уверенность в знании формул СВОДа и СВОДа-по-
  месяцам (какие формулы в каких ячейках, как называются, что считают) и
  отсутствии ошибок на этом уровне. Уровень качества и проработки —
  максимальный. Действие 14.08: снят живой двухплоскостной снимок обоих
  СВОДов (2782 + 6454 формульных ячеек), карта формул с верификацией —
  docs/superpowers/audits/2026-08-14-svod-formula-map.md.
## Разбор скриншотов интервью (14.08, все 41 картинка просмотрены)

Сверх текстового триажа картинки дали следующее:

1. **Сигнальная панель Пульта — главный источник «никто не понимает»**
   (скрины к пп. 1, 3, 51): у УКСиМП «5 замечаний + 650 предупреждений»,
   и каждое предупреждение — «УКСиМП, строка 4: Окорока (куриные)» —
   строка и предмет БЕЗ ПРИЧИНЫ. Категория «Критично» такая же («УИО,
   строка 2237: Шкаф комбинированный» — вдобавок аномальный номер строки
   2237 у маленькой книги УИО — проверить). Сотни строк шума хоронят
   пять настоящих ошибок. → канон п.53: карточка = механизм+адрес+действие;
   плюс приоритизация и свёртка каскадов.
2. **Бейдж периода на карточке ВРЁТ** (пп. 18-19): пирог «Способ закупки»
   носит бейдж «Нед. 3–9 авг», а показывает 2 823 закупки года. Рядом
   карточка «Управления» делает честно: оранжевая пометка «неделя 10–16
   авг выбрана, но числа за 2026 • весь год». Два противоположных паттерна
   на одном экране. → канон п.58.
3. **Один блок ГРБС мешает четыре периметра подряд** (пп. 11, 20): таблица
   «СПОСОБЫ · 3 КВ», строкой ниже «За год: 46 из 72», ещё ниже лимит года,
   ещё ниже «Сверка со СВОД · на текущий момент» — без визуальных границ.
   Заголовок «23,1 % заключено 3 из 13» — периметр заголовка вообще
   не назван. Скрин п.11 показывает шапку: выбрана неделя 37-2 И
   подсвечен месяц Май одновременно — невидимый двойной фильтр (баг #5)
   виден прямо в барабане.
4. **Рейтинг делает вывод из отсутствия данных** (пп. 14-16): «8 управлений
   отстают от графика (<50 %)» при прочерках у 5 из 8; колонка «Δ КВ.»
   (−48…−81 %) неинтерпретируема; «Доверие 91–100» соседствует с «547
   замечаний» у УО — метрики противоречат друг другу на глазах читателя.
5. **Scatter врёт единицами и терминами** (пп. 38-39): ось Y «Цена
   контракта (руб.)» против оси X «Лимит программы (тыс. руб.)» — разные
   единицы на осях одного графика; тултип «Лимит: 7 000 ₽» на деле 7 млн
   руб.; «uagzo» латиницей; в легенде самодельный термин «предрешённость».
6. **«Основание выбора ЕП: X»** — заглушка листа выводится как содержимое
   (скрины пп. 27, 31). Канон org-itself знает «X» для подведов — тот же
   канон нужен всем текстовым колонкам при выводе.
7. **Сильнейшие примеры п.27**: «ожидается 30.08.2027» взято из «лицензия
   до 30.08.2027» (УАГЗО, КАМИН); «ожидается 17.07.2026» взято из «17.07
   будет РАСТОРГНУТ МК» — расторжение прочитано как ожидание заключения.
8. **Тултипы нечитаемы в тёмной теме системно** (п. 25): пирог, карточка
   «Управления» — тёмный текст/подложка на тёмном фоне.
9. **Параллельные системы** (скрины 05-06.08): один и тот же контракт
   исполнители вбивают в книгу ГРБС + «Проект-СМАРТ Про» (Бюджет-NEXT) +
   1С (регистрация обязательств); в Бюджет-NEXT есть реестр бюджетных
   обязательств с учётными номерами БО. Продуктовая цель «единая
   инфраструктура» должна учитывать эти два контура (импорт/сверка по
   номерам БО — кандидат в роадмап).
10. **Docx**: в блоке УЭР «Всего на год: 36» повторяется и в КП, и в ЕП
    блоке — подозрение на дефект источника числа (проверить в волне 4).

- **58. РЕШЕНИЕ-КАНОН (прозрачность периметра).** Слова владельца: «я
  всегда должен точно понимать, что где считается, по любой карточке на
  любой вкладке, за какой период; состояние без выбранных фильтров всегда
  однозначно даёт именно то, что нужно; продуктово понятно и бесшовно;
  сейчас я сам на Пульте ничего не понимаю». Правила: (а) КАЖДАЯ карточка
  объявляет свой периметр собственной подписью (год · период · органы ·
  момент: срез/на сейчас) — данными, а не унаследованным бейджем; (б)
  если карточка не подчиняется глобальному фильтру — явная пометка
  (образец: карточка «Управления»); бейдж, унаследованный от фильтра,
  которому числа не подчиняются, — запрещён (пирог «Нед. 3–9 авг»); (в)
  дефолт без фильтров = «2026 · весь год · все управления · на сейчас» —
  один и тот же на всех вкладках; (г) в одном блоке разные периметры
  разделяются визуально и подписями; (д) шаблон подписи один на всю
  систему. Приёмка: property-тест «у каждого числового блока есть подпись
  периметра, и она соответствует фактическому фильтру данных».
- **59. ДИРЕКТИВА (новая вкладка).** Google-таблица
  15VKFyOPbyP2vJVvmAFVXD0lV14ZwgJ0nxwbhBdjJMps (drive-ссылка владельца
  14.08): построить красивую удобную вкладку с данными оттуда — по образцу
  вкладки СВОД. Сначала прочитать книгу, понять структуру, показать
  владельцу разбор, потом строить.
- **60. ДИРЕКТИВА (анализ данных книг).** Проанализировать снимки листов
  ГРБС с контрактами (атомы всех восьми книг): профиль качества данных по
  каждой книге — типы, инварианты, заглушки, даты, кварталы, способы,
  дубли написаний, аномалии нумерации (УИО «строка 2237») — и поделиться
  выводами с владельцем. ДОПОЛНЕНИЕ владельца 14.08: в тот же анализ —
  расчёт кодификаторов и справочников: пригодность ячеек к кодированию,
  нетиповые ячейки с комментариями-«новеллами» (длинные тексты вместо
  кодов), связка с квестом кодификатора 05-06.08 (НЕ ЗАБЫВАТЬ; канон в
  спеках: specs/2026-08-06-unified-codifier.md — девятиуровневое дерево
  программа→договор-атом; specs/2026-08-06-smart-process-mapping.md;
  specs/2026-08-06-smart-stock-reports.md — две трети аналитики стоково
  достижимо в СМАРТ; факты: лимит 210 знаков нарушают 60/60 сцепок,
  региональная классификация УФБП уже иерархическая — модернизировать;
  в 1С есть контракт+обязательство со способом; переход район→округ,
  программы 2027–2031 пишутся сейчас).
- **62. РЕШЕНИЕ-КАНОН (маркер отсутствия — общесистемный).** Слова
  владельца: «X x Х х» в разных ячейках — например программы и подпрограммы
  (D, E) — означают, что их НЕТ: принятый владельцами книг явный маркер
  отсутствия, смысл как у пустоты. Подозрение владельца: в фильтрах,
  сигналах и коде это учтено не везде — проверить всё. Подтверждено с
  экрана: прод печатает «Основание выбора ЕП: X» как содержимое.
  Сделано 14.08: единый предикат `isAbsentCell`/`cellTextOrNull` в
  packages/shared/src/absence.ts + страж-тест; узкие каноны (org-itself
  для C, PROGRAM_EMPTY для D) обязаны быть его подмножеством и мигрируют
  на него. Свип всех потребителей (показ, фильтры, группировки, сигналы,
  счётчики заполненности, выгрузки) — обязательный пункт приёмки.
- **64. РЕШЕНИЕ-КАНОН (сверка обязана вести до строки).** Скриншот
  сотрудницы 14.08: вкладка сверки показывает УКСиМП факт −181,9 тыс.
  (−0,1 %), но пишет «разбирать нечего», вердикт «Совпадает», сверху
  «Сверка чиста: сходятся все 8». Слова владельца: «система не показывает
  провенанс до самой проблемы с рекомендацией? если нет — нахрена она нам
  вообще?» Канон: (а) ненулевая разница ВСЕГДА кликабельна и раскладывается
  до строк-вкладчиц с карточкой диагноста; (б) допуск 1 % может давать
  вердикт «в пределах допуска», но НЕ «разбирать нечего» и НЕ «сверка
  чиста» — эти слова разрешены только при нулевой разнице; (в) текст «кто
  вероятнее ошибся» обязан быть выводом разбора, не заглушкой.
  Живой разбор −181,9 (14.08, по строкам прод-книги): наш пересчёт =
  Σ(ФБ+КБ+МБ) по строкам 2026 с датой заключения = 137 200,7 — совпало
  с вкладкой; вклад +50,0 — строка 206 (Y-итог затёрт, п.52); остаток
  −231,9 — базис Y листа против нашего периметра счётной строки: лист
  SUMIFS видит строки, которые наш классификатор счётной строки
  отбрасывает (открытая развилка «периметр счётной строки», реестр
  расхождений §5.1) — добить в волне диагноста построчным сравнением с
  сырым листом. Попутная находка: 41 строка УКСиМП на 4 628,5 тыс. руб.
  с суммами факта при Q=«Х» (без заключения) — класс «что считается
  фактом» (§5.4): обе стороны их не считают, но это мусор книги, который
  обязан гореть сигналом с адресами строк.
  РАЗВЯЗКА (14.08, повторный прогон по СЫРЫМ живым листам): обе разницы
  (−181,9 УКСиМП факт, −313,6 УО план) оказались ФИКТИВНЫМИ — вкладка
  сравнивала живой официал со старым пересчётом из кэша снимка. На живых
  листах: УКСиМП — нарушений «итог ≠ ФБ+КБ+МБ» НОЛЬ (Y206 уже исправлена
  владельцем книги), сумма по формуле листа 137 382,6 = официал; УО —
  729 835,3 = официал, строк вне счётного набора ноль. Гипотеза
  «классификатор отбрасывает строки-обрубки» живым листом НЕ подтверждена.
  Канон-дополнение: (г) обе стороны сверки — из ОДНОГО снимка одного
  момента, у каждой подпись момента чтения; при расхождении моментов
  вкладка говорит «стороны из разных моментов — разница не
  интерпретируется» и предлагает пересчёт, а не рисует жёлтую разницу;
  (д) урок диагноста: класс первопричины не называется без построчного
  доказательства на данных того же момента.
- **67. ДИРЕКТИВА (программы и знания G/M/AE).** Слова владельца:
  (а) продуктовое отображение/фильтрация по программам и подпрограммам
  (оси D/E) с возможностью их оценивать и анализировать; (б) умная и
  интересная работа со знаниями из колонок G (предмет), M и AE
  (примечания): группировки, справочная работа, подвязка к КТРУ и ОКВЭД —
  НО словарным/куративным слоем (справочники, разметка, подсказки),
  который обогащает аналитику и НЕ ломает систему, как ломал удалённый
  парсер: машинное чтение свободного текста как источника статусов
  запрещено каноном п.27 навсегда. Стыкуется с кодификатором (п.63) и
  справочниками нетиповых ячеек (п.60).
- **68. Новая книга «СВОД_ДЛЯ_GOOGLE» (разбор к п.59, 14.08).** Состав
  прочитан: СВОД ТД-ПМ (26 колонок), СВОД с месяцами (41), Settings,
  Контроль, ШДЮ старый, зеркала всех 8 ГРБС (УО 3166 строк, УЭР 1501),
  служебные вкладки надстройки: КОНТЕКСТ, СПРАВКА, РАСЧЕТ, ИСТОРИЯ,
  ОТЛАДКА, NVScriptsProperties. Это техническая копия/новая версия
  сводной книги; прод смотрит в другую (ID отличается). ВОПРОС владельцу:
  заменить ею основную сводную (переключить источник сервиса) или вести
  как параллельную книгу отдельной вкладкой? Деталь реализации: values
  по имени листа падают на разборе диапазона (вероятно, невидимый символ
  в имени) — адресоваться внутренним sheetId.
- **69. РЕШЕНИЯ владельца (пакет 14.08, вечер).** (а) Домен:
  **dash-elizovo-uer.ru** — утверждён, реализуем вебхук-режим; DNS на
  14.08 ещё не заведён (домен не резолвится) — нужна A-запись на
  193.233.91.162, после неё Caddy сам получает сертификат;
  (б) ПЕРЕПЛАВКА — порядок изменён: сначала ДОБАВИТЬ новые вкладки поверх
  существующих, существующее прокачать продуктово до 100/100 по каждому
  критерию, удаления — потом отдельным решением; писать с ponytail и
  superpowers, диггерить библиотеки/shadcn/скиллы; визуал без плохой
  вёрстки и слопа; (в) «Ежедневный мониторинг» — ОТДЕЛЬНАЯ вкладка, не
  сливать; допустима логика сверки/верификации план ↔ процедуры, если
  найдётся; предварительно прочитать книгу ПОЛНОСТЬЮ — каждую страницу и
  формулы; (г) ДЕФОЛТ БЕЗ ФИЛЬТРОВ УТВЕРЖДЁН: «2026 · весь год · все
  управления · на сейчас»; (д) РАЗРЕШЕНЫ удаления: простыня предупреждений
  «строка N: предмет» (заменить карточками диагноста), лживый бейдж
  периода на карточках, сигнал «ТД с заполненной программой», термин
  «предрешённость», латинские ключи в подсказках; (е) развилки задавать
  владельцу ПО ОДНОМУ вопросу с нюансами и рисками.
- **71. РЕШЕНИЕ-КАНОН (Р-01 закрыта): «Закупки, проводимые в течение
  года».** Диалог владельца дословно: строки с суммами факта при пустой
  дате заключения «меняются по факту заключения договоров по этой строке,
  в отчёт они не идут»; «в свод капают?» — «нет»; имя канона — «Закупки,
  проводимые в течение года» (статья, разбиваемая на несколько закупок в
  течение периода). Следствия: (а) такие строки — отдельная стадия, НЕ
  входят ни в «заключено», ни в отчёт, ни в свод — ровно как считают
  формулы листа; (б) бейдж «есть факт» на них неверен — нужна собственная
  подпись стадии; (в) директива доп-анализа: понять, как они описываются,
  какие бывают виды, под каждый вид — способ считывания и стандарт
  описания для таблицы; рекомендацию прислать текстом.
  УТОЧНЕНИЕ 14.08 (разбор 902 суммирующих формул свода): фильтр по дате
  заключения несут только формулы ФАКТА и ЭКОНОМИИ — формулы ПЛАНА его не
  имеют. Канон точнее так: «факт и экономия этих строк в свод не капают,
  ПЛАН — входит» (и это правильно: закупка запланирована). Рекомендация:
  docs/superpowers/specs/2026-08-14-comments-and-yearlong-canon.md.
- **102. СЕМАНТИЧЕСКИЙ СДВИГ ПЛАНОВЫХ СУММ (18.08, «момент который многое
  меняет»).** Слова владельца: «то, что я называл лимитами — наши
  исполнители начали записывать туда НМЦК с разбивкой из заявок и своей
  бухгалтерии (как я понял)». То есть колонки H/I/J/K книг ГРБС теперь
  (с какого-то момента) несут НМЦК по заявке, а не лимиты бюджетных
  обязательств. Следствия: (а) в одной колонке две сущности без маркера —
  старые строки лимиты, новые НМЦК; (б) «экономия план−факт» по новым
  строкам = снижение на торгах, по старым = остаток лимита — их нельзя
  складывать молча; (в) вся аналитика «остатка к заключению» и сверка
  лимитов читается иначе. ПРОВЕРКА ДАННЫМИ: по построчному маппингу с
  «Ежедневным мониторингом» (п.101а) сверить K книги с НМЦК процедуры —
  где совпадает, там практика подтверждена; датировку перехода искать по
  журналу правок. «Закупки 2.0» обязаны развести колонки: ЛБО (лимит) и
  НМЦК (мин/макс) отдельно. Дампы книг хранить на диске E (решение
  владельца). УТОЧНЕНИЕ владельца (18.08, вдогонку): по ЕП пишут
  планируемую сумму, которая «по идее должна соответствовать потраченной»
  (план≈факт — отсюда тезис босса «по ЕП не может быть экономии», п.98м);
  по закупкам ЭА пишут НМЦК. Подтверждено стопроцентно: УАГЗО, УКСиМП,
  УД; УО и УЭР — «в основном» тоже НМЦК; УДТХ заполняет ЛИМИТЫ (старая
  семантика). УИО, УФБП — владелец ждёт ответа. ВАЖНО: смешение семантик
  МЕЖДУ книгами подтверждено — сводный «план» по всем ГРБС складывает
  НМЦК одних управлений с лимитами других; сводные плановые числа и
  «остаток к заключению» это наследуют, подписывать честно. Следствие для
  сигналов: семантика отклонения факт↔план зависит от способа — по ЕП
  расхождение = ошибка заполнения или запрещённая «экономия», по ЭА
  факт<НМЦК = торговая экономия (норма). НЮАНС УКСиМП (культура, 18.08):
  сэкономленные на торгах деньги при перераспределении на другие
  потребности ОТМИНУСОВЫВАЮТ ОТ НМЦК — то есть K правится задним числом
  вниз на сумму изъятого. Третья семантика: K = НМЦК − изъятая экономия.
  Владелец: «надо будет приводить всё это в вид». Детекция: расхождение
  НМЦК_мониторинга − K_книги = изъятое (плюс следы ретро-правок K в
  журнале). Закупки 2.0: НМЦК неизменная колонка + отдельная колонка
  «изъято перераспределением».
  СЕМАНТИКА УДТХ (дословно, специалист управления, 18.08): «я не показываю
  НМЦК, а расписываю все наши лимиты под планируемые закупки, поэтому
  когда появляется экономия по какой-то закупке, я естественно снимаю
  лимиты с неё и перераспределяю их на следующую закупку». То есть K =
  распределённый лимит, который ПЕРЕПИСЫВАЕТСЯ ВНИЗ при экономии и
  прибавляется к другой строке — план−факт по такой строке всегда ≈0.
  Это объясняет замер витрины РАСЧЕТ листа СВОД: «УДТХ — нулевая экономия
  при 93,11 % конкурентных процедур» (аномалия «ПОВЕДЕНИЕ», средняя).
  ИТОГО ТРИ СЕМАНТИКИ K: (1) НМЦК неизменная (УАГЗО/УКСиМП/УД, в основном
  УО/УЭР); (2) НМЦК минус изъятое (культура при перераспределении);
  (3) лимит, перераспределяемый внутри управления (УДТХ). Общий
  знаменатель: везде, где экономия «уходит», она уходит ПРАВКОЙ ПЛАНА
  ЗАДНИМ ЧИСЛОМ — исчезает из план−факт и становится невидимой. Продукт
  обязан ловить это по журналу правок (ретро-снижения K у строк с фактом)
  и показывать «экономию, растворённую в плане»; экономия по торгам
  честно считается только против НМЦК (книга мониторинга).
- **141. НЕ РАСХОДЯТСЯ — ДОКАЗАНО ЧИСЛАМИ ПО ДАМПАМ, А НЕ ТОЛЬКО ПО КОДУ
  (суд 21.08 над тремя дознаниями и скептиком).** [Номер исправлен 22.08: пункт
  был записан под номером 137, уже занятым решениями владельца по консолидации
  сигналов, и шестьдесят две ссылки в коде на «п.137» стали двусмысленными.
  Ссылки означают решения владельца; доказательство получило свободный номер.]
  Пункт 136 был выводом из
  чтения кода; здесь тот же вывод подтверждён арифметикой по выкачкам книг
  (`E:/aemr-dumps/book-dumps/svod.json`, снят 17.08), пересчитанной судьёй
  независимо от дознавателей.
  (а) ПЕРИМЕТР 2026 — ТОТ, КОТОРЫМ ЖИВЁТ ПРОДУКТ: наш счёт равен листу до
  пятого знака по восьми ГРБС, обоим блокам и району — план района
  1 332 448,46222 против `K29`, факт 1 043 211,73309 против `O29`, штуки
  2833 против `D29`, УЭР 13 331,22766 против `K61` (то самое число, против
  которого в старом реестре стояли наши 13 921). Δ = 0,00000 везде.
  (б) ПЕРИМЕТР 2025+2026: план 1 843 914,69486 = `K28`, факт
  1 519 279,97054 = `O28`, штуки 3332 = `D28`. Δ = 0,00000.
  (в) 294 592,42677 тыс. руб. на 554 содержательных строках без квартала и
  года сходятся с разницей «все счётные строки минус официальный итог» БЕЗ
  ОСТАТКА (0,00000). Это не занижение официального лимита: лист их не видит
  по построению формулы, наш годовой срез — тоже (`bucket`), они лежат ВНЕ
  ОБОИХ чисел как адресуемый остаток.
  (г) 5 307,20914 тыс. руб. на 46 строках ЕП с заглушкой в дате заключения
  сходятся с разницей «сырой факт минус официальный» без остатка; продукт
  гасит их тем же правилом (`fact-date.ts:19-21`), поэтому в факте мы с
  листом равны.
  (д) НУЛИ, закрывающие остальные версии: строк, где пуст только квартал
  или только год, — 0; строк с пустым видом деятельности F внутри
  периметра — 0; строк с годом вне 2025/2026 — 0.
  (е) ЗЕРКАЛА ЛИСТОВ внутри книги СВОД (`IMPORTRANGE`) равны исходным
  книгам ГРБС по всем восьми, и по плану, и по факту, Δ = 0,00000 — спор
  «читаем ли мы то же, что лист» закрыт; продукт берёт лист «ВСЕ»
  (`google-sheets.ts:497`). Отдельное правило: листы ПОДВЕДОВ мастеру «ВСЕ»
  не равны (УКСиМП 652 строки/154 458,93 против 675/156 103,23), читать
  подведы вместо «ВСЕ» нельзя.
  (ж) НАСТОЯЩИЕ РАСХОЖДЕНИЯ — не наши: карточка ГРБС «УО» официального
  листа занижена на 924,35 тыс. руб. и в плане, и в факте (в блоке УО нет
  строки 1 кв. 2025, район добавляет её прямо в формуле `H7 = H161 +
  SUMIFS(...)`); и знаменатель законтрактованности `Q29` = 78,29 % считается
  без невидимых 294 592,43 — с ними было бы около 64 %.
  СЛЕДСТВИЕ: п.136 подтверждён и усилен числами; тексты продукта об
  «официальный лимит занижен» подлежат замене (21 место: 18 в коде и
  текстах, 3 в спеках и картах). Полный вердикт с адресами ячеек —
  `docs/superpowers/audits/2026-08-21-svod-divergence-verdict.md`.
- **140. ЧЕТЫРЕ РЕШЕНИЯ ПО КНИГАМ И КОЭФФИЦИЕНТУ (владелец, 21.08).**
  (а) ЗАЩИТЫ ДИАПАЗОНОВ — «да, применяй сам»: разрешено ставить шесть зон по
  ролям через API от имени сервисного бота в книгах «Закупки 2.0» и
  «Ежедневный мониторинг 2.0». Прежний блокер (политика безопасности как
  изменение прав доступа) снят прямым разрешением владельца.
  (б) ЖИВОЙ КОЭФФИЦИЕНТ ЭКОНОМИИ — «скользящие 12 месяцев»: коэффициент
  пересчитывается по последнему году и заменяет застывшие восемь процентов,
  в том числе в блоке владельца «Прогноз экономии по остатку ЭА» (СВОД
  ТД-ПМ, R30:U32) и в двадцати семи формулах листа «СВОД с месяцами».
  (в) ДВОЙНОЕ ВЕДЕНИЕ КНИГИ МОНИТОРИНГА — «листы управлений главные»:
  сквозной лист «25-26» становится витриной, собираемой формулами; при
  расхождении (сегодня 32,2 млн руб. по экономии) правда за листом
  управления, и продукт считает по нему.
  (г) СПРАВОЧНИК ЗАКАЗЧИКОВ — владелец: «я собираюсь привести таблицу
  мониторинг к нормальному управляемому виду канонических названий». То
  есть строится КАНОНИЧЕСКИЙ справочник (управления отдельным родом,
  совместные закупки фиксированными названиями групп, учреждения — по
  написанию справочника), а книга приводится к нему; продукт отдаёт
  адресный список приведения и следит, чтобы новые написания не плодились.
- **139. ЕДИНИЦА РАБОТЫ — КАРТОЧКА: МУЛЬТИДИМЕНСИОНАЛЬНОСТЬ И ПОЛНАЯ
  РАБОТА С ФИЛЬТРАМИ (владелец, 21.08).** Дословно: «мультидименсиальность
  должна быть и работоспособность с нашей системой фильтров — драм роллом,
  гибкими датами, ГРБСами, подведами и самими управлениями». То есть
  карточка не считается доведённой, пока не выполнено ОБА условия сверх
  прежнего чек-листа:
  (а) МУЛЬТИДИМЕНСИОНАЛЬНОСТЬ — у карточки переключаемые разрезы (мера:
  количество, деньги, доля, отклонение; ось: управления, организации,
  способы, бюджеты, время), переключение на месте, без ухода со вкладки;
  эталон подачи — карточка управления, которую владелец прорабатывал лично
  (все сегменты видны, включая мелкие подведы, п.131);
  (б) РАБОТА СО ВСЕЙ СИСТЕМОЙ ФИЛЬТРОВ — барабан периода (`monthsByYear`,
  квартал, месяцы, недели), гибкие даты, выбор ГРБС, выбор подведомственных
  организаций и режим «только управление». Карточка либо подчиняется каждой
  оси и говорит об этом паспортом периметра, либо честно называет ось
  неприменимой (п.58б: унаследованный бейдж запрещён). Молчаливое
  игнорирование оси — брак.
- **138. РАСПОРЯЖЕНИЕ ЭКОНОМИЕЙ — МАКЕТ ОЖИВЛЯЕТСЯ ПО СЛЕДАМ (владелец,
  21.08, закрывает вопрос 72 из п.101д).** Ответы дословно:
  (1) перечень статусов свободной экономии — «вроде да», то есть пять
  состояний приняты: свободна (утверждена, судьбу не решали) · оставлена
  ГРБС · изъята УФБП · спорная · возвращена в бюджет;
  (2) откуда продукт узнаёт состояние — «пока в», то есть ВЫВОД ПО
  КОСВЕННЫМ СЛЕДАМ, без правки книг: появилась строка-приёмник на сумму
  экономии — оставлена ГРБС; понижен план задним числом у строки с фактом
  — изъята; ничего не произошло — свободна. Механика перераспределения
  УКСиМП из п.126 и есть источник этих следов; детектор ретро-правок по
  журналу уже описан там же. Вывод обязан быть ЧЕСТНЫМ: состояние,
  выведенное по следу, помечается как догадка продукта, а не как факт из
  книги (п.104);
  (3) кто меняет состояние — ГРБС говорит «прошу оставить», УФБП ставит
  маркер решения; продукт отражает эту пару ролей, а не единоличное
  ведение УЭР;
  (4) заявки внутри продукта — ОТЛОЖЕНЫ: «как права и хранилище будут,
  тогда посмотрим». Кнопка «Подать заявку на перераспределение» до тех пор
  не заводится; макет заменяется живым блоком без неё.
- **137. ОТВЕТЫ ПО КОНСОЛИДАЦИИ СИГНАЛОВ — ДЕСЯТЬ РАЗВИЛОК ЗАКРЫТЫ
  (владелец, 21.08).** Дословные решения по спеке
  `2026-08-21-signal-consolidation.md`:
  (0) про строки без года: «в годовой не входит, но в расчёты,
  отфильтрованные по 2025 и 2025+2026, входит; запланированные на 2025 свод,
  кажется, считает по дате плана — так что как будто всё ок»;
  (1) «закупка в течение года — ТОЛЬКО СТАДИЯ» (вариант A): класс уходит из
  Замечаний, Дисциплины, Отчёта и счёта качества, дом — вкладка «В течение
  года», паспорт замечания снимается;
  (2) ЕП-риск — «как рекомендуешь» (вариант C): критический только при
  степенях «без обоснования» и «решение заказчика», информационный при
  «безальтернативная по закону» и «выгода подтверждена документом»;
  (3) инициативные заявки: «отмечаться должны, но не в риск-списки»; и
  отдельно — «хотелки как класс оставляем» (решение начальницы через
  владельца);
  (4) исполнение чужого года — «вполне как норма, но отдельно, чтобы можно
  было их все показать»: род информационный, отдельный признак с возможностью
  показать перечень;
  (5) законность ЕП: «предложи вариант, чтобы мы могли на него перейти в
  Закупках 2.0 и в существующей системе могли видеть и редактировать, или
  найди вариант лучше» — за исполнителем;
  (6) «факт раньше плановой даты» — информационный, чип привести к паспорту;
  (7) судьба вкладок-корзин — «сам подумай, куда эти сигналы определить»;
  (8) сорок восемь сирот — «выводи их, сам реши куда и как»;
  (9) шкала букв — «как в композите датасета везде» (меньше значит лучше);
  (10) «обоснование ЕП не распознано» — «реши сам, но сигнал важный по вопросу
  справочника: нужно хорошо отработать, чтобы систему привести в
  единообразный, хорошо понятный, цифровизуемый вид».
- **136. ОФИЦИАЛЬНЫЕ ЧИСЛА И НАШИ НЕ РАСХОДЯТСЯ — СТРОКИ БЕЗ ГОДА ВНЕ
  ОБОИХ (вызов владельца 21.08, проверка кода).** Владелец: «почему ты
  считаешь, что официальные числа меньше наших? докажи. Если не сможешь —
  они не расходятся, и надо вписать это в канон и весь проект
  актуализировать»; и решающий довод: «почему сверка не показывает
  расхождений во всей системе, а ты говоришь, что цифры отличаются».
  ПРОВЕРЕНО ПО КОДУ, вывод — владелец прав:
  (а) формула официального листа требует РАВЕНСТВА года и квартала
  (`SUMIFS(...;'УО'!O..;1;'УО'!P..;2025;...)`, лист «СВОД ТД-ПМ») — строка
  с пустым P не попадает туда никогда;
  (б) НО и наш расчёт её не берёт: политика пустого года по умолчанию —
  `bucket` (`calc-engine.ts:624`), в срез года строка не пускается, а
  учитывается отдельной корзиной «без года плана»; в самом коде стоит
  «в срез не пускаем (лист её тоже не видит)»;
  (в) расхождение БЫЛО и было устранено: «наш расчёт до консолидации
  07.08 их учитывал; ровно их сумма и была расхождением лимита»
  (`recon-classify.ts`, шапка `classifyUnfunded`);
  (г) сверка молчит правомерно: при расхождении в пределах допуска
  причины не строятся вовсе (`explainReconLine`), плюс страховка
  сходимости понижает вкладчиков до наблюдений, если они объясняют
  больше самой разницы.
  СЛЕДСТВИЕ: строки без года и без плановой даты лежат ВНЕ обоих чисел —
  это адресуемый остаток, ждущий решения, а не занижение официального
  лимита. Тексты продукта, обещавшие занижение, — неправда и подлежат
  замене (реестр проверок, родословная метрик, действия Дисциплины,
  доказательства Отчёта, описание корзины Реестра). Исключение,
  которое надо назвать явно: узкая поверхность экономических метрик
  (`metrics/economic.ts`) до сих пор работает по правилу `lenient` —
  там строка без года проходит в срез любого года; это единственное
  место, где расхождение с официальным листом возможно.
- **135. БЗ — КАРТИРОВАТЬ И ПОДНЯТЬ ВЫШЕ КАНОНА (владелец, 20.08).**
  Карточки базы знаний (KbHover/KBTooltip/kbHint) картировать по ВСЕМ
  страницам и ВСЕМ метрикам: где есть, где нет, какого уровня. Эталон —
  Отчёт («больше всего отрабатывались в отчёте»), но планка — «уровень
  выше даже лучшего нашего каноничного»: каждая карточка отвечает
  механизм + скоуп + родословную + что делать. И «визуал нужно
  максимально крутой делать».
- **134. САМОВКЛЮЧЕНИЯ ФИЛЬТРОВ — ИСКОРЕНИТЬ КЛАСС (владелец, 20.08).**
  Дословно: «убери все проблемы рандомных непонятных включений фильтров,
  которые давно уже мучают проект, и не допускай их больше». Разобрать ВСЕ
  пути, которыми фильтр может установиться без руки пользователя
  (затравки-seed между вкладками, сохранённые префы localStorage,
  URL-параметры, наследование периода/управлений), закрыть самовключения,
  поставить стражей. Плюс: «оптимизируй скорость загрузки и обновления
  страницы».
- **133. ПРОВЕНАНС И ИЗМЕНЕНИЯ — В ПРАВОМ ВЕРХНЕМ УГЛУ ШАПКИ (владелец,
  20.08, уточнение давнего пожелания по вебхук-режиму).** Компактный
  индикатор на верхней линейке справа от кнопок снятия фильтра и смены
  темы: «понятно написанный, одновременно показывающий всё и вместе с тем
  лаконичный» провенанс — свежесть книг, режим обновления (вебхук/цикл),
  момент последнего чтения, свежие изменения.
- **132. ПУЛЬС — ФИНАЛЬНЫЕ РЕШЕНИЯ ПО КОНЦЕПТУ (владелец, 20.08).**
  Концепт одобрен с правками: (а) переходы-дубли навигации НЕ нужны —
  переходы делаются из тематических мультидименсиональных метрик,
  связанных с каждой вкладкой (как сделано с сигналами); (б) сносы дублей
  подтверждены, как и «сносы и улучшения малофункциональных, занимающих
  место, недекларативных элементов»; (в) показывать ВСЕ плитки сигналов
  (нулевые не сворачивать); (г) раскрыв адресов на месте — да; (д) KPI
  усилить: добавить количество изменений, совершаемых исполнителями
  (журнал правок), и шире — «собрать все показатели и все данные по всем
  таблицам, максимально из них пользы взять».
- **131. ПУЛЬТ → «ПУЛЬС»: ПЕРЕИМЕНОВАНИЕ И КОНЦЕПЦИЯ (владелец, 20.08).**
  Вкладка переименовывается в «Пульс» ВЕЗДЕ (навигация, заголовки, тексты,
  БЗ). Концепция: «держать руку на пульсе» — обзор для управленца и
  мониторящего: всё основное видно мультидименсионально (эталон подачи —
  карточка управления, которую владелец прорабатывал лично: сегменты
  видны ВСЕ, включая мелкие подведы), и из Пульса переходы во все важные
  места продукта. Сигналы внутри Пульса не дублируются (сейчас: полоса
  замечаний + красная карточка + плитки сигналов — три места): один дом,
  остальное — ссылки. Дословно: «надо все везде сводить к одному».
- **130. СПРАВОЧНИКИ ПРОТИВ БАРАХЛА — ПО ВСЕМ ПОЛЯМ 2.0 (владелец,
  20.08).** Одобрено: маркер «Х» в формах 2.0 не переносится — явное поле
  состояния срока («запланирован (дата)» / «исполняется в течение года по
  потребности» / «не обеспечен финансированием»). Дословно: «так по всем
  крестикам»; и шире: «поля, которые тяжело сортировать и где пишут
  всякое барахло, продуктово улучшить до уровня справочник + короткий
  комментарий с шаблонами, как их писать». Касается всех свободнотекстовых
  полей форм 2.0: примечания, комментарии стадий, обоснования ЕП, имена
  подведов — выбор из справочника, свободный текст только коротким
  примечанием по шаблону.
- **129. ОБЛИК: ТОЛЬКО ТОЧЕЧНЫЕ ПРАВКИ, ТЁМНАЯ ТЕМА БЕЗ КОРИЧНЕВОГО
  (владелец, 20.08, гнев).** Дословно: «коричневые прямоугольники —
  уродство, убрать везде»; «нужно было на кнопки навигации в светлой
  теме добавить обводки, а ты перекроил весь проект»; «до этого было
  достаточно симпатично — стоило улучшать, а не портить». Правила:
  (а) кремовая палитра — только светлая тема; в тёмной поверхности
  нейтральный графит, кремовым остаётся лишь золотистый акцентный текст;
  (б) правка облика делается ровно в объёме просьбы — без попутного
  перекроя соседних элементов; сомневаешься в трактовке — покажи макет
  до применения; (в) референс качества — Apple (п.120), не «ИИ-слоп».
- **128. ВЁРСТКА ВКЛАДКИ МОНИТОРИНГ — КОМПАКТ (владелец, 20.08).**
  Убрать левую линейку с организациями; верхнюю панель переверстать в
  один ряд кнопок с минимумом места; вся таблица и её элементы
  помещаются в экран без горизонтального листания страницы; колонки
  дат («заявка · публикация · окончание подачи · торги») складываются
  гармошкой и раскрываются по клику. Запомнить как постоянное
  дизайн-правило дальнейших изменений — ничего из этого не ломать.
- **127. ПОЛНАЯ ИЗОЛЯЦИЯ УПРАВЛЕНИЙ В ФИЛЬТРАХ (владелец, 20.08,
  дословно: «никакие сигналы, замечания и т.д. не лезут в фильтр не к
  своим управлениям — УКСиМП опять жалуется»).** Обобщение прежнего
  фикса периметра Контроля на ВЕСЬ продукт: любой экран, срез, карточка,
  сигнал, замечание, строка гигиены, событие журнала при выбранном
  управлении показывает ТОЛЬКО это управление. Сигнал уровня листа несёт
  departmentId владельца листа; сигнал уровня района не показывается в
  срезе управления вовсе (не «прицепляется» к первому попавшемуся).
  Примеры в подсказках базы знаний подписываются книгой-источником
  («пример из книги УО») и не выглядят текущей ошибкой чужого управления
  (п.119, п.122-дополнение). Проверяется стражем: срез каждого экрана по
  каждому управлению не содержит чужих departmentId.
- **126. МЕХАНИКА ЭКОНОМИИ УКСиМП + СЕМАНТИКА ЛИМИТОВ УФБП (владелец,
  20.08, дословно).** «Если ходатайство на перераспределение остатка
  (экономии) подтверждено — понижаем сумму с планируемой (и она
  становится равна факту), далее убираем пункт „да, является экономией"
  (так как там становится ноль), создаём новую строку и в неё вписываем
  сумму и все последующие действия с закупкой — вот так с экономией
  работает УКСиМП». То есть экономия у УКСиМП растворяется правкой плана
  К (план=факт, флаг AD снимается), а высвобожденные деньги переезжают в
  НОВУЮ строку-приёмник — след держится парой «ретро-правка K у строки с
  фактом» + «новая строка на ту же сумму» (журнал правок). «По
  перераспределению экономии пишут комментарии». УФБП: «пишет лимиты, а
  не НМЦК; практики как у УДТХ (или УКСиМП) нет» — семантика плана УФБП =
  лимит (род 3 из п.102), без перераспределительной механики. Откуда
  лимиты: по ЭА — план-график в ЕИС; по ЕП — сметы (классификация по
  направлению едиными суммами, далее на основании сметы). Закрывает
  давний вопрос семантики плана УФБП; для «экономии, растворённой в
  плане» (п.102) появился точный детектор-паттерн.
- **125. СЕМЕЙСТВО ЭК — НАСТОЯЩЕЕ; ОПЕЧАТКИ КОДОВ ОБЪЯСНЯЮТСЯ (сверка
  20.08 по скриншоту босса).** Реестр мониторинга помечал строки «без
  кода», хотя код виден в предмете. Разбор: «ЭКЗ301-26» и «ЭЗЗК01-26» —
  опечатки (в книгах ГРБС живут ЭЗК301-26 ×6 и ЭЗК01-26 ×5), а «ЭК03-26» —
  НЕ опечатка: код живёт в ГРБС 5 раз, семейство ЭК (электронный конкурс)
  добавлено в парсер пятым (ЭА/ЭЗК/ЭЕП/ЭАС/ЭК). Метка теперь различает
  «кода нет» и «код с опечаткой» с догадкой («похоже на ЭЗК301-26, буквы
  переставлены»); догадка только показывается — сверка по ней не строится.
- **124. ЕП-ОБОСНОВАНИЕ ПРИ КОНКУРЕНТНОМ СПОСОБЕ — СИГНАЛИТЬ (владелец,
  20.08).** Вопрос дословно: способ «ЭА», а в M — текст про ЕП («Закупка
  с ЕП предусматривает заключение по наименьшей цене», УО № п/п 2346) —
  «грубая логическая ошибка». Ответ: не ловилось; добавлено правило (к)
  ep_reason_on_competitive в проверку согласованности комментариев.
  Живых 13: УО №2346 + 12 строк УД со ссылкой «ч.12 ст.93» (малые закупки
  через электронную площадку — формально ЕП, значит неточен способ L).
  Свободные комментарии в M без ЕП-маркеров (11 живых) правило не трогает.
- **123. N И Q ПЕРВИЧНЫ — РАЗ И НАВСЕГДА (владелец, 20.08).** Дословно:
  «P — вторичная история, смотреть надо на N. Запомни раз и навсегда и
  поправь везде, так и с факт-датами и с план-датами». Рукописны N
  (план-дата) и Q (факт-дата); O/P/R/S — производные формулы. Любой
  предикат о датах гейтится по N/Q; производные — только свидетели
  стёртых формул (derivedFormulaBroken). Поправлено: «Факт без квартала»
  смотрел на O в обход N — переделан в остаточную страховку с подавлением
  первопричинами (живых срабатываний 0 на 2 979 строк с фактом; строка
  УДТХ на 67,7 млн из замера 07.08 в книге уже починена); тексты правил
  ведут с N («плановая дата N не проставлена — и год P пуст вслед за
  ней»). Память: n-q-primary-canon.md.
- **122. ГИГИЕНА ТЕКСТОВ НА ЭКРАНЕ — ПЕРЕДЕЛАТЬ ПОДАЧУ (владелец, 20.08).**
  Дословно: «продуктово гигиену текстов, орфографию и т.д. сделай более
  удобно и нормально, а то там текст сплошняком — это полный треш и слоп».
  Результаты проверки орфографии/пунктуации/гигиены текста нельзя вываливать
  сплошной простынёй: нужна структура — группировка по роду находки и по
  управлению, адрес каждой находки (лист · строка · колонка), само слово с
  контекстом вокруг, счётчики, фильтры. Та же планка, что у остальных
  сигналов (п.53, п.119: какая строка, что в ней, почему). ДОПОЛНЕНИЕ
  20.08 (владелец): поясняющие примеры в подсказках («Жар-Птица») смущают
  возрастных сотрудниц — они читают пример как текущую ошибку. Каждый
  пример в базе знаний обязан явно называться пояснением из прошлого
  («пример, НЕ текущая ошибка»), а текущее состояние — только в перечне
  находок карточки с адресами.
- **121. КОД ЛОТА — ВАЛИДНАЯ ФОРМА НОМЕРА (начальница через владельца,
  20.08).** Дословно владелец: «ЭАС205/1-26 существует код, начальница
  говорит». Номер с косой чертой — не искажение, а лот совместных торгов:
  базовый номер + «/» + номер лота. Парсер читает штатно (`lot` в
  ProcedureRef); лот и базовая процедура — разные ключи, мост к базе — по
  семейству+номеру+году. Из списка «пяти паттернов искажений» спеки
  мониторинга форма исключена (осталось четыре).
- **120. ПРАКТИКА РЕФЕРЕНСОВ APPLE/TELEGRAM (директива 19.08, приоритет
  средний).** Дословно: «то, как я допустил, прорабатывал фильтры и кнопки
  (в частности кнопки вкладок до смены цвета) — это крутая работа; для
  этого мы прямо смотрели референсы кнопок и приложений Эппл. Подобную
  практику экстраполировать и уделить ей много времени — в частности по
  BI-графикам, shadcn и другим крутым референсам, библиотекам, кнопкам.
  Но это не высочайший приоритет однозначно».
  ЧТО ЭТО ЗНАЧИТ: при работе над обликом и вёрсткой (волны переезда
  палитр и трёх режимов) обязательный шаг — разбор референсов уровня
  Эппл и Телеграма по конкретному элементу ДО его проектирования:
  навигация, фильтры, кнопки, жесты, мобильные списки, переходы.
  Инструменты уже в арсенале: скиллы разбора руководств Эппл
  (макет, навигация, взаимодействие, платформы), библиотека референсов
  дашбордов из разведки 18.08. Практику вести как с кнопками вкладок:
  смотреть живые образцы → забирать приёмы → надстраивать каноном
  (п.114). Приоритет: после мониторинга, гейта и выката — в волне
  оформления, не отдельной волной.
- **119. ЗАМЕЧАНИЯ ПО ПЕРВОЙ ВЕРСИИ МОНИТОРИНГА (владелец, 19.08) +
  ПРИОРИТЕТ ВТОРОЙ ВЕРСИИ.** Дословно: «нет разбивки по экономии, нет
  поставщиков, нет фильтров по организациям (управления и подведы), нет
  сортировки, нет визуализации». Все пять закрываются второй версией
  (волна «Мониторинг 100% книги» построила ядро, сервер, реестр и
  аналитику). ПОРУЧЕНИЕ: «пушь вторую версию мониторинга, когда полностью
  её завершишь — первоочередно». Порядок: достроить волны → гейт →
  коммит → выкат, мониторинг в приоритете.
- **118. РЕШЕНИЯ ПО НУМЕРАЦИИ И РЕЗУЛЬТАТИВНОСТЬ ПЛАНИРОВАНИЯ (19.08).**
  Дословно: «дублирующихся номеров в рамках одного листа ГРБС мы не
  должны получать, но ДЫРЫ МЫ БУДЕМ ОСТАВЛЯТЬ — нумерация новая всегда
  продолжается вперёд». СЛЕДСТВИЕ: дубль номера внутри листа — нарушение
  (сигнал остаётся); ПРОПУСК номера — НЕ нарушение, а информация: след
  выведенной из плана закупки. Семантика проверки нумерации меняется:
  пропуски из «предупреждения» переезжают в информационную метрику.
  ИДЕЯ ВЛАДЕЛЬЦА (на будущее, «хороший показатель пользы»):
  результативность работы с планированием процедур — какие закупки
  убираем, какие совмещаем. Пропуски нумерации + сравнение снимков +
  события журнала = сырьё для показателя «выведено из плана / объединено
  за период». Признано, что силами системы сейчас полностью не
  реализуемо, но закладывается в модель.
  ТАКЖЕ (поручение 19.08): сигнал на ОТСУТСТВУЮЩИЕ номера ЭА в
  комментарии УЭР (AG) у конкурентных строк — номер процедуры обязан
  быть проставлен, без него строка не связывается с книгой мониторинга.
  ТАКЖЕ (картинка 1, культура): в карточке гигиены текста для УКСиМП
  показан пример из книги УО (Жар-Птица) — статический текст подсказки
  читается как содержимое фильтра. Правило: примеры в подсказках обязаны
  быть ЯВНО подписаны «пример из книги …», а сами находки — только из
  отфильтрованного периметра. ОБЩЕЕ ТРЕБОВАНИЕ канона подтверждено:
  система фильтров изолирует организации — любой сигнал показывает, по
  какому ГРБС и какой организации внутри него он сработал.
  ТАКЖЕ (картинка 2): сигнал «строки листа не попали в расчёт — 1 строка»
  непонятен читателю. Требование: по каждому сработавшему сигналу должен
  быть виден ОТВЕТ — какая именно строка, что в ней, почему не попала.
- **117. ЖУРНАЛ ПРАВОК АДРЕСУЕТ ПО НОМЕРУ СТРОКИ — ИСТОРИЯ СКЛЕИВАЕТСЯ
  ИЗ ЧУЖИХ ЗАКУПОК (находка 19.08, разбор жалобы про ансамбль «Русь»).**
  Журнал книги (_ChangeLog) пишет адрес ячейки вида «G437», то есть КОЛОНКУ
  И НОМЕР СТРОКИ на момент правки. Строки листа двигаются (вставки,
  сортировки, удаления — канон п.98б, п.105), поэтому записи с одним и тем
  же номером относятся к РАЗНЫМ закупкам.
  ДОКАЗАНО живьём: в книге культуры строка 437 сегодня — «Оплата проезда
  ансамбля „Русь“», а запись журнала от 01.06.2026 гласит: «G437: было
  „Бумага А-4“ → стало „ЭА213-26 Поставка бумаги“». Значит история строки
  437 сшита из двух разных закупок; в июне там стояла бумага.
  СЛЕДСТВИЕ ДЛЯ ПРОДУКТА: таймлайн строки (core/timeline/row-timeline.ts)
  отбирает записи журнала сравнением номера строки (строка 185:
  `addr.row !== input.sheetRow`) — то есть показывает пользователю чужую
  историю, не сообщая об этом. Класс ошибки тот же, что у адресации
  замечаний (п.98б), только тут он подменяет не адрес, а СОДЕРЖАНИЕ.
  ЛЕЧЕНИЕ (в работу): (1) отбор записей журнала дополнительно сверять с
  подписью строки, которую пишет сам журнал (колонка «Строка» вида
  «№ 38 · Услуги почтовой связи»), — она несёт № п/п и предмет; (2) при
  расхождении подписи с текущей строкой — не выбрасывать запись молча, а
  помечать её как «правка прежней закупки на этом месте»; (3) если
  подписи нет, показывать честную оговорку «история собрана по номеру
  строки и может включать правки других закупок».
- **116. МАНДАТ НА МИНИ-БЛОКЕРЫ (директива владельца 18.08).** Дословно:
  «каждый мини-блокер в процессе, где можно улучшить или мигрировать —
  делай, как порекомендуешь, в сторону улучшения и прогресса».
  ЧТО ЭТО ЗНАЧИТ: мелкие развилки я решаю сам и двигаю вперёд, не
  останавливая работу вопросом. Под мандат попадают: обновления версий
  библиотек и замена мёртвых зависимостей живыми; ужесточение проверок
  типов и линтеров; ускорение сборки и отклика; чистка мёртвого кода;
  разделение разросшихся файлов; замена самописного готовым (п.112) и
  надстройка готового нашим знанием (п.114).
  ЧТО ОСТАЁТСЯ ЗА ВЛАДЕЛЬЦЕМ (мандат НЕ покрывает): смысл показателей и
  методики счёта; тексты, которые видит человек, если меняется смысл;
  правки в живых книгах; выдача прав и доступов; выкат на прод; крупные
  переезды, меняющие облик или требования к рабочим местам (миграция
  оформления — отдельное решение, уже принято 18.08).
  ПРАВИЛО БЕЗОПАСНОСТИ: любое такое изменение идёт со стражем-тестом,
  проходит гейт и называется в отчёте — «сделал то-то, потому что то-то».
- **115. ИСКЛЮЧЕНИЯ ИЗ ПРАВИЛА «СИНИЙ → КРЕМОВЫЙ» (решение владельца 18.08).**
  Правило кремового хрома остаётся (палитры blue/sky/indigo переопределены
  кремовой шкалой в настройках оформления — 748 вхождений классов по коду).
  ПОДТВЕРЖДЁННЫЕ ИСКЛЮЧЕНИЯ, менять НЕ надо (дословно: «бирюзовый и
  фиолетово-синий ок»):
  · бирюзовая подсветка раздела «Реестр» в навигации (Header.tsx, цвет
    прописан числом);
  · фиолетово-синие столбцы графика на Пульте (Dashboard.tsx; индиговый на
    светлой теме, сиреневый на тёмной), а также категориальный ряд
    словаря ролей, начинающийся с индигового.
  Цвета данных (федеральный синий, краевой зелёный, местный янтарный,
  серии графиков) исключением не считаются — они и не были под правилом.
  ПРИ МИГРАЦИИ на четвёртую ветку оформления: переопределение палитр
  обязано переехать в переменные темы без потерь; эти три места
  сохраняются как есть.
- **114. ГОТОВОЕ — ОСНОВАНИЕ, А НЕ ПОТОЛОК (директива владельца 18.08).**
  Дословно: «и готовые системы ты уже можешь улучшать… это касается не
  только проверки орфографии, а всего проекта вообще; всё, что ты уже
  сделал, ты можешь лучше».
  ПРАВИЛО ДВУХ ШАГОВ: (1) взять готовое (п.112) — не изобретать велосипед;
  (2) НАДСТРОИТЬ его нашим знанием предметной области, которого у готового
  решения нет. Примеры надстройки: словарь орфографии знает русский язык,
  но не знает наших предметов закупок, наименований учреждений и кодов
  процедур — частотный словарь корпуса и справочники дают то, чего в нём
  нет; библиотека графиков рисует, но не знает канона «цвет только данным»
  и двух тем; статистика считает Бенфорда, но не знает порогов 44-ФЗ.
  ТО ЖЕ ПРО СВОЁ: любая уже сделанная часть продукта — черновик следующей
  редакции. Волна обязана оставлять после себя лучше, чем было (п.113 §7),
  и возвращаться к сделанному, когда появилось новое знание.
- **113. ХАРНЕСС РАБОТЫ: ДУМАТЬ ДО, А НЕ ПОСЛЕ (директива владельца 18.08).**
  Дословно: «продумывай заранее — и что делаешь, и харнесс в подробностях,
  заранее понимая, что делаешь и что хочешь; строй систему, а не плюй в
  колодец».
  ДОКУМЕНТ: docs/superpowers/specs/2026-08-18-work-harness.md — пять ворот
  (постановка · готовое · данные · приёмка · швы), карточка волны,
  разделение зон между параллельными волнами, проверка результата глазами,
  различение «лоскут против системы», ритм работы.
  ПОВОД (мои промахи 18.08, разобраны в документе): самодельный
  спелчекер до поиска готового; плоский реестр вместо ста процентов книги
  мониторинга; сигнал, обвинявший здоровые строки; 23 метрики-сироты на
  экране; управляющие символы, записанные скриптом в файл.
  ЖЁСТКОЕ: волна без заполненной карточки не запускается; канон-решение
  без стража в коде считается незакреплённым; после записи канона о данных
  обязательна ревизия уже написанного кода на соответствие.
- **112. БРАТЬ ГОТОВОЕ, НЕ ИЗОБРЕТАТЬ (директива владельца 18.08).**
  Дословно: «в целом не изобретать, а брать готовое — так же, как скилл
  ponytail и Context7, касается всего в этой программе. В целом найти даже
  кучу классных визуальных библиотек, кнопочек, карточек, материалов,
  референсов и делать красивее, лучше и качественнее — это гуд».
  ПРАВИЛО: перед тем как писать своё, обязан проверить готовое —
  документацию библиотеки через Context7, открытые решения на GitHub,
  практику в обсуждениях. Своё пишем, только если готовое не подходит по
  лицензии, весу, языку (нужна кириллица) или архитектуре — и причина
  называется в шапке модуля. Это распространяется на всё: проверку
  русского языка, статистику и обнаружение аномалий, таблицы, графики,
  компоненты интерфейса, инфраструктуру.
  ВИЗУАЛЬНАЯ ЧАСТЬ: собирать библиотеки компонентов, кнопок, карточек,
  анимаций, референсы дашбордов; брать лучшее и доводить до нашего канона
  (русский язык, крем-фон, цвет данным, обе темы, доступность).
- **111. СКОРИНГ = ДЕТЕКТОР ПОДОЗРИТЕЛЬНЫХ ЗАКУПОК (уточнение 18.08).**
  Дословно: «про доверие речь шла так — выглядит ли конкретная закупка и
  числа в ней пиздежом или опечаткой; поскольку это ручной ввод, очень
  хочется иметь умную математическую современную мощную машину, которая
  будет находить подозрительные закупки, которые люди могут вбивать,
  чтобы прятать коррупцию».
  РЕШЕНИЕ: старый сводный «балл доверия» снят (смешивал несопоставимое).
  Вместо него ДВЕ РАЗДЕЛЬНЫЕ ШКАЛЫ на строку: «похоже на опечатку»
  (порядок величины, выпавший разряд, рубли вместо тысяч, год мимо на
  единицу, круглая сумма среди копеечных) и «похоже на подгон» (закон
  Бенфорда по учреждению, липкость к порогам 44-ФЗ снизу, дробление
  однотипных закупок одного поставщика, масса конкурентных процедур с
  нулевым снижением, ретро-правки сумм после факта). Выход — не балл, а
  список признаков с объяснением, суммой под риском и адресом; малые
  выборки честно помечаются «данных мало, вывод ненадёжен»; формулировки
  без обвинений (п.53).
  СНЯТЫ КАК ПУСТЫЕ (подтверждено владельцем 18.08): «итого экономия»,
  «процент исполнения ФБ» (тройной дубль), «ожидает» (равен отклонению),
  «доверие двоичное».
- **110. СИГНАЛЫ ПРОТИВОРЕЧИЯ ТЕКСТА И ФАКТА (решение владельца 18.08).**
  Владелец вернул тему «подвисшего контракта»: «это неплохой сигнал, он
  может говорить о некорректности и неактуальности комментариев; хотелось
  бы ряд сигналов, которые бы это вычленяли — например, где пишут, что
  контракт отложен до даты позже той, по которой фактически уже
  заключились». Причина прежнего отключения названа прямо: «отключал
  потому, что ты мне даты контрактации портил датами из комментариев,
  даже если это даты постановлений».
  ГРАНИЦА (уточняет п.27): из текста НЕЛЬЗЯ брать факты и статусы; текст
  МОЖНО сверять со структурой и сообщать о противоречии. Дата в тексте
  никогда не становится датой закупки.
  РЕАЛИЗОВАНО 18.08 (comment-consistency.ts, 26 тестов):
  (д) «отложено до даты позже заключения» — текст пережил событие; даты
  чужих документов («распоряжение от…», «протокол от…») исключаются;
  (е) отрицание при заполненной дате — «контракт не заключён» при дате Q;
  ТОНКОСТЬ: «аукцион не состоялся» и «процедура отменена, размещена
  повторно» — про ПРОЦЕДУРУ, законный сюжет закупки у единственного
  поставщика, карточка НЕ выдаётся; «закупка отменена» — выдаётся;
  (ж) «оплачено/исполнено» при пустых фактических суммах.
- **109. ФЕДЕРАТИВНОСТЬ: ВМЕСТЕ И РАЗДЕЛЬНО (директива владельца 18.08).**
  Дословно: «всё должно быть как бы и вместе, и как бы разделено, потому
  что мониторингом занимается ДРУГАЯ КОМАНДА, но тем не менее проект один,
  и наша задача вести к консолидации, верификации, прозрачности и полной
  аналитике, мониторингу, контролю и управлению».
  ЧТО ЭТО ЗНАЧИТ В ПРОДУКТЕ:
  (1) ВЛАДЕНИЕ ВИДНО. У каждого источника на экране назван хозяин: книги
  ГРБС ведут управления, книгу мониторинга — команда УЭР. Число всегда
  подписано, чьё оно и когда прочитано (п.58). Чужие данные продукт
  ПОКАЗЫВАЕТ и СВЕРЯЕТ, но не переписывает молча.
  (2) РАЗДЕЛЬНЫЕ КОНТУРЫ. Разделы живут своими данными, кэшами и
  сигналами; поломка чужого источника не роняет свой (честная плашка
  вместо тишины). Права на правку — по владельцу источника (роли, п.99/66-67).
  (3) ОБЩИЙ ПЕРИМЕТР И ЯЗЫК. Фильтры, адресация, единицы, карточки
  диагноста, карта происхождения — общие для всех разделов (п.108).
  (4) СВЕРКА КАК ЖАНР, НЕ КАК ПРАВКА. Расхождение источников двух команд
  показывается ОБЕИМ сторонам с адресами и суммами; продукт — арбитр
  прозрачности, а не редактор чужой книги. Итог сверки: кто что видит и
  что кому делать, без «победившего числа» по умолчанию.
  (5) ЦЕЛЬ ДВИЖЕНИЯ: консолидация → верификация → прозрачность → полная
  аналитика → мониторинг → контроль → управление. Каждая волна обязана
  двигать хотя бы одну ступень и не ломать предыдущие.
- **108. ИНТЕГРАЦИЯ, А НЕ ЛОСКУТНОЕ ОДЕЯЛО (директива владельца 18.08).**
  Дословно про вкладку Мониторинга: «интегрируй это в проект — не как
  лоскутное одеяло». ПРАВИЛО ДЛЯ ВСЕГО НОВОГО: раздел считается сделанным
  не когда он работает сам по себе, а когда сшит с остальным продуктом по
  пяти швам:
  (1) ОБЩИЕ ФИЛЬТРЫ — периметр управлений, период и год действуют и здесь;
  свой периметр раздел не заводит;
  (2) СКВОЗНАЯ НАВИГАЦИЯ — из процедуры в строку реестра и обратно, из
  замечания в источник, каждая сущность адресуема (строка листа + № п/п,
  процедура — кодом);
  (3) ЕДИНЫЙ ПОТОК ЗАМЕЧАНИЙ — сигналы раздела попадают в общий Контроль и
  Проблемы карточками диагноста (п.53), а не живут отдельным списком;
  (4) ОБЩИЕ ЧИСЛА — метрики раздела входят в карту происхождения (п.104),
  подписаны единицами и моментом чтения (п.58), не спорят с соседями;
  (5) ВЗАИМНОЕ ОБОГАЩЕНИЕ — данные раздела питают другие экраны (НМЦК и
  цена торгов → живой коэффициент экономии → Экономика и Свод; стадии →
  карточка строки; победители → конкуренция), а не лежат в своём углу.
  Обратное тоже: карточка строки реестра показывает связанную процедуру.
- **107. ДВА РОДА ДЕФЕКТА ФОРМУЛ (уточнение владельца 18.08).** Дословно:
  «есть беда не только со стёртыми формулами, но и с НЕ ПРОТЯНУТЫМИ при
  создании новых позиций». То есть класс шире, чем сигнал п.98а:
  (а) СТЁРТАЯ формула — в строке она была, её затёрли значением или
  очистили (живой пример: УКСиМП 280/283, O и P пусты при заполненной N);
  (б) НЕ ПРОТЯНУТАЯ формула — строку добавили ниже последней, а формулы
  из соседней строки в неё не растянули: ячейка пуста с рождения, и
  «стирать» было нечего. Второе особенно связано с п.105: добавление
  строки — отдельное событие, и именно в нём рождается дефект.
  РАЗЛИЧЕНИЕ ВАЖНО ДЛЯ ДЕЙСТВИЯ: стёртую восстанавливают из соседней
  строки; непротянутую — протягивают при создании позиции, а лечится
  класс правилом «формулы протянуты на весь рабочий диапазон листа» +
  проверкой при добавлении. ДЕТЕКЦИЯ: сигнал по строке уже ловит оба
  (производная пуста при заполненном источнике), но карточка обязана
  называть род: «формула стёрта» (журнал знает прежнее значение ячейки)
  против «формула не протянута» (ячейка пуста с момента появления строки,
  соседние строки формулу имеют). Плюс правило уровня листа: «в столбце
  формула есть в N строках из M — не протянута в строках …».
- **106. ПРАВКИ МОНИТОРИНГА — РУКАМИ СПЕЦИАЛИСТА (решение 18.08).**
  Список из 40 позиций (docs/superpowers/specs/2026-08-18-monitoring-edit-proposal.md)
  передан специалисту; правки он делает сам, доступ на запись продукту
  пока НЕ нужен. Дальше отталкиваемся от его обратной связи. Инструмент
  проверки результата — `scripts/book_diff.py`: сравнивает снимок «до» с
  новым и печатает «было → стало» по ячейкам тремя классами (правка,
  заполнение, очистка), отдельно называя стёртые формулы. Порядок: снять
  новый дамп книги → прогнать сравнение → отчитаться, какие позиции
  закрылись, что появилось нового.
- **105. ТРИ РОДА СОБЫТИЙ НАД СТРОКОЙ (уточнение владельца 18.08).**
  Дословно: «есть правка, а есть добавление новой строки — новой закупки.
  Так же как их возможные удаления… это тоже важно!»
  ЗАМЕР 18.08 по журналам: УКСиМП — 3 702 записи, из них 43 группы —
  ДОБАВЛЕНИЕ строки (5+ ячеек одной строки заполнены из пустоты в одну
  минуту; пример: строка 95, 09.04.2026 16:08, 7 ячеек), 3 404 —
  точечные правки, УДАЛЕНИЙ — НОЛЬ; УД — 529 записей, добавлений и
  удалений ноль. Вывод: скрипт журнала реагирует на правку ячеек, а
  УДАЛЕНИЕ СТРОКИ через меню Google Sheets правки ячеек не порождает —
  закупка может исчезнуть из книги БЕССЛЕДНО. Плюс удаление сдвигает все
  нижние строки: адрес «строка листа N» в старых замечаниях протухает
  (та же болезнь, что в п.98б; лекарство — № п/п).
  СЛЕДСТВИЯ В РАБОТУ: (1) метрика нагрузки считает три рода событий
  раздельно — правки, добавления, удаления; (2) детектор исчезнувших
  строк строится СРАВНЕНИЕМ СНИМКОВ (снимок SQL + недельные срезы) по
  № п/п, а не по журналу; (3) сигнал «строка исчезла» с последним
  известным содержимым — деньги и предмет пропавшей закупки; (4) в
  предложение правок книг добавить пункт: доработать скрипт журнала,
  чтобы он фиксировал добавление и удаление строк явно.
  ⏰ ОТЛОЖЕНО владельцем 18.08 («пока что фиг с ним») — доработка скрипта
  журнала (Apps Script книг: onChange для INSERT_ROW/REMOVE_ROW вдобавок к
  onEdit) ждёт его отмашки. НАПОМНИТЬ владельцу при возврате к теме
  провенанса/книг. До тех пор пропажи строк ловятся сравнением снимков.
- **104. БАЗА АНАЛИТИКИ — ПЯТЬ ИСТОЧНИКОВ (директива владельца 18.08).**
  Дословно: «свод, отчёт, доп. отчёт, мониторинг, 44-ФЗ — база нашей
  аналитики помимо целей и задач. Работая со всем проектом и всеми
  вкладками, подмечать рассчитываемые вещи из них и то, на что они
  обращают внимание, и т.д. — стоит наполнять этим и отталкиваться от
  этого. Это изначально так нужно было делать, но что поделаешь».
  РАБОЧЕЕ ПРАВИЛО: каждая метрика продукта обязана иметь происхождение из
  одного из пяти источников (лист СВОД ТД-ПМ · еженедельный отчёт ·
  дополнительный отчёт · книга «Ежедневный мониторинг» · нормы 44-ФЗ)
  либо явно помечаться как наша инициатива. Метрика без родословной —
  кандидат на снятие. Вести карту «показатель → источник → как считает
  источник → как считаем мы» и пополнять её при работе с каждой вкладкой.
- **103. МЕТРИКА НАГРУЗКИ УПРАВЛЕНИЙ (идея владельца 18.08).** Повод —
  замер журналов правок: УО 33 724 записи, УКСиМП 4 904, УД 568, УФБП
  124, УАГЗО 70, УДТХ 34, УЭР 31, УИО 13. Владелец: «вот тоже хороший
  повод для метрики нагрузки на управления и количеству изменений, в
  частности на количество закупок и подведов вообще». Строить: правок на
  закупку, правок на подведа, закупок на подведа, закупок на управление,
  доля правок «критических» (даты/суммы) — с оговоркой наблюдаемости
  (пустой журнал ≠ нет работы). Экран: нагрузка и трудоёмкость ведения,
  НЕ рейтинг вины (тон п.53); связка с провенансом (п.102).
- **101. РЕШЕНИЯ 18.08 (вторая серия).**
  а) Вкладка Мониторинг: в наименование добавить «Реестр процедур
  определения поставщика». Перенести ВСЕ страницы книги; смотреть
  по-разному; своя аналитика, свои визуальные метрики и графики; своя
  сверка и подтяжка к нашим данным; свои сигналы; маппинг с книгами ГРБС
  ПО КАЖДОЙ СТРОКЕ (проблемы маппинга — отдельный класс сигналов).
  б) Свод ТД-ПМ: владелец только ПЕРЕИМЕНОВАЛ существующие метрики,
  расчёты НЕ менялись («суть стала понятнее»); сканировать полностью
  (дамп 278 МБ выполнен, анатомия в работе).
  в) Вопрос 73 — финал: слить канон №15 «ЭА не состоялся» со ссылкой
  «п.12 ч.1 ст.93», мёртвый канон убить.
  г) Вопрос 70: строка-приёмник одобрена; «куда в таблицу писать» —
  передано начальству, ЖДАТЬ ответа.
  д) Вопрос 72: только макет скриншотом. Постановка: понять ВСЕ статусы
  свободной экономии («оставлена и распределена внутри; отобрана и
  отдана куда-то ещё; какая ещё может быть?»).
  е) Правовая база: «переходи глубже вообще в весь БК РФ и законы» —
  фронт изучения всей матчасти бюджетирования.
  ж) Доступ: у владельца есть настройки доступа для сервисного аккаунта
  (пилот записи возможен).
- **100. ДИРЕКТИВА СТОПРОЦЕНТНОСТИ (18.08, дополнение к п.96).** Дословно:
  «нужно 100 процентов покрытия, точности, понимания исходных данных, 100
  процентов отработки каждого файла программы, каждого слоя и функции и
  функционала программы, потом сто процентов выдачи той мощи что она дает
  на экран и сто процентно удобный красивый быстрый безотказно работающий
  интерфейс». Плюс: обязательная отчётность по поставленным задачам.
- **99. РЕШЕНИЯ (вопросы 62–74, ответы 18.08).**
  62 (СМАРТ) — просмотр рабочего контура со специалистом УФБП.
  63 (акты бюджпроцесса) — прислать; мне назвать конкретно, что нужно.
  64 (ИКЗ) — колонка по крупным закупкам (свыше порога малой).
  65 (архив доп-отчётов) — прислать что сохранилось.
  66 (паритет правки) — ЦЕЛЬ: работа В ПРОДУКТЕ, книга = дубляж. Критика
  владельца: продукт «в зародыше продуктового состояния для заполнения»,
  табличная форма — «ИИ-слоп залипуха, неэффективная, неудобная,
  неэффективно расходует пространство». Реестр-эталон обязан это закрыть.
  67 (право записи) — пилот на одной книге; сказать владельцу, что от него
  требуется (выдать редактора сервисному аккаунту).
  68 (перераспределение) — УФБП решает только «забирать или нет» (да/нет);
  что не забрали — распоряжается сам ГРБС. Поручение: изучить закон о
  бюджетном процессе (БК РФ) и матчасть самостоятельно.
  69 — статусы заявок ведёт продукт. 70 — донор→приёмник да, но объяснить,
  что имеется в виду. 71 — арбитра нет: ГРБС просит УФБП не забирать
  остаток (не считать экономией). 72 (момент погашения) — показать
  конкретную реализацию, тогда ответит. 73 (мёртвые каноны ЕП) — оставить
  оба, отметить как мёртвые; по слитию №15 со ссылкой на п.12 ст.93 —
  уточнить суть; если идея хороша, владелец сформулирует запрос коллегам.
  74 (Реестр-эталон) — ПОДТВЕРЖДЁН, но проработать глубоко каждое понятие
  («действительно ли понимаешь, что значит таблица уровня Google Sheets»).
- **98. ПАКЕТ ПОРУЧЕНИЙ 18.08 (второе интервью аудитории + задачи владельца).**
  Дословные проблемы и поручения:
  а) **БАГ unfunded**: строки, где производные ячейки дат (O/P от N; R/S от Q)
     остались без формулы, определяются как «не обеспеченные финансированием»
     («мол нет года»). Канон п.93/45: рукописны только N и Q, остальное —
     формулы. Фикс: unfunded только при пустой N; N есть + производная пуста =
     сигнал «сломана формула» с адресом ячейки. Живое подтверждение — жалоба
     оператора: «не обеспечено финансирование — 4 строки — в каждой все данные
     заполнены и сверены».
  б) **БАГ номеров строк**: «номера строк закупок в системе не везде бьются —
     как будто идут не те данные»; оператор: «УКСиМП, строка 155 (по вкладке
     пульт) в реальности 534 — Опрессовка системы»; «внесла данные — из
     красного не ушла»; «отсутствие номеров строк восстановила — из замечаний
     не ушло». Диагноз ожидается: пульт на другом/старом источнике.
  в) **БАГ Контроль/AD**: «раздел контроль — статус AD — подтягивает всех
     подряд вместе с нами — в УКСиМП по фильтру попадает УД». Потерян периметр
     управления.
  г) **Свод ТД-ПМ изменён владельцем** («чуть поменял, сделал лучше»);
     начальница добавила разбивку план-факта («посмотри — можешь добавить»);
     расчётная экономия без УИО удалена с листа. Поручение: полностью
     просканировать, оценить, задать вопросы, скорректировать программу.
  д) **Сигналы гигиены текста**: опечатки, двойные пробелы, прочие символы —
     «очень хорошо понятно сделанные», чтобы помогать операторам приводить
     данные в порядок (расширение п.95/55 на все текстовые поля).
  е) **Справочник предметов закупок**: помогать приводить предметы к
     справочникам/группировкам (сверх инструмента объединения закупок).
  ж) **Динамика ЕП + обоснованность (идея босса)**: показывать, насколько
     меньше становится ЕП и особенно НЕобоснованного ЕП. Категория
     «обоснованный ЕП»: монополисты, направляли КП в адрес УЭР, наименьшая
     цена и т.д. — возможно расширить справочники. Поручение: понять из всех
     данных, что такое обоснованный/необоснованный ЕП по 44-ФЗ и по местной
     практике; оценить и указать, если у нас не гуд; долю уменьшения ЕП
     считать и по изменениям в книгах, и по срокам (неделя/месяц/квартал).
  з) **Сигнал нумерации A**: отсутствующие и повторяющиеся порядковые номера.
  и) **Выгрузки по референсу**: возможность выгружать таблицы «в подобном
     виде как по референсу, но лучше» (референс — найти в материалах).
  к) **Живой коэффициент экономии вместо 8%**: «внедрить в свод ТД-ПМ такой
     коэффициент, который не примерный посчитанный когда-то, а всегда
     актуальный и правильный; в дэшборде такой же; очень хорошо обосновать и
     представить проект по замене формул во всех таблицах и модулях». Для
     среднего использовать книгу «Ежедневный мониторинг» (там НМЦК и
     фактическая сумма торгов). «У нас пока чё попало нестабильное».
  л) **«Закупки 2.0»**: улучшенный проект ВСЕХ наших таблиц с разбивкой НМЦК
     (план-минимум и план-максимум), решающий все реальные проблемы исходных
     таблиц (справочники, возврат экономии назад). Выдать НЕ черновиками, а
     отработанными 10/10 Google-таблицами на Google Drive владельца в новой
     папке «Закупки 2.0»: в основе дамп существующих данных, новые столбцы,
     все формулы целостные, актуализированные под новый формат.
  м) **Сигнал «экономия по ЕП»** (босс: по ЕП не может быть экономии): факт ≠
     план по ЕП — оценить необходимость, возможно разбить существующий сигнал.
  н) **Модель экономии в новых таблицах**: общая экономия по закупке =
     свободный остаток лимитов (наше «экономия „да“» — могут забрать) +
     экономия по торгам; суммарно — общая экономия по закупке.
  о) Оператор: «Оплата проезда ансамбля „Русь“ — данные слетели, не
     сохранилось, посмотреть с Баклановым» — проверить историю строки по
     таймлайну/журналу.
  п) «Остальные прошлые задачи не снимаются. Собери всё воедино, спланируй
     работу максимально качественно по всем фронтам, с учётом всех нюансов —
     вообще, а не только этой сессии».
- **97. РЕШЕНИЯ (понедельник): SVOD_MODES отменён; карточный реестр =
  мобильная версия эталона.** (61-финал) SVOD_MODES отменён С
  НАСЛЕДОВАНИЕМ (владелец: «да»): забираем идею помесячного двойника как
  представления в будущем Реестре-эталоне; документ помечается
  superseded. (Реестр) Идея владельца: нынешний карточно-списочный вид
  Реестра НЕ выбрасывается — «переделать и допилить под МОБИЛЬНУЮ/
  альтернативную версию»: у Реестра-эталона два представления — таблица
  уровня Google Sheets (десктоп, основная) и карточный список (мобильная/
  альтернативная), одна модель данных и одни слои под обоими.
- **96. ДИРЕКТИВА (инженерный максимум) + проверки 53/55 выполнены.**
  Слова владельца: «придумай очень гениальные кодовые решения, чтобы
  добиться максимального ПОКРЫТИЯ во всём — в ресурсах, методах,
  организациях, документах, форматах документов; чтобы абсолютно всё
  было корректно; максимальное качество и скорость работы». Рамка
  следующей волны после 100/100: полнота словарей и реестров, все
  форматы (xlsx/xlsb/docx/pdf), все 75+ организаций, property-тесты
  корректности, профилирование скорости.
  ПРОВЕРКА 53 (Settings, живьём): УАГЗО объявляет «УАГЗО» — листа НЕТ,
  должно «ВСЕ»; УКСиМП объявляет «УКСиМП» — листа НЕТ, должно «ВСЕ»;
  УД объявляет «УД» — лист ЕСТЬ, но это лист аппарата (95 строк), а не
  полный, должно «ВСЕ»; УО — верно («ВСЕ»). ⏰ НАПОМНИТЬ владельцу
  завтра исправить три значения.
  ПРОВЕРКА 55 (Жар-Птица, живьём): двоение ПОДТВЕРЖДЕНО — «ДС № 3» 43
  строки, «ДС №3» 5 строк (листы 2309, 2521, 2647–2649); выдано
  проблемой, канон-сигнал отступлений от справочника — в работу.
- **95. РЕШЕНИЯ (вопросы 52–61 серии).** (52в→а, СРОЧНО) 34+70 строк
  УО между «ВСЕ» и вкладками подведов: файл-реестр для передачи УО,
  разовый разбор, затем канон «ВСЕ». (53) Settings УАГЗО/УКСиМП:
  проверить живьём, какой лист объявляют и какой должны; владелец
  исправит завтра — ⏰ НАПОМНИТЬ с точными значениями. (54б+) 
  «Администрирование»/«Опека» — самостоятельные единицы счёта, но
  ВИЗУАЛЬНО ВЫДЕЛЕНЫ (не сливать с учреждениями). (55+КАНОН) «Жар-Птица»:
  подтвердить двоение живыми строками и выдать проблемой; ОБЩЕЕ ПРАВИЛО:
  сигналы покрывают ВСЕ отступления от справочника — опечатки, двойные
  пробелы, регистр, любые ошибки ввода имён. (56а) Наименования — ждём
  документ, до него реестр по книгам де-факто. (57а+) Счётная строка:
  как лист, мусор сигналом; ТРЕБОВАНИЕ владельца: быть реально уверенным,
  что таблица считает экономически и математически правильно — «что мы
  не идиоты»: полный экономико-математический аудит формул листа (карта
  формул — база, добавить проверку корректности каждой считаемой величины
  экономическим смыслом). (58в) Порог совместных не навязываем, мягкая
  подсказка при пересечении 600 тыс. (59а) «Тревога не заключён»
  поглощена «Близкими к дате»: тревога = просрочка БЕЗ живой причины.
  (60а+) Конструктор отменён в пользу ролевых экранов, НО идею
  прорабатывать на будущее — здоровый скептицизм владельца: «новый канон
  может оказаться дерьмом». (61) SVOD_MODES: прежде отмены — подробно
  рассказать владельцу, о чём документ и чем хорош/плох.
- **94. РЕШЕНИЯ (вопросы 47–51 серии).** (47в) «Совместные закупки» —
  и категория в оси организаций (как в книге), и признак строки
  («считать все совместные района одной кнопкой»). (48в) До ролевой
  модели норма доли ЕП — сравнение со СРЕДНЕЙ ПО РАЙОНУ («выше/ниже
  средней»), выдуманные 50/30/40 убираются. (49в+) Снимки: хранить всё;
  идея владельца одобрена к проработке — хранение ДЕЛЬТ между «якорными»
  полными бэкапами (мандат: «ты больше знаешь в программировании — решай,
  моё предложение побрейнштормь»); решение при реализации: якорь =
  полный недельный снимок, между ними — построчные дельты. (50) Вкладка
  «РАСЧЕТ» = собственный скрипт владельца под автогенерацию отчётов,
  «не оч — забей»: продукт её игнорирует; ⏰ НАПОМНИТЬ владельцу скинуть
  скрипт — вдруг есть полезное. (51в) Место «Системы» в навигации —
  решается в консолидацию, навигацию перекраиваем один раз.
- **93. РЕШЕНИЯ (вопросы 42–46) + КАНОН ОСЕЙ ВРЕМЕНИ + два признания.**
  (42а) Неделя = СРЕЗ состояния, не фильтр: недельный выбор переезжает
  из фильтров в переключатель «Эфир / Архив недели», янтарные оговорки
  уходят за ненадобностью. (44б) «Нормальная строка» = счётная по
  формулам листа (F, O, P) + предмет. (45-КАНОН владельца, ПОДТВЕРЖДЁН
  ЗАМЕРОМ): «колонки N и Q канонические — остальное производное
  формулами; так работают наши таблицы». Замер по всем восьми книгам:
  O (кв. плана) — формула в 3 333 из 3 333 заполненных; R (кв. факта) —
  формула в 2 973 из 2 973 заключённых; рукописных и пустых — НОЛЬ.
  Продукт читает производные и СВЕРЯЕТ с датами; расхождение = сломанная
  формула (детектор Г1). (43 — ПРИЗНАНИЕ: владелец прав) развилка
  «достраивать ли квартал факта» была беспредметной — лист сам считает R
  формулой из Q, пустых R при заключении не существует; «достраивание
  кодом с пометкой» упраздняется в пользу чтения R / вычисления из Q.
  (46 — ПРИЗНАНИЕ: владелец прав) строк «квартал есть, плановой даты
  нет» НЕ СУЩЕСТВУЕТ ни в одной книге — класс из реестра §5.4 был
  синтетикой код-аудита (второй случай после Р-02); корзина «без даты
  внутри квартала» не нужна, вопрос аннулирован. УРОК (усиление п.64д):
  каждый класс из код-аудитов ОБЯЗАН быть подтверждён живым экземпляром
  до попадания в реестры и вопросы владельцу — теперь проверяю все
  оставшиеся классы реестра расхождений на живых данных.
- **92. РЕШЕНИЯ (вопросы 37–41) + ГЛАВНАЯ ПРОДУКТОВАЯ ПРОБЛЕМА.**
  (37) Слова владельца-канон: «Реестр буквально планировался как GOOGLE
  SHEET ВНУТРИ СИСТЕМЫ — полностью рабочий, включающий абсолютно всё
  полезное и абсолютно все правила: от защиты данных до групп
  пользователей. Сейчас это мясо, список проблемных строк,
  превращающихся в карты — продуктово плохо. Реестр, ошибки, проблемы,
  рекомендации, сигналы, признаки строк — нужно сводить во что-то ОДНО:
  большое, глубокое, позволяющее погружаться, всё видеть, работать с
  этим. Это ОСНОВНАЯ проблема продукта». Вторая: «делать сразу ЭТАЛОН,
  а не поддопиливать по чуть-чуть»; поручение поднять полную
  формулировку задачи от 14.08 — возможно, понимание было неполным.
  (38а) Разбивка КП/ЕП в срезах видов деятельности — как в листе.
  (39а+) Обещания по конкретной сделке — никогда; обоснованные опытные
  прикидки — можно, если ОЧЕВИДНО и ОБОСНОВАНО; «не хватает данных —
  нафиг пустые обещания»; прогнозы и рекомендательные системы — ок, но
  сделанные хорошо: скоринг-«доверие» был лоскутным и надоел; движок
  рекомендаций (В-20 закрыт): «если там есть настоящее и хорошее —
  ПЕРЕПЛАВЛЯЕМ», не восстанавливаем как было. (40а+) Стадии «в работе у
  ОЗ»: редактируемые отметки — можно в принципе; плюс посмотреть
  мониторинг закупок и РАЗОБРАТЬСЯ в исторических отчётах и таблицах —
  был ли в данных маркер этих стадий. (41б) Адресат вне управления — на
  уровне органа из типизированного словаря, без поимённости.
- **91. ДИРЕКТИВА (допил каждой вкладки до 100/100).** Слова владельца:
  «помимо консолидации и более логичного дизайна существующего нужно это
  существующее — КАЖДУЮ ВКЛАДКУ допилить до 100/100 — без этого мы имеем
  куски со швами». Критерии сотни на вкладку: подпись периметра везде;
  карточка БЗ у каждой метрики (закрыть 12 дыр «экран без БЗ»);
  пустые/ошибочные состояния с причиной и действием; контраст обеих тем;
  мобильная вёрстка; литературные тексты без жаргона; провенанс чисел до
  строк; снятие дублей смыслов и швов между вкладками; скелеты и
  скорость; доступность. Главный «кусок со швом» — пустой каркас
  «Мониторинга»: наполнить данными в первую очередь.
- **90. РЕШЕНИЯ (вопросы 32–36 серии).** (32а) Доля ЕП: ДЕНЕЖНАЯ —
  главная в заголовках/рейтингах/оценках; счётная — рядом второй строкой,
  обе подписаны. (33а) Метрика «исполнено в срок к плановой дате»
  вводится сразу (в срок / ≤30 дней опоздания / >30 / просрочено без
  заключения) с честной сноской о массовом подгоне дат (п.80) и
  антиподгонной пометкой из истории строки по мере накопления таймлайна.
  (34а) «Низкая экономия»: граница 5 % с условием «участников ≥ 2»
  (число заявок — из мониторинга). (35) Письма о смене способа — ПОКА
  скриптом внутри таблицы (тот самый Apps Script, что логирует правки);
  серверная рассылка — позже. (36) ФАКТ-КАНОН от владельца: план-график
  ведут в АИС и ЕИС; владелец параллельно работает над другим проектом
  и подсветит, когда закончит; «потом сведём к АИС/ЕИС или СМАРТу, пока
  так» — продукт остаётся на книгах, интеграция с планом-графиком —
  будущая фаза с готовым решением по ключам (ИКЗ там уже есть).
- **89. РЕШЕНИЯ (вопросы 27–31 серии).** (27) Роли управлений определяю
  Я САМ по критериям (оборот, число подведов, профиль закупок, законная
  специфика) — мандат владельца: «у тебя есть все данные, интернет и
  законы; ты можешь быть гениальным экономистом, если я попрошу»;
  ролевую модель предъявить с обоснованием каждого критерия. (28б, 29б)
  Кривые квартала и года — из СОБСТВЕННОЙ истории района, С
  САМООБУЧЕНИЕМ: профиль пересчитывается по мере накопления кварталов,
  подпись «по фактическому профилю района, обновлено {дата}»; отдельно
  КП и ЕП. (30б) ВСЕ пороги светофоров — в единый редактируемый
  справочник настроек, порог виден в легенде. (31б) Факт выше плана —
  информационная пометка; замечанием становится, только если план в
  книге не был увеличен (данные разошлись с действительностью).
- **88. РЕШЕНИЯ (вопросы 22–26) + ДИРЕКТИВА о консолидации.** (22в)
  Дробление: пороги в редактируемый справочник настроек + флаг ВСЕГДА
  раскрывается до баллов. (23в) Детекторы дробления сливаются в один
  двухуровневый (внутри подведа / через подведы), единая шкала, пометка
  уровня. (24) Тесты аномальности — ОСТАВИТЬ ОБА, но подать
  образовательно: «очень понятно описать, показать, разъяснить и
  отработать продуктово, чтобы можно было погрузиться, понять, найти
  косяк» — интерактивное объяснение Бенфорда (ожидаемое против
  фактического распределение первых цифр; пик у лимитов — законное
  поведение, не криминал) и z-оценки с раскрытием строк-вкладчиц. (25в)
  Сводный индекс дисциплины снят; слова владельца-канон: «истории с
  весами сложно описываемые — нужны более абсолютные, понятные,
  применимые вещи, дающие реальную рекомендацию, интуитивно; к одному
  числу можно прийти, только когда все сигналы и метрики правильные и
  без мусора». (26б) Оценки: всё в одном месте, но разбито КОНКРЕТНЫМИ
  категориями по природе (уточнение владельца): «ОШИБКА ЗАПОЛНЕНИЯ КНИГИ»
  отдельно (дисциплина ввода исполнителя: пустые обязательные колонки,
  кривые даты, несведённые итоги), дефекты формул/листа отдельно,
  содержательное исполнение закупок отдельно — и далее по природе, без
  абстракций; термин «доверие» убрать.
  ⚠ КРИТИКА ВЛАДЕЛЬЦА (принята как директива): «на новых страницах я
  пока не вижу переплавку — вижу, что они буквально новые и ПРО НОВОЕ;
  есть ощущение РЕГРЕССИИ»; «непрозрачно и раскидано где попало».
  Значит: следующий крупный шаг — КОНСОЛИДАЦИЯ, а не новые поверхности:
  старые экраны начинают переезжать в новые по списку удаляемого
  (п.87/19: сначала строим — построили — теперь показываем переезд),
  сигналы и метрики собираются в одно место, дубли смыслов убираются.
- **87. РЕШЕНИЯ (вопросы 18–21 серии).** (18а) Схемы нумерации нормативных
  актов семи мунпрограмм 2027–2031 запрашиваются у ответственных СЕЙЧАС,
  пока программы пишутся (письмо-образец — за мной). (19) Порядок
  переплавки подтверждён: СНАЧАЛА СТРОИМ НОВОЕ; список удаляемого — на
  отдельное решение после. (20) Опрос источников — ТОЛЬКО В РАБОЧИЕ ЧАСЫ
  **8:45–18:20 по Камчатке** (вебхук работает всегда — он бесплатный);
  квота экономится. (21) «ЗАПУСК ИДЁТ» — предзапускного списка нет,
  руководство пользователя / вход по логину / автокопия базы становятся
  обычной очередью по приоритету.
- **86. РЕШЕНИЯ (вопросы 14–17 серии).** (14а) АВТОВЫКАТ: сервер сам
  подтягивает новые версии зелёной главной ветки, после выката —
  автопроверка здоровья, при провале — откат на предыдущий образ.
  (15а) ДЕНЬ СРЕЗА: ПЯТНИЦА 12:00; регламент прозрачен: «до четверга все
  заполняют книги, в пятницу пишется отчёт»; час и день — подписью на
  недельных экранах. (16) 2025: остаётся опционально в фильтрах; сверка —
  ДВА РЕЖИМА с объяснением у переключателя: «рабочий 2026» (двухлетние
  официальные строки сверяются по 2026-компоненте, хвост 2025 не
  считается расхождением) и «полный с хвостом 2025»; конструкция обязана
  пережить 2027-е («работаем с 2026, дальше 2027 и т.д.»). (17а)
  РАСЧЁТНАЯ ЭКОНОМИЯ: обе цифры с подписями — «по методике листа (8 % от
  неосвоенного)» и «по фактической статистике торгов»; методика нашей —
  прозрачно объяснена (см. ниже), экономическая корректность обязана быть
  выверена «как швейцарские часы»; ЗАКРЫТЬ ОДНИМ ИЗ ПЕРВЫХ — «все ноют».
  Методика-канон: ожидаемая экономия = незаконтрактованный остаток ПО
  СПОСОБАМ × средневзвешенный (по деньгам, не по штукам) фактический
  процент снижения состоявшихся процедур ЭТОГО способа за период; ЕП
  даёт ~0; сегменты не смешиваются; рядом — размах (мин/макс кварталов).
- **84. КАНОН (словарь стадий процедуры — собран сам, УЭР подтвердит
  постфактум).** По слову владельца «почему мы не можем сами без них»:
  словарь выведен из трёх источников (этапные колонки мониторинга, его
  статусные записи, 2 795 строк этапности в книгах). Одиннадцать стадий:
  Планирование → Подготовка документации → Заявка в УО → Размещено
  (подслучай: повторное, вкл. «не прошёл контроль УФК») → Приём заявок →
  Рассмотрение → Торги/переторжка → Итоги/протокол → Заключение (на
  подписании) → Заключён → Исполнен; ветки: Не состоялся (0 заявок, 45
  живых случаев) · Отменена (заказчиком/ФАС) · Приостановлена (ФАС/УФК).
  Принцип источников: стадии 10–11 — ТОЛЬКО структурно (дата заключения,
  суммы); 3–8 — из мониторинга по номеру процедуры; 1–2 и ветки — из
  колонки U по словарю (после возвращения ей роли, п.79). Текст никогда
  не назначает «заключён».
- **85. РЕШЕНИЯ (вопросы 10–13 серии).** (10б+) «нецелесообразность
  проведения аукциона» — синоним канонов п.8/п.18 С УСЛОВИЕМ владельца:
  если рядом НЕТ объяснения, почему нецелесообразно, — мигает сигнал
  «объяснитесь»; если объяснение есть — показываем его. (11а) Семейство
  «прямая ссылка на норму»: справочник пунктов ст. 93 44-ФЗ (~15
  позиций), продукт расшифровывает «п.4 ч.1 ст.93 — закупка малого
  объёма до 600 тыс.». (12б) Слой «заявки на перераспределение экономии»
  в безопасной версии (подсветка автоматом, счёт после клика-
  подтверждения) + КРУПНАЯ ДИРЕКТИВА владельца: «с последующим улучшением
  всей системы на перераспределение, потому что сейчас экономия
  бесконечно растёт — решения, как это исправить, придумано не было» —
  спроектировать системный процесс работы с экономией (стык с Q8:
  экономия, оставленная ГРБС на распределение, считается отдельно).
  (13в→а) Дубли строк: нейтральная пометка «возможный дубль» с вопросом
  исполнителю; эскалация в сигнал «удалите дубль» при полном совпадении
  вплоть до копейки и номера процедуры.
- **83. РЕШЕНИЕ (доп-вопрос 2): разметка видов — владелец кликом, я как
  подстраховка.** Порядок для новых строк стадии «в течение года»:
  (а) строка появляется с подписью «вид не определён», в карточке —
  выпадающий список девяти видов, владелец (или назначенный) размечает
  одним кликом, продукт запоминает; (б) подстраховка: если строка висит
  неразмеченной дольше двух недель — размечаю я с пометкой «разметка
  предварительная», владелец может исправить в любой момент. Стартовые
  46 строк уже размечены анализом 14.08.
- **82. РЕШЕНИЕ (доп-вопрос 1): ТУМБЛЕР «юридически чисто ↔ как считает
  свод».** Слова владельца: «иметь возможность включать и выключать их,
  чтобы было и как правильно, и как считает свод — убьём двух зайцев».
  У доли ЕП и комплаенс-проверок 44-ФЗ — переключатель: режим «без выплат
  физлицам и платежей без договора» (юридически чистый) и режим «как в
  листе» (все строки по колонке способа). Оба режима подписаны; дефолт —
  «как в листе» (совпадение со СВОДом по умолчанию, чистый режим — явный
  выбор). Деньги плана/факта в общих итогах — всегда полные.
- **81. РЕШЕНИЯ (вопросы 7–9 серии): журнал AF утверждён; выплаты и
  платежи — подклассы стадии с раздельным показом.** (7а) Шаблон журнала
  исполнения в AF утверждён: «ДД.ММ.ГГГГ · договор №… · сумма т.р. ·
  примечание» (выплата/подотчёт, платёж по счёту — свои слова события);
  контрольное правило «сумма журнала = факт»; разослать исполнителям
  трёх книг как просьбу. (8, 9) Слова владельца: «если они в течение
  года — ок: пусть будут как бы подкатегорией по одному из фильтров и
  аналитики, но по сути выводи в отдельный класс — не меняя сути, но
  отдельно это показывая». Иерархия: стадия «В течение года» — родитель;
  внутри ПОДКЛАССЫ с собственными подписями и раздельным показом:
  «Закупки серией договоров» (36 строк), «Выплаты физлицам» (7),
  «Платежи без договора» (3). Фильтры и аналитика видят их внутри
  стадии; предъявление — раздельное.
- **80. ДИРЕКТИВА (правдоподобие объяснений + красный флаг постфактум).**
  Слова владельца 14.08: (а) при изучении дат — с учётом отраслевой
  специфики и АКТУАЛЬНЫХ 44-ФЗ и 223-ФЗ для данных видов организаций и
  работ — проверять: все ли соблюдают, правильно ли пишут, не обманывают
  ли в комментариях, насколько массивы объяснений адекватны и
  правдоподобны; это KPI организаций плюс качество заполнения и подачи
  данных; (б) КРАСНЫЙ ФЛАГ: закупки, вносимые постфактум или в день факта
  («запланировать на 15-е, купить 15-го, внести 15–16-го») — по будущим
  и по прошлым. ЗАМЕР 14.08: из 2 971 заключённой строки с обеими датами
  76 % (2 251) заключены ДЕНЬ-В-ДЕНЬ с плановой датой, ещё 2 % в ±2 дня;
  96 % таких — ЕП; лидеры УО (1 683) и УКСиМП (422). Это не единичный
  флаг, а массовый режим: план для ЕП в основном документирует покупку,
  а не предшествует ей. ЧЕСТНАЯ ОГОВОРКА: совпадение дат само по себе не
  различает «внесли задним числом» и «подогнали плановую дату при
  заключении» — различает только история строки (журнал правок/снимки:
  менялась ли N после появления Q) — это уровень (б) детекции, его даёт
  таймлайн. Двухуровневая детекция: (1) структурная — близость N↔Q;
  (2) провенансная — строка впервые появилась в книге уже с фактом.
- **79. РЕШЕНИЕ-КАНОН (вопрос 6 серии): колонка U = причина отклонения
  от срока; поля-справочники с контекстным хвостом.** Гипотеза владельца
  («U — по идее причина отклонения, если просрочили») ПОДТВЕРЖДЕНА
  замером 14.08: U заполнена содержимым у 95 % строк, заключённых с
  опозданием (154/162), и у 95 % просроченных без заключения (21/22) —
  против 6 % у заключённых в срок (181/2809) и 12 % у ненаступивших
  (40/338). Роль U однозначна: причина отклонения от плановой даты.
  Решения: (а) вариант (б) — вернуть U роль по СЛОВАРЮ (словарь причин
  2.0 с типизацией retro/live — он ровно для этого) — ПОСЛЕ мониторинга;
  (б) ОБЩИЙ ПРИНЦИП-КАНОН: каждое поле под статус/комментарий в идеале —
  ПО СПРАВОЧНИКУ + возможность небольшого контекстуального комментария
  дополнительным хвостом; (в) исторический контекст от владельца: таблицу
  не хотели расширять, поэтому статусы, не предусмотренные изначально,
  полезли в чужие колонки — сейчас придумываем обработку и улучшение на
  уровне И таблицы, И дэшборда.
- **78. РЕШЕНИЕ-КАНОН (вопрос 5 серии): сигнал «просроченное обещание» —
  в постоянную работу.** Владелец: «включаем в работу». Правило: в
  комментарии обещание с датой («будет заключен/подписан/размещён до
  ДД.ММ.ГГГГ»), дата прошла относительно момента снимка, а дата
  заключения — заглушка → карточка исполнителю «обновите комментарий или
  проставьте дату заключения» с адресом ячейки. Это сверка текста со
  структурой (п.72а), не чтение статуса (п.27 в силе). Живой прецедент:
  УО, строка 331 («договор будет заключен 01.06.2026» при пустом
  заключении на 14.08). Включение = подключение слоя аннотаций к экрану
  (правило уже написано и покрыто тестами волной 14.08) — в ближайший
  коммит после финиша волны «строка во времени».
- **77. Вопрос 4 серии (Распоряжение 03.09.2025): ответ «не знаю» —
  запрос закупщикам.** В книгах 14 номеров распоряжения с одной датой
  (канон № 112 — 202 ячейки; №№ 113–126 — 62 ячейки, почти все УО;
  номера идут плотной серией — на опечатки не похоже). Владелец поручил
  сформулировать запрос закупщикам: предоставить документы и уточнить
  всё по закупкам, которые на них ссылались. До ответа продукт помечает
  не-112 номера нейтрально: «номер основания требует подтверждения»,
  без обвинительного тона.
- **76. РЕШЕНИЕ-КАНОН (вопрос 3 серии): «хотелки» = стадия «инициативная
  заявка» с правилом дожития.** Владелец выбрал вариант (в). Материал:
  88 строк книги УО (план 5 943,1 тыс., факт 0, заключено 0), шесть
  подведов, комментарий ровно в трёх вариантах («хотелки» 48, «Хотелки»
  20, «просто хотелки» 20). Канон: (а) слово «хотелки» КОДИФИЦИРУЕТСЯ как
  маркер стадии — читается структурно (ячейка AF, чьё содержимое ЦЕЛИКОМ
  равно маркеру из словаря; п.27 не нарушен: маркер = код, как «Х»);
  (б) стадия «инициативная заявка без подтверждённой потребности»: план
  виден, но подписан отдельно («в плане, в т.ч. инициативные заявки
  X тыс.»), в риск-списки не шумит; (в) ПРАВИЛО ДОЖИТИЯ: заявка без
  движения к началу 4 квартала поднимается управлению карточкой
  «подтвердить или снять»; (г) сверку со СВОДом НЕ трогаем: официальный
  план включает эти строки — наши производные подписывают их отдельно,
  а не вычитают втихую.
- **75. РЕШЕНИЕ-КАНОН (вопрос 2 серии): словарь причин 2.0 + строка во
  времени.** Владелец утвердил уточнённый вариант и расширил: (а) словарь
  причин ТИПИЗИРУЕТСЯ: то, что есть, — типизировать, добавить уточнения
  и максимум сути; каждая причина несёт ТИП: «провенанс по уже
  совершённой закупке» (объяснение прошлого) ИЛИ «живая организационная
  проблема текущего этапа»; (б) отдельно важны закупки, БЛИЗКИЕ к
  реализации: произойдут ли сейчас или есть нюансы — риск-статус по мере
  приближения к плановой дате; (в) относительно плановой даты по мере
  приближения к ней показывать ВСЕ изменения по строке контракта,
  ОСОБЕННО просрочки — по всей истории проекта, отдельно, максимально
  красиво и корректно. Состав словаря: 12 → 14 живых (№10 слит с №9,
  №12 переформулирован конкретно «подготовка ТЗ/сметы/обоснования НМЦК/
  проекта контракта», №7 остаётся ждать прецедента; + три семейства:
  отсутствие финансирования С ДАТОЙ переноса, кассовый план,
  поздний проект контракта от поставщика).
- **74. РЕШЕНИЕ-КАНОН (вопрос 1 серии): AG = номер процедуры.** Ответ
  владельца дословно: «да. убираем иные комментарии, пусть они будут как
  сигнал. не важно как она называется, уэр считает нужным писать такой
  комментарий». Следствия: (а) колонка AG книг — структурный ключ «номер
  процедуры» (формат ЭА152-26 и родня ЭЗК/ЭЕП/ЭАС), основной мост к
  «Ежедневному мониторингу»; (б) НЕ-номерное содержимое AG не трётся
  молча — поднимается сигналом «в колонке номера процедуры посторонний
  текст, перенести в примечание» (4 % содержимого, 14 ячеек из 345);
  (в) просьба исполнителям: номер проставлять всегда, без пробелов и
  приписок; (г) парсер номера из AG — структурное чтение, канону п.27 не
  противоречит (номер — код, не свободный текст).
- **73. ДИРЕКТИВА (мобильность и навигация) — слова владельца 14.08.**
  (а) Светлая и тёмная темы и вёрстка под смартфоны и разные разрешения
  обязаны работать хорошо; на мобильной версии нужен АНАЛОГ наведения
  мыши на метрики (тап/долгое нажатие открывает ту же подсказку БЗ);
  (б) блок «Что изменилось с последнего среза» ДО СИХ ПОР не переведён
  вниз (п.35 — долг!) и не имеет отдельной кнопки навигации;
  (в) нужны отдельные вкладки/кнопки навигации для «закупок, не
  обеспеченных финансированием» и «закупок, проводимых в течение года».
- **72. ДИРЕКТИВА (комментарии M/U/AE/AF/AG/AH — слой знаний).** По
  1 289 ячейкам с датами: (а) сигнал по НЕАКТУАЛЬНЫМ комментариям —
  текст описывает этапность, противоречащую факту (например «в стадии
  подачи заявок» при заключённом контракте); это НЕ чтение статуса из
  текста (п.27 в силе), а проверка СОГЛАСОВАННОСТИ текста со структурными
  колонками; (б) придумать, как внедрить актуальные комментарии и их
  провенанс в систему, НЕ портя существующие поля и сигналы; (в) анализ
  каждой закупки с комментариями по всей системе + возможное
  сопоставление с «Ежедневным мониторингом»; (г) владелец приложил три
  файла частичной стандартизации: «Обоснование выбора способа определения
  поставщика.docx», «Причины отклонения.docx», «общий реестр доп инфы вне
  причин отклонений.xlsx» — словари-канон формулировок. Проработка 100 %
  без галлюцинаций; по проблемным местам задавать вопросы.
- **70. Строки-обрубки: живых нет (закрытие вопроса «покажи примеры»).**
  Прогон 14.08 по сырым листам ВСЕХ восьми книг: строк, которые формулы
  листа считают, а наш счётный набор не содержит, — 0; строк с деньгами
  2026 года без предмета (G) или номера (A) — 0. Класс существует только
  в правилах счёта; при появлении живого экземпляра продукт обязан
  показать карточку с адресом. Срочность развилки снята.
- **66. РЕШЕНИЕ-КАНОН (свежесть снимков — «прямой эфир»).** Слова
  владельца по следам фиктивных −181,9/−313,6: «пусть сервис обновляет
  эти снимки или вообще работает в прямом эфире — подтягивает». Механика:
  (а) СВЕРКА读 обе стороны ИЗ ОДНОГО ПРОЧТЕНИЯ: официальные ячейки и
  пересчёт по строкам берутся из одного и того же чтения книги — класс
  «кэш против живого» исчезает по построению, кэш для сверки не
  используется вовсе; (б) снимки обновляются сервером сами по расписанию
  (рабочие часы, каждые 30–60 минут; квоты Sheets API позволяют — 8 книг
  = десятки запросов), у снимка виден возраст; (в) на каждой вкладке —
  возраст данных и кнопка «подтянуть сейчас»; (г) режим «Эфир» остаётся
  дефолтом, архивные срезы — явный выбор. Реализация — ближайшая волна
  (server: планировщик снимков + сверка одним чтением).
- **65. РЕШЕНИЕ-КАНОН (дефекты листа — продуктовые сигналы).** Слова
  владельца по дефекту границы 999 помесячного СВОДа: «если такой дефект
  есть и таблица растёт — дэшборд это тоже должен решать: показывать, что
  проблема есть, так чтобы исполнитель сам мог всё исправить». Детекторы
  дефектов формул листа (Г1 «значение вместо формулы», Г2 «обрезанный
  диапазон», включая границу 999 ЕП-яруса у семи ГРБС) — не разовый аудит,
  а постоянные продуктовые сигналы с инструкцией исполнителю: какая
  ячейка, какая формула, что протянуть. Приоритет поднят: Г1/Г2 входят в
  ближайшую волну диагноста, не «после».
- **63. ПОПРАВКА владельца к кодификатору (к пп. 60–61).** Региональная
  классификация УФБП уже иерархическая, но видит только ДО основного
  мероприятия — глубже слепа. Направление: изучить опыт региональных и
  дополнительных классификаторов других регионов; отталкиваться от
  реального состава наших закупок (предметы пишутся по факту — пусть
  неконсолидировано и без группировок) и лимита 210 символов; найти
  свитспоты и спроектировать систему, которая даст мощную фильтрацию и
  отчёты по нужным контрактам в рамках ТД и ПМ, малых закупок и ЭА — и
  мощную аналитику поверх.
- **61. ДИРЕКТИВА (исследование чужого опыта) — одобрено владельцем.**
  Изучать устройство и опыт СМАРТ (Бюджет-СМАРТ/Кейсистемс), 1С БГУ,
  Бюджет-NEXT и других учётных систем/БД как источник паттернов качества
  для нашей системы: справочники с датами действия и вышестоящими кодами,
  версионирование кодов («код предыдущего периода»), учётные номера
  обязательств, генераторы отчётов. Продолжение линии
  smart-process-mapping/smart-stock-reports.

- **57. ДИРЕКТИВА (ревизия канона мультиперспективой) — в очередь после
  текущих волн.** Проанализировать ВЕСЬ канон проекта глазами каждого
  специалиста большой ИТ-команды и предметников: инженер БД, бэкенд/
  фронтенд-инженер, дизайнер, продуктовый менеджер, статистик, аналитик
  данных, закупщик, экономист, автор еженедельных отчётов, исполнитель,
  заполняющий таблицы и сталкивающийся со сложностями, руководитель
  (управленческие решения и принципы) — на предмет оптимальности,
  качества и проработанности. По итогам — наполнить, переработать и
  консолидировать проект. ОБЯЗАТЕЛЬНОЕ УСЛОВИЕ ДО НАЧАЛА ПЕРЕРАБОТКИ:
  пройти канон от А до Я и задать владельцу МНОЖЕСТВО вопросов — каждый
  пункт канона утверждается явно, чтобы уверенность была полной.
