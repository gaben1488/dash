# Передача состояния — конец сессии 20.08.2026 (пауза по лимитам)

## Что бежит в фоне (восстанавливать первым делом)

| Волна / агент | runId / способ восстановления | Состояние на паузу |
|---|---|---|
| Переплавка страниц | `Workflow({scriptPath: .../grand-remelt-wf_c9fb6ac1-b38.js, resumeFromRunId: 'wf_c9fb6ac1-b38'})` | 3 зоны из 10 завершены, остальные догоняют |
| Атлас карточек | `.../cards-atlas-wf_2a484294-14f.js`, `wf_2a484294-14f` | 6 карт из 7 на диске, синтез-план не готов |
| Enterprise-сервер (вебхук/API/скорость, Context7) | `.../enterprise-backend-wf_f10bb070-fb1.js`, `wf_f10bb070-fb1` | разведка → укрепление |
| Голос владельца + взаимодействия | `.../owner-voice-and-interactions-wf_88eced4b-9ad.js`, `wf_88eced4b-9ad` | археология транскриптов |
| Закупки 2.0 (Drive) | агент, SendMessage по id из журнала сессии | 12 файлов залиты, пилот УЭР в работе |
| Карта БЗ (п.135) | агент | «данные собраны, пишу карту» — умер на обрыве |
| Охота на самовключения фильтров (п.134) | агент | перезапущен после лимита |

Восстановление: `resumeFromRunId` возвращает завершённых агентов из кэша мгновенно —
перезапускать волны целиком безопасно и дёшево.

## Что сделано этой сессией и лежит в дереве (не закоммичено)

- Канон дорос до **п.135**: 130 (справочники против барахла во всех полях 2.0),
  131 (Пульт → «Пульс», концепция), 132 (решения по концепту Пульса),
  133 (провенанс в правом верхнем углу шапки), 134 (искоренить самовключения
  фильтров + скорость), 135 (БЗ картировать и поднять выше канона).
- **Узел провенанса** (п.133): `packages/web/src/components/live/provenance-text.ts`
  (+тесты, 12 стражей) и `ProvenanceHub.tsx` — написаны, **в шапку ещё не вставлены**
  (Header.tsx свободен; вставка — справа от кнопок темы и сброса фильтров).
- **Сверка канона книг с сигналами dash**: глава 12 в
  `2026-08-20-sheet-rules-inventory.md` + docx владельца; команда для Gemini
  пересобрана в 43 правила; 7 пробелов закрыто; выправлена легенда AE–AH.
- **Карта сигналов по продукту**: `2026-08-20-signal-product-map.md` — 174 класса,
  34 живут в 2+ домах, 22 носят 2+ имени (41 лишняя подпись), 48 считаются и
  никуда не выводятся. Находки: словарь `getSignalBadges` — призрак (никто не
  читает), устаревшая легенда про «подвисшие контракты» на Пульте.
- **Сверка корзин 1-к-1 по дампам**: 552 «не обеспечено» (226 с цитатой оператора
  «нет финансирования»), 46 «в течение года» (у всех дата N заполнена),
  пересечение 0, стёртых формул 2. Вывод: сигналы разделены чисто, лишнего нет;
  «Факт без квартала» — имя-врун, первопричина в отсутствии даты N.
- **Правило в CLAUDE.md**: атлас карточек обязателен к обновлению любой волной,
  трогающей экран.

## Первые шаги следующей сессии

1. Восстановить пять волн и агентов (таблица выше).
2. Вставить `ProvenanceHub` в `Header.tsx` (nav-tools, справа от темы и сброса),
   прогнать гейт веба.
3. Дождаться реестра требований владельца (`2026-08-20-owner-requirements-register.md`)
   и плана финала (`2026-08-20-endgame-plan.md`) — дальше работать по ним.
4. Коммит логическими группами + выкат (мониторинг v2 первым номером) со
   скриншотами прода в обеих темах — обещано владельцу.
