/// <reference types="vite/client" />

// ── Типы среды сборки.
//
//    ЧТО ВЗЯТО ГОТОВЫМ (канон п.112). Vite поставляет описание собственной
//    среды (`import.meta.env`, импорт стилей и картинок как модулей) — эта
//    строка его подключает. До неё описания не было, и каждое обращение к
//    `import.meta.env` приходилось прятать за `as any`
//    (`components/HeroKPICard.tsx`, строка 263: «`as any` на import.meta —
//    ради переносимости настроек сборки»). Приведение к `any` — это не
//    переносимость, а отключённая проверка: опечатка в имени переменной
//    среды после него не ловится ничем.
//
//    Файл нужен ровно затем, чтобы ветка «только в разработке» проверялась
//    типами, а не держалась на честном слове.
