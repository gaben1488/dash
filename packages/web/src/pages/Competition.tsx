/**
 * «Конкуренция» — исходы 1–2 продукта: меньше закупок у единственного
 * поставщика, больше конкурентных; ниже операционные расходы. Каркас;
 * наполнение — волной (премия за конкурентность, цена отказа от конкурса в
 * рублях, кандидаты на объединение закупок — включая ЕП).
 */
export function CompetitionPage() {
  return (
    <div className="max-w-7xl mx-auto px-4 py-6 space-y-4">
      <h1 className="text-lg font-semibold text-zinc-800 dark:text-zinc-100">Конкуренция</h1>
      <p className="text-sm text-zinc-500 dark:text-zinc-400 max-w-2xl">
        Сколько стоит отказ от конкурса и где объединение закупок даст экономию.
        Раздел собирается — расчётное ядро уже считает, экран появится в
        ближайшем обновлении.
      </p>
    </div>
  );
}
