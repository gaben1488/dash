# Caddyfile внутри web-образа — отдаёт SPA на :80
:80 {
    root * /usr/share/caddy
    encode gzip
    try_files {path} /index.html
    file_server
    log {
        output stdout
        format console
    }
}
